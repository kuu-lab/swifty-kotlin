package kotlin

/**
 * `Nothing` has no instances. It represents the type of an expression that
 * never returns normally (e.g. a thrown exception, an infinite loop, or a
 * `return` statement). It is the subtype of every Kotlin type.
 */
public final class Nothing private constructor()
