package golden.sema

fun main() {
    val max = Long.MAX_VALUE
    val min = Long.MIN_VALUE
    val bits = Long.SIZE_BITS
    val bytes = Long.SIZE_BYTES
    val explicitMax = Long.Companion.MAX_VALUE
    val explicitMin = Long.Companion.MIN_VALUE
    val explicitBits = Long.Companion.SIZE_BITS
    val explicitBytes = Long.Companion.SIZE_BYTES
}
