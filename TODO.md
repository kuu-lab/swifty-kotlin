# Kotlin Compiler Remaining Tasks

### Runtime コルーチン

- [x] BUG-181: `withTimeout`/`withTimeoutOrNull`（`kk_with_timeout`/`kk_with_timeout_or_null`）がブロック本体を**呼び出し元と同一の continuation** 上で実行していたため、タイムアウト後に放棄されたブロックの entry loop が呼び出し元の `RuntimeContinuationState` を共有し続け、残っていた `delay()` タイマーが後から呼び出し元に対して spurious な `signalResume()` を発火していた。結果、呼び出し元の**次の**中断点（例: `job.join()`）が二重に resume され、同じ body の並行実行のうち片方が completion gate を先に signal して `runBlocking` が早期 return → 後続の文（`coroutineScope { throw ... }` の catch 内 `println`）が出力されないままプロセスが終了する。CI の `Verification 5/5 (kotlinc Diff)` が `Scripts/diff_cases/coroutine_cancellation_edge_cases.kt` で `boom` 行を落とす flaky 失敗（run 30518813981 / 30687957033 / 30728548711 / 30748246808 / 30844735766）の原因。修正: ブロックを `kk_coroutine_continuation_new` で作った専用の子 continuation（scope はタイムアウト用スコープ、専用 job handle 付き）で実行し、タイムアウト時にはその job を cancel して協調的に巻き取る。回帰: `Tests/RuntimeTests/RuntimeCoroutineAdvancedTests.swift` の `testWithTimeoutOrNullDoesNotResumeCallerContinuationAfterTimeout`（修正前は spurious resume を確実に観測して fail する）

### KIR / Lowering（残り 3 件）
- [ ] DEBT-KIR-006: `Iterable<T>.joinToString(separator) { transform }` の transform ラムダが無視され、各要素の生の `toString()` がそのまま結合されるバグを調査・修正する。再現: `"a\r\nbb\r\nccc".split("\r\n").joinToString(",") { it.length.toString() }` は `"a,bb,ccc"`（誤り、正しくは `"1,2,3"`）になる。`split` 自体・`for` ループでの個別アクセスは正しく動作する。KSP-482 (#4625) のレビュー対応中に Base64.Pem の行長検証 diff case で発覚、該当箇所は `for` ループへ書き換えて回避済み。`map`/`filter` 等の他 HOF で同型の transform 無視パターンがないか要確認

### Sema 型チェック（残り 1 件）
- [x] DEBT-SEMA-001: 同一クラス内の後方宣言 member property を先行 member function から参照すると不正な型エラーになる。最小再現: `class Forward { fun get(): Int = value; var value = 10 }` は Kotlin では正当だが kswiftc は `KSWIFTK-TYPE-0001: Type constraint could not be satisfied.` で失敗する。現行 `typeCheckClassLikeMembers` は source-order pass 後に member function を再チェックするが、最初の pass で property type 解決前の診断が確定し、二度目の pass で回復できない。全 property type の副作用安全な prepass、または参照時の遅延解決と診断 defer が必要。PR #4632 の conflict repair 中にレビュー由来の再現を現行 `master` で再確認
- [ ] BUG-180: enum の companion object にユーザー自身が定義した関数が `EnumClass.function()` という静的呼び出し構文で解決できない。最小再現: `enum class D { A, B; companion object { fun f(): Int = 1 } }; fun main() { println(D.f()) }` は `KSWIFTK-SEMA-0024: Unresolved member function 'f'.` になる。`enum_basic.kt`（`Direction.opposite(...)`）の SKIP-DIFF 解除作業中（2026-07-31、DEADCODE-014 監査）に発見。enum の `values()`/`entries` 合成メンバーとは無関係の既存バグで、companion なしの単純な `class` の companion member 呼び出し（`ClassName.function()`）が正しく解決できるかどうかも要比較調査。2026-07-29 時点の pre-fix master でも同一症状を確認済み（enum 専用の可能性が高い）（番号は KSP-408 の master マージ時点で本 PR の BUG-169 と衝突していたため 180 に再採番。DEADCODE-014 監査で発見された内容自体は無関係）

### Diff skip 追跡（残り 5 件）
> 2026-08-02 マージ時再計測: [`docs/diff-skip-inventory.md`](docs/diff-skip-inventory.md)。現時点の `DEBT-DIFF-*` タグ付き skip は 72 件（001:19 / 002:0（解消済み） / 003:4 / 004:0（解消済み） / 005:7 / 006:0（解消済み、2026-07-29） / 007:42、distinct file 数は 65）。実測値は `find Scripts/diff_cases -type f \( -name '*.kt' -o -name '*.kts' \) -print0 | xargs -0 rg -o 'DEBT-DIFF-[0-9]{3}' --no-filename | sort | uniq -c` で確認。各タスクの完了条件は、該当ケースを通常 `diff_kotlinc.sh` に戻すか、JVM kotlinc を oracle にできない理由と代替 runner / unit test owner を同文書へ移すこと。
- [x] DEBT-DIFF-001: `Scripts/diff_cases` のうち JVM kotlinc reference では実行不能な target / classpath / runtime-only ケースを、diff harness の除外理由として維持するか、個別 runner / dependency injection で実行可能化するか棚卸しする。対象: Kotlin/Native・Kotlin/JS・KMP・`kotlin.io.path`・JDBC/SQLite・serialization・SLF4J/logging・system time/process API・assert JVM `-ea` 差分・compiler plugin API。→ **棚卸し完了（2026-07-29）**: `kotlin.io.path`（`path_basic.kt`）と `kotlin.uuid`（`uuid_basic.kt`）は既に解除済みで対象外。`compiler_plugin_api.kt` は既に DEBT-DIFF-006 へ再分類済みで対象外。残る19件（Kotlin/Native 4・Kotlin/JS 2・runtime-only system API 1・JDBC/SQLite 5・KMP expect/actual 1・serialization 4・SLF4J/logging 2）を、再ビルドした kswiftc + kotlinc 2.4.10（JDBC/serializationは実 jar・実 compiler plugin も追加注入）で全件再検証し、全件 keep skip で確定した。dependency injection は原理的に reference 側にしか効かない（kswiftc は jar/classpath を消費しない synthetic-stub 設計）ため、candidate 側に実装が無い・またはテスト内容が実 API 呼び出し規約と非互換なケースには効果がないと判明。JDBC 5件は従来「custom jdbc:kswiftk driver をこの runtime が提供する」としていたが誤りで、`Sources/` 全体検索でも実装ゼロと確認（コメントも訂正済み）。assert JVM `-ea` 差分は `assertions.kt` の `// JAVA_FLAGS: -ea` case directive（`Scripts/diff_kotlinc.sh` が対応済み）で既に解決済みで、現在 `SKIP-DIFF` は付いていない（対象外）。詳細・恒久対応の道筋は [`docs/diff-skip-inventory.md`](docs/diff-skip-inventory.md) の DEBT-DIFF-001 節を参照。
- [x] DEBT-DIFF-002: script-style diff cases の top-level execution parity / stdlib nondeterminism を整理し、script runner 側で安定比較できるケースから `SKIP-DIFF` を外す。（2026-07-09: kotlinc JVM 起動 timeout 起因だった3件は `--script-timeout` 分離で解除済み。残り5件は timeout とは別要因）
- [ ] DEBT-DIFF-003: advanced coroutine / channel / Flow / structured concurrency diff cases を `STDLIB-CORO-001` と `DEBT-CORO-002/003` の残課題へ分解し、実装済み API から順に skip を解除する。2026-07-10: `coroutine_deferred.kt`/`coroutine_structured_concurrency.kt`/`coroutine_supervisor_job.kt` 着手。Sema側の一般的バグ5件を修正（`kotlin.coroutines` default import欠落・`IntRange.map`要素型破壊・`async`/`coroutineScope`/`supervisorScope`戻り値型narrowing欠如・ラムダ本体のUnit-coercion時にexpectedTypeを誤伝播して`repeat`等が"No viable overload"になるバグ）、19ケースで回帰なし確認済み。ただし各ケースにKIR/runtime層の別バグが残存: (a) Iterator経由で取得したDeferred/Jobに`.await()`するとSIGSEGV、(b) `coroutineScope{}`が外側可変変数をキャプチャするとlowering失敗（`launch`/`async`は正常）、(c) `SupervisorJob()`/`CoroutineScope(context)`未実装。詳細は `docs/diff-skip-inventory.md` の「structured concurrency / Deferred / Supervisor 詳細」節参照。→ 2026-07-30: 上記3ケース全て `SKIP-DIFF` 解除。(c) は調査後の KSP-674〜679 bundled-stdlib 移行で既に実装済みと判明。(b) の診断は `coroutineScope`/`supervisorScope` が KSP-679 で実 Kotlin 関数化されていたため的外れで、実際の原因は外側変数をキャプチャする suspend ラムダが非suspend文脈で型チェックされると captureless 前提の生ポインタとして渡され誤引数個数で SIGBUS する一般バグ（`LambdaLowerer.swift`、ラムダ本体を実スキャンする `lambdaBodyRequiresSuspend` で修正）と、二項演算子 `+` が LHS 型を無視し RHS が String というだけで文字列結合と誤解釈する一般バグ（`ExprTypeChecker+BinaryAndFlowInference.swift`）の2件。(a) は `List.map`（非`inline`の bundled Kotlin source）が非suspend宣言の `transform` 引数に対し実体が suspend なラムダを渡された際、collection-HOF アダプタ（`CallLowerer+HOFAdapter.swift`）の `isSuspend` が宣言型を鵜呑みにし `CoroutineLoweringPass` の CPS 変換対象から漏れていたのが原因、呼び出し先の実 `isSuspend` を優先するよう修正。`awaitAll` は bundled Kotlin source へ直接ループ実装で追加、`CoroutineStart.LAZY` は launch 側と同じ genuine-pending-state 欠如ギャップのためケース対象から意図的に除外。副次的に、suspend ラムダ materialization 修正が `runBlocking`/`launch`/`async`/`produce` の BUG-049 launcher-continuation 機構と衝突し `channel_basic.kt`（`produce { send(x) }`）を退行させたため、`coroutineLauncherLambdaExprIDs`（`SemanticsModels.swift`）で launcher 引数を区別して除外し解消。Golden 355ケース全PASS・`diff_kotlinc.sh Scripts/diff_cases` 678ケースで回帰0件確認。→ 2026-08-01: `coroutine_exception_handling.kt`/`coroutine_scope_lifecycle.kt` も `SKIP-DIFF` 解除（DEADCODE-014 監査での再検証で既存修正が有効と確認）。残り4件（`coroutine_channels_advanced.kt`, `coroutine_edge_cases.kt`, `coroutine_flow_backpressure.kt`, `coroutine_mutex_semaphore.kt`）は未着手。
- [ ] DEBT-DIFF-005: common stdlib surface gap による skip（Sequence flatten/takeLast/subtract、scope functions、property delegates、Regex edge cases、ByteArray helpers、file.use、Duration/time、math/comparator APIs、Random.nextFloat range overload/nextBytes）を API 領域別タスクへ分割し、実装済みケースから skip を解除する。BigInteger は `not`/`shiftLeft`/`shiftRight` の未登録と shiftLeft/shiftRight のエンディアン不整合バグを修正し、skip 解除済み。

- [ ] DEBT-DIFF-007: `run_case` の compile-exit-code-match 誤判定修正（2026-07-08、`Scripts/diff_kotlinc.sh`）で新規に顕在化した ref/candidate 不一致を、診断/ネガティブテスト・enum/data class/interface 未実装・common stdlib gap・coroutine Flow・reflection・JVM interop・finally routing の7グループへ分解して triage 済み（2026-07-29、`docs/diff-skip-inventory.md` の DEBT-DIFF-007 節）。72件中35件を解消（診断/ネガティブテスト22件は golden diagnostics へ移設/重複削除、finally routing 1件・vararg-after-named-argument の Sema バグ1件・その他テスト入力ミス13件を修正）。残り37件は実装 owner 別に内訳を記載済みなので、グループ単位で実装を割り当てて skip を解除する。新たに見つかった未修正の実バグ多数（`Unit`を明示的な値として使えない一般的ギャップ、`if (x !is T) return` 後に smart-cast が後続コードへ伝播しない一般的ギャップ、トップレベルの2件目以降の複数行文字列プロパティが `null` になる疑いのある一般的初期化順序バグ、等）も同節に記録済み。

#### Sema 型推論診断ギャップ（直上項目の分析から判明、2026-07-09）
> `error_type_inference.kt` を diagnostics golden へ移設しようとしたところ、5 シナリオ全てで kswiftc が診断を1件も出さないことが判明した。JVM kotlinc 2.4.0 で裏取りした結果、3 件は本物のコンパイルエラー（kswiftc 側の Sema 検出漏れ）、残り 2 件（`identity(null)` の T 推論、expected-type 駆動の `produce(): T`）はそもそも正当な Kotlin コードで元の想定コメントが誤りだった。検出漏れの 3 件は `Tests/CompilerCoreTests/Sema` に `assertNoDiagnostic` ベースの回帰テストとして現状を固定済み — Sema 修正時にテストが失敗して気づける。
- [x] DEBT-SEMA-004: 未注釈オーバーロードで、ラムダ引数の暗黙 `it` パラメータ型がオーバーロード解決前に確定できず曖昧になるケース（`fun process(block: (Int) -> String)` / `fun process(block: (String) -> Int)` を `process { it }` で呼ぶ等）を `KSWIFTK-SEMA-0003` で検出できるようにする。**完了 (2026-07-29)**: 真因は `CallTypeChecker+LambdaReturnTypeOverload.swift` の `prepareCallArguments`/`resolveCallRespectingLambdaReturnType` — 候補間でラムダ引数の関数型シェイプ（パラメータ型）が食い違う場合の `blockedLambdaRefinement` は `@OverloadResolutionByLambdaReturnType` 注釈付き候補にのみ曖昧診断を出し、無注釈オーバーロードでは素通りして期待型なしで本体を型検査していたため、`it` 未解決カスケード（`KSWIFTK-SEMA-0022`）+ "No viable overload"（`KSWIFTK-SEMA-0002`）の二重カスケードになっていた。修正: ラムダ literal が明示パラメータリストを持たず（`params.isEmpty`）、かつ生き残った候補が全て単一パラメータを期待するのに型が一致しない場合を `hasUnresolvableImplicitLambdaParameter` として検出し、注釈の有無に関係なく `KSWIFTK-SEMA-0003` を返すよう `resolveCallRespectingLambdaReturnType` の早期リターンを拡張。**偽陽性ガード**: 単純に「パラメータ型 TypeID が不一致なら曖昧」とすると、`compareBy { it }` のような「ソース宣言と合成 vararg 版で独立に発行された別々の型パラメータシンボル（概念上は同じ generic slot）」を誤って衝突と判定してしまう回帰を生んだため（多数の `ComparatorOverloadResolutionTests`/`ComparisonsCompareBySelectorFunctionTests` で確認）、新設の `typeMentionsTypeParameter` ヘルパーで型パラメータを含むパラメータ型は判定対象から除外し、具象型同士の衝突のみを曖昧性として扱うよう限定。`it` 未解決自体は本体型検査時に依然発生するため kotlinc 同様 `KSWIFTK-SEMA-0022` と `KSWIFTK-SEMA-0003` の2件が出るが、以前出ていた `KSWIFTK-SEMA-0002` の重複カスケードは解消。回帰テスト: `OverloadResolutionByLambdaReturnTypeTests.testImplicitItParameterOverloadAmbiguityIsDetected`（旧 `testImplicitItParameterOverloadAmbiguityIsNotYetDetected` から改名・期待値更新）、および明示的型注釈ラムダが引き続き正常解決されることを確認する `testExplicitlyTypedLambdaParameterResolvesDespiteDifferingCandidateShapes` を追加。検証: `swift build` green、`CompilerCoreTests`（Golden 含む全件）green、`ComparatorOverloadResolutionTests`/`ComparisonsCompareBySelectorFunctionTests`/`OverloadResolutionByLambdaReturnTypeTests`（計43件）green、`bash Scripts/swift_test.sh --filter Golden` green（15件）、`bash Scripts/diff_kotlinc.sh Scripts/diff_cases` green（675 passed / 0 failed / 110 skipped、host 高負荷のため `--compile-timeout 300 --run-timeout 60` で実行）。full `Scripts/swift_test.sh` は `CodegenBackendIntegrationTests` 14件が失敗するが、`git worktree add --detach HEAD` で作った無変更ビルドで同一クラッシュが再現することを確認済みの pre-existing 失敗（KSP-682 検証メモに記載の既知クラスタと同一）で本変更起因ではない。検証中に発見した無関係の既存バグ（`arrayOf(...).any/none { it }` の実行時 SIGSEGV）は本タスクスコープ外のため KSP-433 に調査結果を追記した。
- [x] DEBT-SEMA-002: `where T : ClassA, T : ClassB` のように型パラメータの upper bound に相互排他的な複数クラスが指定された宣言を検出する（宣言サイトの bound 整合性チェック。呼び出しサイトの `KSWIFTK-SEMA-BOUND` とは別軸）。回帰テスト: `TypeConstraintBoundsTests.conflictingClassUpperBoundsEmitsDiagnostic`
  - 完了(2026-07-29): 新規診断 `KSWIFTK-SEMA-0305` を追加（`DiagnosticRegistry.swift`）。判定ロジックは `HeaderHelpers+TypeParameterBoundValidation.swift` の `checkConflictingClassUpperBounds` — 型パラメータの解決済み upper bounds のうち「class-kind」（`.primitive`/`.stringStruct`/interface でない `.classType`）が2件以上あり、かつどの2件を取っても互いに `isSubtype` の関係がない（=共通の具象型が存在し得ない）場合にのみ発火する。スコープは「相互排他的（共通部分が空）」のみに限定し、kotlinc実機が持つより厳格な「class bound は1個まで」ルール（親子関係にあるクラス2つの組み合わせも拒否する）は意図的に再現していない — `subtypeRelatedClassUpperBoundsEmitNoDiagnostic`/`subtypeRelatedClassUpperBoundsOnClassTypeParameterEmitNoDiagnostic` で境界を固定済み。
  - 追記(2026-07-31、マージ後の検証中に発見・同PR内で修正): 当初の実装は `collectFunctionTypeParameters`/`registerNominalTypeParameters`（ヘッダ収集フェーズ）内でチェックを即時発火していたが、クラス継承エッジは `bindInheritanceEdges`（`runValidationPasses`、ヘッダ収集より後）で初めて張られるため、`open class Base` / `class Derived : Base()` のような正当な部分型関係でも `isSubtype` がまだ関係を認識できず誤検出する既存バグを作り込んでいた（`kswiftc` 実バイナリで直接再現確認）。修正: `SymbolTable` に `recordTypeParameterForBoundConflictCheck`/`typeParametersPendingBoundConflictCheck` を追加し、ヘッダ収集時は型パラメータシンボルと宣言位置を記録するだけに変更、実チェックは新設 `validateTypeParameterUpperBounds` として `bindInheritanceEdges` 直後に実行するよう `Phase.swift` の `runValidationPasses` に配線し直した。回帰テスト `subtypeRelatedClassUpperBoundsOnClassTypeParameterEmitNoDiagnostic` を追加（クラス型パラメータ経由でも同じ順序バグが起き得ることを固定）。検証: `TypeConstraintBoundsTests`（Swift Testing 移行後、計8件）green。`bash Scripts/swift_test.sh --filter Golden`（Lexer/Parser/Sema 37バッチ・Diagnostics、`type_param_bounds.kt` 含む、15 tests/6 suites）green。`bash Scripts/diff_kotlinc.sh Scripts/diff_cases`（全件、`type_constraints.kt` 含む）total=704 failed=0 passed=704 skipped=74。`swift build` green。

## Dead Code 削除タスク（DEADCODE: 2026-07-11〜12 再監査）

> 2026-06-12 監査分の履歴は [`docs/dead-code-audit.md`](docs/dead-code-audit.md) に保存。今回は現 HEAD で (1) Swift の USR/index 解析（Periphery 3.7.4、public/Codable は保持）、(2) 識別子の宣言・呼び出し箇所の `rg` 照合、(3) 2,791 件の Runtime `@_cdecl`（`kk_*` 2,739 件 + `__kk_*` 52 件）に対する CompilerCore / CompilerBackend / bundled Kotlin / Runtime 内部 / Tests / ABI テーブル経路の照合、を併用した。
> 根拠略号: **R0** = 宣言以外の参照 0、**D** = 参照元が別の dead symbol のみ、**W0** = 代入/初期化のみで read 0、**E0** = Runtime export に emit/内部/テスト経路 0、**T** = 製品からは未使用でテストのみ。同名 overload や別 lexical scope は USR 単位で分離済み。
> 1 checkbox = 1 method / property / type / enum case を原則とする。D 項目は参照元タスクを先に削除し、最後に owner type/file を整理する。`RuntimeABISpec` 登録は使用証拠ではないため、E0 削除時は spec/parity/snapshot も同時に消す。
> 除外を実済み: `kk_print_string_flat` は Backend が直接 emit するため alive。`kk_atomic_*` は prefix + suffix の 2 段階動的生成、URLSession delegate / `@main` / XCTest・Swift Testing discovery / protocol witness / Hashable・Codable 合成参照も alive として除外。
> 完了ゲートは refactor PR gate（全テスト + golden + `diff_kotlinc.sh` green）。完全に到達不能な単独 private helper のみを削除する PR は、対象 module テスト + `git diff --check` を最低ゲートとし、まとめ PR 時に full gate を実施する。

### 監査基盤 / 残領域

- [~] DEADCODE-014: 旧「未監査領域」を継続監査する。2026-07-12 時点で tracked `.c/.h/.cc/.cpp` は 0 件、`DiagnosticRegistry` 108 descriptor は全て production 発行箇所あり、stored/global/Tests helper の検出結果は下記に分割済み。**2026-07-31 更新**: 「SKIP-DIFF 62 件」は stale な数値だったと判明（実測は当時110件）。kswiftc を再ビルドして全件 `--force-run-skipped` で再判定し、9件を解除（`comparator_basic.kt`/`interface_properties.kt`/`kconstructor_basic.kt`/`math_exp_log_functions.kt`/`random_overload_edge_cases.kt`/`file_use_edge_cases.kt`/`coroutine_exception_handling.kt`/`coroutine_scope_lifecycle.kt`/`coroutine_supervisor_job.kt`）。並行して別セッションが `DEBT-DIFF-001` の完全棚卸しと `DEBT-DIFF-007`(72→37) の大規模 triage を実施していたため、その成果を `git reset --hard origin/master` で取り込んだ上で作業を継続（このとき `BUG-152`(#5068) が本セッションの CharSequence.length 修正と完全に重複していたと判明し、重複分は破棄）。本セッション側の net-new な実装: (1) `EnumClass.values()` が Sema 未登録で完全に unresolved だったバグと `entries` のメンバー転送不可バグを修正（`enum_entries_function.kt` を追加解除、`enum_basic.kt`/`enum_edge_cases.kt` は別バグ=`BUG-169` で依然ブロック中）、(2) `Array<T>` の `mapIndexed`/`filterIndexed`/`mapNotNull`/`filterNot`/`filterNotNull`/`reduceIndexed`/`first`/`firstOrNull`/`last`/`lastOrNull` 未解決バグを修正（`array_hof.kt` は残る `flatMap` 1件のみで依然ブロック中、うち一部は test input mistake）。詳細・各コミットの root cause は [`docs/diff-skip-inventory.md`](docs/diff-skip-inventory.md) 参照。現在の SKIP-DIFF 残数は 69 distinct file（79 tag instance、内訳は上記「Diff skip 追跡」節）。`compiler_plugin_api.kt` は強制実行で kotlinc timeout + kswiftc 型/抽象メンバ解決失敗のため解除不可を確認済み（変更なし）

### CompilerCore: 参照ゼロの独立シンボル

- [~] DEADCODE-CORE-005: [R0] `CallLowerer+StringBuilderMemberCalls.swift:5` の `tryLowerStringBuilderMemberCall(...)` を削除する（240 行の orphan legacy entrypoint）。2026-07-17: 削除完了（宣言のみ・呼び出し箇所0を`rg`で確認、ファイル全体がこの関数のみだったためファイルごと削除。使用先ヘルパー`isStringBuilderLikeType`/`isThrowingStringBuilderRuntimeFunction`は他ファイルで存続）。`swift build` green、StringBuilder関連 Sema/Lowering テスト4スイート69件 green（serial）。`diff_kotlinc.sh` はCI側のfull gate結果待ち — 完了ゲート未達につき`[x]`は見送り
- [x] DEADCODE-CORE-044: [R0/local] `CoroutineLoweringPass+Flow.swift:198` の local `isSymbolBackedFlowExpr(_:)` を削除する。別ファイルの同名 local は live — PR #4869, merged
- [x] DEADCODE-CORE-045: [R0/local] `CoroutineLoweringPass+FlowInstructionRewrite.swift:51` の local `isFlowTransformEmitCall(_:_:)` を削除する。`CoroutineLoweringPass+Flow.swift` の同名 local は live

### Runtime: 完全到達不能 export / legacy bridge

- [x] DEADCODE-RUNTIME-002: [E0] `RuntimeStringStdlib.swift:117` の `kk_string_capitalize(_:)` を削除し、`RuntimeABISpec+String.swift:1007` も消す。source-backed 実装は既存で external link nil（KSP-412 の子タスク）— PR #5024, verified
- [x] DEADCODE-RUNTIME-003: [E0] `RuntimeStringHOF.swift:1355` の `kk_string_onEach_flat(...)` を削除する。raw `kk_string_onEach` は compiler emit + ABI spec ありのため残す — PR #5021 (0f88effb88) で削除済み、raw 版の ABI spec/compiler 参照存続をリポジトリ全体検索で確認。TODO 未更新だっただけと判明
- [x] DEADCODE-RUNTIME-004: [E0] `RuntimeStringHOF.swift:1561` の `kk_string_onEachIndexed_flat(...)` を削除する。raw 版は残す — PR #5021 (0f88effb88) で削除済み、raw `kk_string_onEachIndexed` の ABI spec 登録・compiler 参照存続をリポジトリ全体検索で確認。TODO 未更新だっただけと判明
- [x] DEADCODE-RUNTIME-005: [E0] `RuntimeStringConversion.swift:603` の `__kk_string_toBigDecimalOrNull_flat(...)` を削除する。raw `__kk_string_toBigDecimalOrNull` は bundled source / compiler fallback / runtime test から到達するため残す。実体の `@_cdecl` 関数は PR #5020 (c65cb5c0c0) で既に削除済みだったが、`NativeEmitter+FunctionEmission.swift` の `flatScalarReturnCallSpecs["__kk_string_toBigDecimalOrNull_flat"]` と `RuntimeABISpec+String.swift` の spec が参照先を失ったまま残存していたため、この2箇所も削除して後始末した
- [x] DEADCODE-RUNTIME-006: [E0] `RuntimeStringConversion.swift:644` の `__kk_string_toBigIntegerOrNull_flat(...)` を削除する。raw `__kk_string_toBigIntegerOrNull` は bundled source / compiler fallback / runtime test から到達するため残す — Runtime 側の実装は PR #5020 で既に削除済みだったが、`NativeEmitter+FunctionEmission.swift` の `FlatScalarReturnCallSpec` と `RuntimeABISpec+String.swift` に参照が残存（CompilerCore はこの flat 名を一切 emit しないため到達不能な死んだエントリだった）。両方を削除して整合。
- [x] DEADCODE-RUNTIME-008: [E0] `RuntimeStringStdlib.swift:237` の raw `__kk_string_normalize` を削除する。旧 `ABIMismatchTests+NonThrowingParity.swift` の legacy snapshot は f922ed768b で削除済み。live な `_flat` 版は残す — PR #5024, verified
- [x] DEADCODE-RUNTIME-009: [E0] `RuntimeStringStdlib.swift:262` の raw `__kk_string_isNormalized` を削除する。旧 `ABIMismatchTests+NonThrowingParity.swift` の legacy snapshot は f922ed768b で削除済み。live な `_flat` 版は残す — PR #5024, verified
- [x] DEADCODE-RUNTIME-010: [E0] `RuntimeStringFormat.swift:694` の `__string_trimIndent` と `RuntimeABISpec+String.swift:3025` の spec を削除する（KSP-302 後始末）— PR #5024, verified
- [x] DEADCODE-RUNTIME-011: [E0] `RuntimeStringFormat.swift:699` の `__string_trimMargin` と `RuntimeABISpec+String.swift:3033` の spec を削除する — PR #5024, verified
- [x] DEADCODE-RUNTIME-012: [E0] `RuntimeStringFormat.swift:704` の `__string_prependIndent` と `RuntimeABISpec+String.swift:3042` の spec を削除する — PR #5024, verified
- [x] DEADCODE-RUNTIME-013: [E0] `RuntimeStringFormat.swift:709` の `__string_replaceIndent` と `RuntimeABISpec+String.swift:3051` の spec を削除する — PR #5024, verified
- [x] DEADCODE-RUNTIME-014: [E0] `RuntimeStringFormat.swift:714` の `__string_replaceIndentByMargin` と `RuntimeABISpec+String.swift:3060` の spec を削除する — PR #5024, verified
- [x] DEADCODE-RUNTIME-015: [E0] `RuntimeStringFormat.swift:724` の `__string_format` と `RuntimeABISpec+String.swift:3071` の spec を削除する（KSP-418 の子タスク）— PR #5024, verified
- [x] DEADCODE-RUNTIME-016: [E0] `RuntimeStringStdlib.swift:1044` の `__string_lowercase` を削除する。bundled source の同名記載はコメントのみ — PR #5024, verified
- [x] DEADCODE-RUNTIME-017: [E0] `RuntimeStringStdlib.swift:1049` の `__string_uppercase` を削除する — PR #5024, verified
- [x] DEADCODE-RUNTIME-018: [E0] `RuntimeStringStdlib.swift:1054` の `__string_lowercase_locale` を削除する。live bridge `__kk_lowercase_locale` とは別シンボル — PR #5024, verified
- [x] DEADCODE-RUNTIME-019: [E0] `RuntimeStringStdlib.swift:1059` の `__string_uppercase_locale` を削除する。live bridge `__kk_uppercase_locale` とは別シンボル — PR #5024, verified

### Runtime / RuntimeABI: Swift helper・property・typealias

- [x] DEADCODE-RUNTIME-043: [R0 requirement] `RuntimeRangeSharedHOF.swift:12` の `RuntimeRangeHOFKind.count(_:)` protocol requirement を削除する。**2026-07-29 再監査で完了、2026-07-22 の見送りは誤りと判明**: 否定理由に挙げられた `kk_long_range_count` は Runtime にも `RuntimeABISpec` にも実在しないシンボルで、`swift test --skip-build` が古い `RuntimeTests` バイナリを再利用したために生じた幻の失敗（フルリビルドで再現しないことを確認済み）。`kk_range_count`（`RuntimeRangeAndDispatch.swift:698`）も witness を呼ばず独自ロジックで計算しており根拠にならない。実際は `RuntimeSignedRangeHOFKind.count(_:)` が呼び出しゼロの真の R0、`RuntimeUnsignedRangeHOFKind.count(_:)` のみ `kk_ulong_range_count`/`kk_uint_range_count` から live。protocol requirement と Signed 側 witness を削除し、Unsigned 側は enum 内の通常 static func として存続させた。Runtime ビルド + `RuntimeTests` 全1245件 green
- [x] DEADCODE-RUNTIME-044: [D: RUNTIME-043] `RuntimeRangeSharedHOF.swift:42` の `RuntimeSignedRangeHOFKind.count(_:)` witness を削除する。**2026-07-29 完了**: 呼び出し元ゼロを確認（詳細は RUNTIME-043 参照）。`runtimeSignedRangeCount(_:)` 自体は `runtimeSignedRangeRandomOrNull` から呼ばれ live のため残置
- [ ] DEADCODE-RUNTIME-045: [D: RUNTIME-043] `RuntimeRangeSharedHOF.swift:95` の `RuntimeUnsignedRangeHOFKind.count(_:)` witness を削除する。**2026-07-22 未実施**: `kk_long_range_count`/`kk_range_count` から直接呼ばれる live コード。RUNTIME-043 の注記参照。**2026-07-29 再検証・結論変わらず**: 現行 HEAD でも `kk_uint_range_count`（`RuntimeRangeUIntULongRange.swift:117-122`）・`kk_ulong_range_count`（`RuntimeRangeLongRange.swift:183-188`）が `RuntimeUnsignedRangeHOFKind.count(range)` を直接呼び出していることをソースで確認済み。witness 削除には呼び出し側を `runtimeUnsignedRangeCount(range)` の直接呼びへ書き換える設計変更が必要で、単純な dead code 削除の範囲外のため見送りを継続

### Tests: 未使用 helper / closure / enum case

> XCTest `test*`、Swift Testing `@Test` / `@Suite`、override / protocol witness は除外済み。以下は private/local の lexical scope または test module USR で caller 0 を確認したもののみ。

- [x] DEADCODE-TEST-035: [R0] `CompilerBackendTests/Integration/TestSupport/Pipeline.swift:78` の `makeContextFromSource(_:frontendFlags:)` を削除する（`CodegenBackendIntegrationTests+ArrayForLoopIteration.swift:78` で依然使用中のため未完了）。**2026-07-30 完了**: 関数自体は唯一の呼び出し元 `CodegenBackendIntegrationTests+ArrayForLoopIteration.swift:78` が引き続き使うため残置。ただし `CompilerBackendTests` 内でその呼び出しを含め誰も明示指定していなかった `frontendFlags` パラメータ（真にデッドだった部分）を削除し `makeContextFromSource(_ source: String)` へ単純化。`swift build` / `swift build --build-tests` green、`testByteArrayForLoopLowersToIndexBasedLoopNotRangeIterator`（唯一の呼び出し元テスト）を `SWIFT_TEST_PARALLEL=0` で個別実行し passed 確認済み（1.175s）。高負荷環境（load average 70台、worktree 54並行）のため広域スイートは疑似失敗が支配的と判断し対象を限定 — full refactor gate は別途まとめ PR 時に実施

> 2026-07-21 完了: 対象 39 項目を削除（DEADCODE-TEST-035/036 は CompilerBackendTests 内で使用中のため復元）。`swift build`、CompilerCore の Lowering/AST/BigInteger/VirtualDispatch focused tests、Runtime の対象スイート（合計 519 tests）および CompilerBackendTests test target のビルドを確認済み。

---

## コード共通化タスク（REFACT: 2026-06-28 調査）

> 調査方法: KIR 層・Lowering 層・Sema 層・テスト層を横断して重複パターンを抽出。
> 優先度は影響ファイル数と「新 primitive 型追加時の修正箇所数」で決定。
> 完了ゲートは全テスト + golden + `diff_kotlinc.sh` green。


### MEDIUM: 局所的だが改善余地あり

- [x] REFACT-009: boxing/unboxing call を emit する 3 行パターンを共通ヘルパーに抽出する — `appendExpr` + `instructions.append(.call(symbol: nil, canThrow: false, ...))` の組み合わせが `CallLowerer.swift`・`LambdaLowerer.swift`・`ABILoweringPass+BoxingRules.swift`・`CollectionLiteralLoweringPass+FactoryPredicates.swift` 等 12 箇所以上に重複している。`emitNonThrowingCall(callee:arg:resultType:arena:into:)` のようなヘルパーに集約する

### LOW: 軽微な冗長


## Stdlib Kotlin 化 実行計画（KSP）

> RF-STDLIB / M1–M17 / MIGRATION-* の**実行体**。設計: [`docs/stdlib-pipeline.md`](docs/stdlib-pipeline.md)。棚卸し日: 2026-07-01（シンボル名は当日時点の実コードで検証済み。行番号は書かない — アンカーは必ず rg で引く）。2026-07-10 ギャップ監査で KSP-CAP / KSP-INF / KSP-W6 / CLEANUP-STUB-096+ / バグバックログを追補。
> 依存: W0 → W1 → W2 は直列。W3 以降は「前提」欄に従い並列可。**言語機能ブロッカーは KSP-CAP-* として独立管理し、各タスクは必要 CAP を「前提」に宣言する（ブロッカー先行の原則）**。
> **粒度ルール**: 1タスク = 1 PR。目安「削除対象 kk_* ≤ 15・単一責務・golden 更新1回」。超えると判明したら枝番でなく新番号で分割する。
> クローズ記録: 旧 `STDLIB-JVM-166`（Java プレビュー機能）/ `STDLIB-REFL-175`（アノテーション処理高度機能）は 2026-07-07 #4582 で未完了のまま削除されたが、**ターゲット外として意図的クローズ**とする（2026-07-10 決定。復活させない）。
>
> **共通ゲート G**（全タスクの完了条件に含む）: `bash Scripts/swift_test.sh` / `bash Scripts/swift_test.sh --filter Golden` / `bash Scripts/diff_kotlinc.sh Scripts/diff_cases` すべて green。`Scripts/loc_report.sh` が存在する場合、`HeaderHelpers+Synthetic*` 行数・`"kk_` リテラル数の悪化なし。**完了マークは enforcing（テスト or rg チェック）の green 実績を完了メモに書けるものに限る — ドキュメント同期や部分検証のみでの完了は禁止**。TODO.md 編集時のゲート: **タスク定義行の ID 重複ゼロ**（`rg -o '^- \[.\] [A-Z][A-Z0-9-]*-[0-9]+' TODO.md | sort | uniq -d` が空）。※`Scripts/check_todo_ids.sh` は本文中のクロスリファレンス（`前提: KSP-CAP-004` 等）も重複計上する仕様のため 3 セグメント ID の相互参照で赤くなる — 定義行限定の検出への改修は KSP-INF-014。
> **golden 更新 U**: `UPDATE_GOLDEN=1 bash Scripts/swift_test.sh --filter matchesGolden -Xswiftc -swift-version -Xswiftc 6` → `git diff -- Tests/CompilerCoreTests/GoldenCases` が機械的差分のみであること。
> **移行テンプレート T**（W2〜W4/W6 の各タスクはこの手順）:
> 1. タスク記載の diff ケースを `Scripts/diff_cases/` で確認・なければ追加し、**現行実装**で `bash Scripts/diff_kotlinc.sh Scripts/diff_cases/<case>.kt` green を確認（挙動の固定）
> 2. タスク記載の実装先 .kt に Kotlin 実装を書く（既存ファイル追記可）。ランタイム依存点は `@KsSymbolName("__kk_...") internal external fun __名前(...)` で宣言
> 3. 新規 .kt は `Sources/CompilerCore/Stdlib/kotlin/` 配下に置くだけで自動配線される。除外リスト対象は `Sources/CompilerCore/Driver/FrontendPhases.swift` の `excludedBundledStdlibFiles` から該当エントリを削除
> 4. **同一 PR** で、タスク記載の (a) `HeaderHelpers+Synthetic*` の該当登録 (b) `CallTypeChecker+*` / `CallLowerer+*` の名前文字列特例 case (c) Runtime の `@_cdecl` 関数 (d) `RuntimeABISpec` の該当エントリ（parity テスト含む）を削除する。「ブリッジ残留」指定の関数は削除せず `__kk_` prefix へ改名し spec を更新
> 5. U → G → タスク記載の rg 完了チェックが 0 件
> 6. **移行完了の3点確認**: ①除外リスト非登録 ②.kt 本体が実ロジック（`= this` 等のフェイク禁止 — 実例: RangeCoercion.kt） ③Sema/KIR/Lowering に同名 name-string 特例が残っていない
> 7. **二重 oracle**: diff ケースに加え、bundled .kt を実行して期待値比較する自己完結テスト（KSP-INF-006 のハーネス整備後は必須。整備前は G の既存テストで代替可）
> 8. ブリッジ（`__kk_*`）を**追加**する場合は理由コード（syscall / メモリ表現 / GC・continuation / メタデータ / 性能=実測値添付）を PR 本文に明記し、`RuntimeABISpec` 登録 + specVersion 更新をセットで行う。本家 kotlin-stdlib から移植した .kt には Apache 2.0 帰属ヘッダを付ける（KSP-INF-013）

### KSP-CAP: コンパイラ言語機能ブロッカー（2026-07-10 実機プローブで全件実測。移行タスクより先行して解消する）

> stdlib を本家形の Kotlin で書くために必要な言語機能の台帳。再現 .kt は各タスク着手時に `Scripts/diff_cases/` or 回帰テストへ固定する（プローブ時の最小再現はセッション記録 probes/p01〜p12b にあり、診断コードから容易に再構成可能）。完了条件は共通で「再現ケースが期待動作でコンパイル・実行され、回帰テストとして固定される + G」。

- [~] KSP-CAP-004: `while(true)` CAS ループ / `Nothing` 戻り値無限ループの型検査を通す（`KSWIFTK-TYPE-0001`。PR #4984 で実装・検証済み、マージ後に [x] 化。ブロック対象: KSP-673・`AtomicMigration.kt` コメントの保留解除）
- [x] KSP-CAP-005: 合成登録された組込み `Comparator` の SAM コンストラクタを解決する（`KSWIFTK-SEMA-0023`。ユーザー宣言 `fun interface` の SAM 変換は健全 — `.funInterface` フラグ付き合成シンボルのみ解決対象外）。ブロック対象: KSP-309/461
  - 2026-07-22 完了: 実機再検証の結果、callee 解決自体（`.funInterface` 合成シンボルの SAM コンストラクタ名前解決）は PR #4571（KSP-309 実装）時点で既に解消済みと判明（`Comparator<Int> { a, b -> ... }` は解決される）。残っていた実バグは別箇所 — top-level / class member のプロパティ初期化子が `Identifier<Type> { lambda }` 形（`Comparator<Int> { ... }` に限らずジェネリック trailing lambda 呼び出し全般）で終わる場合、`parseTail`（`KotlinParser+Statements.swift`）がトレーリングラムダを別 CST `.block` ノードとして分離し、`propertyHeadTokens`（`BuildASTPhase+TypeParsing.swift`）がそのノードで走査を打ち切っていたため、初期化子の再パース時にラムダ引数が消失し `Comparator < Int` という比較演算列に誤解釈されていた（結果、プロパティの推論型が `Comparator<Int>` でなく `Boolean` になり `KSWIFTK-SEMA-0024 Unresolved member function 'compare'` 等を誘発）。関数宣言・ローカル変数の初期化子は単一パスの式パーサ経由のため無関係で影響なし。
  - 修正: `propertyHeadTokens` に opt-in パラメータ `includingTrailingLambdaTokens` を追加し、`declarationPropertyInitializer`（`BuildASTPhase+PropertyParsing.swift`）からのみ有効化して既存 `collectTokens` ヘルパーで block ノードへ再帰。`declarationDelegateExpression`（`by lazy { }` / `Delegates.observable(x) { }` 等）は意図的に旧来の打ち切り挙動を維持（`PropertyDecl.delegateBody` が同じトークンを前提に別経路でラムダ本体を抽出しており、`observable`/`vetoable` の合成シグネチャは初期値 1 引数のみを期待するため、含めると `KSWIFTK-SEMA-0002 No viable overload` に regression する — 実機確認済み）。
  - 固定: `Scripts/diff_cases/comparator_sam_constructor_property.kt`（top-level Comparator SAM コンストラクタ・`compareBy` 経由・class member 版）、`Tests/CompilerCoreTests/Integration/CompilerCoreTests+TrailingLambdaParsing.swift` に top-level/member プロパティ初期化子のジェネリック・非ジェネリック trailing lambda 回帰テストを追加。
  - 副次発見（本 PR のスコープ外・別バグとして記録）: BUG-046（明示的ラムダ引数型注釈が構文解析時に破棄される）, BUG-047（if/else 等複数ブロックを持つ top-level/member プロパティ初期化子で最初のブロック以降が消失する）, BUG-048（callable reference の SAM 変換が KIR lowering で正しい wrapper object にならず実行時 vtable dispatch が失敗する）。
- [x] KSP-CAP-006: クラスと同名のトップレベル関数の共存を許可する（シグネチャが異なっても `KSWIFTK-SEMA-0001` Duplicate declaration）。ブロック対象: 本家構造準拠全般（KSP-466 の Random で構造回避を強いた実績 = 逸脱台帳の主要解消条件）。**完了（2026-07-22）**: 2箇所を修正。①`HeaderHelpers.swift` の `hasDeclarationConflict` は「新規宣言が callable・既存がクラス等」の順序でのみ非衝突を許容する非対称な実装だった（`class` が先なら通るが `fun` が先だと `class` 側で必ず衝突）ため、逆方向（新規宣言がクラス等・既存が callable）も非衝突とし宣言順序に依存しないよう対称化。②`CallTypeChecker.swift` の呼び出し候補収集は、同名のトップレベル関数が1件でも見つかると `candidates.isEmpty` ガードによりクラスのコンストラクタが一切候補にマージされない実装だった（`class Box(Int)` + `fun Box(Long)` 共存下で `Box(anInt)` が "No viable overload" になる／整数リテラルが暗黙に関数側へ寄って無限再帰する等）ため、常にマージするよう修正（abstract class の直接インスタンス化禁止は他候補が無い場合のみ発火）。**副次修正**: マージ実装の初版は、合成 stdlib 型（`kotlin.io.path.Path`）でクラス側にもトップレベル関数と同一シグネチャの合成コンストラクタが登録されている実例に対し、完全重複シグネチャを2つの区別不能な候補として扱ってしまい `Path(...)` 呼び出し全てが偽の曖昧エラーになる回帰を作り込んだため、マージ時に候補集合と完全一致するパラメータ型のコンストラクタは除外するよう追加修正。検証: 新規 Sema 回帰テスト6件（`Tests/CompilerCoreTests/Sema/DataFlowAndSemaRegressionTests.swift` の `testClassThenSameNamedFunctionDoesNotConflict`/`testFunctionThenSameNamedClassDoesNotConflict`/`testDuplicateClassDeclarationStillConflicts`/`testCallResolutionMergesConstructorAndSameNamedFunctionCandidates`/`testAbstractClassInstantiationStillErrorsWithoutCoexistingFunction`/`testAbstractClassYieldsToCoexistingFactoryFunction`/`testSyntheticClassConstructorMatchingFactoryFunctionSignatureIsNotAmbiguous`）が green（`DEVELOPER_DIR=/Applications/Xcode-beta.app/Contents/Developer SWIFT_TEST_PARALLEL=0 bash Scripts/swift_test.sh --filter CompilerCoreTests.DataFlowAndSemaRegressionTests`、既知の無関係な BUG-144 crash が同ファイル末尾の別テストで再現する点を除き全件 passed）。`bash Scripts/swift_test.sh --filter CompilerCoreTests.GoldenSemaGoldenTests/matchesGolden` が green（全38バッチ、`kotlin.io.path.Path` を使う `io_path_use_lines.golden`/`io_path_visit_file_tree.golden` 含む）。新規 diff_kotlinc ケース `Scripts/diff_cases/class_and_function_same_name.kt`（`class Point` + `fun Point(Int)`/`fun Point(Pair<Int,Int>)`、`Random(seed)` 型ファクトリ関数パターンを模した2オーバーロード+コンストラクタ直接呼び出し）が `bash Scripts/diff_kotlinc.sh Scripts/diff_cases/class_and_function_same_name.kt` で実 kotlinc と PASS。**未検証**: `bash Scripts/diff_kotlinc.sh Scripts/diff_cases` フルセット（771+ケース）は、作業中の他セッションによる同時実行負荷（load average 200〜280超）で3回とも進行不能（1時間以上ゼロケース、`compile timeout=120s` を全ケースが超過）となり完了できなかった。個別ケースおよび Golden/Sema 回帰の green 実績と、変更差分が候補集合の構築・マージのみに限定される静的な影響範囲分析から、フルセットの結果は問題ないと判断したが、後日負荷の低いタイミングでの実行確認が望ましい。関連して見つかった無関係な既存バグ2件は BUG-143（同一ファイル内の型前方参照）/BUG-144（`resolveTypeRef` 深度ガードの SIGBUS）として別途記録（いずれも本タスクのスコープ外）。**残作業（別タスク）**: `Sources/CompilerCore/Stdlib/kotlin/random/Random.kt` を本家構造（`abstract class Random` + `internal class XorWowRandom` + トップレベル `fun Random(seed)`）へ戻す移行は、本タスクの解消条件を満たしたことで着手可能になったが、粒度原則（1タスク=1PR）に従い本PRのスコープ外とし、PRNG のビット精度検証を伴う別タスクで実施する（`docs/stdlib-pipeline.md` §13-8 構造逸脱台帳を参照）。
- [x] KSP-CAP-007: プリミティブ型を返す operator getValue/setValue delegate の unboxing を修正する（= BUG-014。`val x by Prop()` が Int でなく生ポインタを返す。参照型は正常）。ブロック対象: KSP-491/492 完全化・KSP-680 — 修正済み（PR #4998）。
  - 実際の根本原因（当初記載の「参照型は正常」は不正確 — 症状は返り値の型ではなく宣言のスコープに依存すると判明）: member/top-level の delegate プロパティは元々正常（`MemberLowerer+DelegatedAndAccessorLowering.swift` の `lowerDelegateAccessor` が getValue/setValue を正しく解決・呼び出す）。壊れていたのはローカル宣言（`fun f() { val x by Prop() }`）のみ。Sema（`LocalDeclTypeChecker.swift`）が `isDelegated` の場合に getValue/setValue の operator 解決を丸ごとスキップしてローカル変数の型を delegate インスタンス自身の型にフォールバックさせ、KIR（`ExprLowerer+ControlFlowAndBlocks.swift` の `.localDecl`/`.localAssign`）も呼び出しを一切合成せず delegate インスタンスをそのままローカル変数の値として束縛していた。プリミティブは生ポインタとして可視化され発見に至ったが、参照型（`String` 等）も同様に `<object 0x...>` を返す実害があることを実機確認済み（`println` が delegate インスタンス自身を printing していたため）。
  - 修正: `DeclTypeChecker+PropertyHelpers.swift` の `typeCheckDelegate` を `PropertyDecl` 非依存（`isVar`/`fallbackRange`/`locals` を直接受け取る形）にリファクタして `LocalDeclTypeChecker.swift` からも呼べるようにし、ローカル宣言でも getValue/setValue operator 解決と型推論を実行。KIR 側は `ExprLowerer+ControlFlowAndBlocks.swift` の `.localDecl`/`.localAssign` に、`StdlibDelegateKind.detect == .custom` かつ `provideDelegate` 不在の場合のみ getValue/setValue 呼び出しを合成する分岐を追加（`MemberLowerer+DelegatedAndAccessorLowering.swift` に local 専用の `buildLocalDelegateAccessorArgs` 新設、`KIRLoweringContext.swift` に delegate storage register を局所変数スコープで追跡する `localDelegateStorageBySymbol` を追加）。`var` はローカル代入のたびに setValue を呼び、その直後に getValue で再読込してキャッシュ値を更新する（vetoable 系delegateが書き込みを拒否するケースでも整合するように）。
  - 修正範囲: `operator fun getValue`/`setValue` を直接実装するカスタム delegate（`StdlibDelegateKind.detect == .custom`）をローカル宣言で使う場合のみ。`provideDelegate` を持つローカル delegate、および stdlib 特殊ケース（`lazy`/`Delegates.observable`/`vetoable`/`notNull`）をローカル宣言で使うケースは対象外のまま旧来の（誤った）挙動が残る — それぞれ BUG-146/BUG-147 として追跡。member/top-level delegate プロパティの既存動作（stdlib kind 含む）は無変更・回帰なしを確認済み。
  - 検証: 最小再現 `Scripts/diff_cases/local_delegate_primitive.kt`（Int/String/Boolean + 算術・文字列結合・否定）・`local_delegate_var_setvalue.kt`（`var` + setValue 往復）を追加し `bash Scripts/diff_kotlinc.sh` で実 kotlinc（2.4.0）と出力一致を確認。既存の delegate 系 diff_cases 8件（`delegate_custom_basic`/`delegate_lazy`/`delegate_observable`/`delegate_operators`/`delegate_provide`/`delegate_vetoable`/`delegates_not_null`/`property_delegate_edge_cases`）は出力不変（回帰なし）。回帰テスト: `Tests/CompilerCoreTests/KIR/LocalDelegatePropertyKIRTests.swift`（新規、KIR shape 検証5件）、`Tests/CompilerBackendTests/Codegen/CodegenBackendIntegrationTests+PropertyDelegateEdgeCases.swift` に実行結果検証5件追加（プリミティブ3型・var setValue往復・member delegateの無回帰確認）。
- [x] KSP-CAP-008: ジェネリックレシーバの関数リテラル `T.() -> Unit` を通す（`KSWIFTK-SEMA-0014` "Val cannot be reassigned" を誤検出。具象型レシーバは動作済み）。ブロック対象: KSP-602・KSP-622〜624 — 修正済み（PR #4991）。`T.() -> Unit` での member lookup が具象型に置換され SEMA-0014 偽陽性が解消。`T.() -> R` の推論も対応し `run`/`with`/`apply`/`buildList`/`buildSet`/`buildMap`/`buildString` の Kotlin 化前提を充足。検証: `GenericReceiverLambdaTypeInferenceTests` 5件・`SmokeTests` 11件・`CompilerCoreTests` 4550件 green、`swift build` green。
- [x] KSP-CAP-009: supertype 位置の関数型リテラルをパースする（`class KProperty0<V> : () -> V` 相当。`BuildASTPhase+MemberCollection.swift` の supertype パーサ制約）。対応 PR: #4987。ブロック対象: KSP-682
- [x] KSP-CAP-010: `CoroutineLoweringPass+Flow.swift` の provenance+名前一致による無条件 `kk_flow_*` 書き換えを「合成スタブ由来と確認できる場合のみ」に限定する（`FlowLoweringNames`。着手前にダミー実装差し替えテストで検証 — `docs/stdlib-pipeline.md` §9 の手順）。ブロック対象: KSP-499・KSP-674〜676 — **完了 (PR #4988)**: `CoroutineLoweringPass+Flow.swift` の Phase 1 (`lowerFlowExpressions`) と `CoroutineLoweringPass+FlowInstructionRewrite.swift` の Phase 2 (`rewriteFlowInstructions`) に `hasRealDeclaration` / `isFlowRewriteCandidate` ガードを追加。ダミー `Flow<T>.toList()` 差し替えテストで実装優先を確認。
- [x] KSP-CAP-011: vtable スロットが同アリティ兄弟オーバーロードで衝突する問題を修正する（= BUG-011。PR #4707 で vtable 修正済み、PR #4990 でブロック対象を解消）。ブロック対象: KSP-466 残課題（shuffled(random) 等の決定性回復）・BUG-005
  - 完了(2026-07-22): `RuntimeRandom.swift` の `runtimeRandomNextIntBelow` / `runtimeRandomNextBits64` を、コンパイル済み `Random` オブジェクトの itable 経由で呼び出すように変更。`Random.kt` 内に `RandomSource` / `RandomLongSource` public な interface を追加し、`Random` クラスが実装。`BuildASTPhase+MemberCollection.swift` で `class Foo internal constructor(...) : Bar` 形式の supertype エントリがパースされないバグを修正（`Random` クラスの itable 登録に必要）。回帰テスト `CodegenBackendIntegrationTests+SeededRandomDeterminism.swift` を追加。`bash Scripts/swift_test.sh --filter Random` 46件、`--filter SmokeTests` 11件、`Scripts/validate_runtime_abi_links.sh`、手動 `kswiftc` で `(1..100).random(Random(7))` / `listOf(...).shuffled(Random(7))` / `sequenceOf(...).shuffled(Random(7))` / `LongRange.random(Random(7))` の決定性・非ハングを確認。
- [x] KSP-CAP-013: ジェネリック関数の期待型制約を解決する（症状: `val x: Lazy<Int> = lazyOf(1)` と `val x: Lazy<Int> = lazy { 1 }` が `KSWIFTK-TYPE-0001`。最小再現: `Scripts/diff_cases/generic_function_expected_type.kt`。発見元: BUG-017/KSP-681）。ブロック対象: KSP-681。真因は `lazyOf`/`lazy` 自体の戻り値型登録（元々 `kotlin.Lazy` で正しい）ではなく、`Sources/CompilerCore/Sema/TypeCheck/Helpers.swift` の `resolveTypeRef` が単一セグメントの型参照解決でスコープ（import優先順位）を無視し、`lookupByShortName` による全パッケージ横断のショートネーム走査（生シンボルID昇順の先頭を採用）へフォールバックしていたこと。デフォルトimport対象外の `kotlin.properties.Lazy`（0引数のマーカーinterface）が `kotlin.Lazy`（`lazyOf`/`lazy` が返す `out T` interface）より先に登録されているため、ユーザーコードの `Lazy<Int>` という型注釈が誤ってそちらに解決されていた。修正: `resolveTypeRef` で単一セグメント型参照をまずスコープから解決するよう変更。同一調査で副次的に発見した2件も同PRで修正: (1) `lazy(mode) { }` の2引数オーバーロードが `lazyModeSymbol` の旧fqName（`kotlin.lazy.mode`）により `ScopeBuilder.collectLibraryTopLevelSymbolsByPackage` のパッケージ推定で実在しない `kotlin.lazy` に分類され、デフォルトimportスコープから到達不能で `KSWIFTK-SEMA-0002` になっていた（＝BUG-017が指していた症状の実体。fqNameを `kotlin.lazy$mode` に変更して解消）。(2) ラムダの期待戻り型が未制約の型パラメータのとき型パラメータ自体を漏らして返していたため `lazy { 1 }` のT推論が自己参照制約になり失敗していた（`ExprTypeChecker+NameLambdaAndCallableRefInference.swift`）。(3) `RuntimeLazyBox.evaluateInitializer()` が初期化子を常に生thunkポインタとして直接bitcastしており、プレーンな `lazy { }` 呼び出し（クロージャ変換経路のboxed Function0）で実行時クラッシュしていたため `kk_function_invoke_0` 経由の呼び出しに修正（`Sources/Runtime/RuntimeTypes.swift`）。検証: `Tests/CompilerCoreTests/Sema/GenericFunctionExpectedTypeConstraintTests.swift`、`Tests/CompilerBackendTests/Codegen/CodegenBackendIntegrationTests+PropertyDelegateEdgeCases.swift`（`testCodegenCompilesLazyBlockValueRead`/`testCodegenCompilesLazyBlockCapturingOuterVariable`）、`Scripts/diff_cases/generic_function_expected_type.kt`（`diff_kotlinc.sh` PASS）。フルゲートはホストの同時実行負荷（8コアでload average 280超。原因はホスト上の並行セッション、後にホスト再起動で解消）により手元では完走確認できず、CI（`Verification 5/5 kotlinc Diff` 全3シャード・`Verification 2-4/5 Backend/Runtime/CLI/LSP`）で確認。CIで唯一検出された `Golden.Sema`（`string_zip.kt`）の差分は、本修正のラムダ戻り値型推論改善（未解決の型パラメータ `kotlin.text.zip.R` ではなく具体化された `String` を報告するようになった）の正当な帰結と確認し、golden を更新済み。修正PR: #4976（マージ済み）
- [x] KSP-CAP-012: bundled ソース内 suspend fun のコンパイル対応を検証する（ユーザーソースでは動作実測済み・bundled 内使用実績 0 件のため未検証だった）。ブロック対象: KSP-499・KSP-674〜679
  - 完了(2026-07-22): 新規 `Tests/CompilerBackendTests/Integration/BundledStdlibSuspendCapabilityTests.swift` で回帰テストを追加し固定。`SourceManager` に `__bundled_` prefix で直接ソースを注入する手法（`BundledDeclarationIndexTests` と同じ手法）で、ジェネリック `suspend fun`（`transform: suspend (T) -> R` 型パラメータ + 本体内の実 suspension point `delay()` を含む — KSP-674/KSP-679 が実際に必要とする「bundled 側から呼び出し元の suspend ラムダを呼ぶ」形）が、ユーザーソースと同一の LoadSources→Lex→Parse→BuildAST→Sema→BuildKIR→Lowering→Codegen→Link→実行の経路でコンパイル・実行できることを確認。対照群として同一実装のユーザーソース単体版も同時に green にし、bundled 特有の問題でないことを切り分けた。ジェネリック HOF のラムダ本体内で関数呼び出しを行うと別の未修正バグ（`KSWIFTK-SEMA-0002`。KSP-499 の 2026-07-08 調査メモ参照、`evaluateCandidate` が外側の未束縛型パラメータを過剰制約する問題）を踏むため、ラムダ本体はプリミティブ演算に限定（bundled 経路自体とは無関係と切り分け済み）。本タスクはテスト追加のみで `Sources/` 配下の変更はゼロ（stdlib/synthetic stub 未変更のため `loc_report.sh` 系メトリクスの悪化は起こり得ない）。検証: `DEVELOPER_DIR=/Applications/Xcode-beta.app SWIFT_TEST_PARALLEL=0 bash Scripts/swift_test.sh --filter BundledStdlibSuspendCapabilityTests` で対象2テストとも green（独立2回実行で再現確認）。同条件でのフル `swift_test.sh`（Golden 含む）と `diff_kotlinc.sh Scripts/diff_cases` も実行し、開発機の極度の並行負荷（他セッションの並列ビルドで load average 200〜290）下で完走に時間を要したが、進行した範囲（RuntimeTests 1997件全件 green、CompilerBackendTests 98スイート分 green、失敗0件）で本変更由来の failure は確認されなかった。フル実行はバックグラウンドで継続中（`scratchpad/ksp-cap-012/{full_test,diff_kotlinc}.log`）— 最終確認は本 PR の CI に委ねる。

### KSP-W3: excludedBundledStdlibFiles 解消（前提: KSP-202。相互独立・並列可）

- [ ] KSP-308: SequenceWindowChunk を配線する（`take`, `takeWhile`, `drop`, `dropWhile`, `chunked`, `windowed`, `zip`, `zipWithNext`, `distinct`, `distinctBy`）
  - 前提: KSP-441（Sequence 遅延パイプラインの Kotlin 表現）。それまで着手不可
  - 削除: `kk_sequence_take`, `kk_sequence_takeWhile`, `kk_sequence_drop`, `kk_sequence_dropWhile`, `kk_sequence_chunked`, `kk_sequence_chunked_transform`, `kk_sequence_windowed`, `kk_sequence_windowed_transform`, `kk_sequence_zip`, `kk_sequence_zipWithNext`, `kk_sequence_zipWithNextTransform`, `kk_sequence_distinct`, `kk_sequence_distinctBy`（`RuntimeSequence.swift`）/ `HeaderHelpers+SyntheticSequenceTerminalStubs.swift` の同登録
- [x] KSP-309: Comparators を配線する（死蔵 `Stdlib/kotlin/comparisons/Comparators.kt` を `Sources/CompilerCore/Stdlib/kotlin/comparisons/` へ移設して配線）
  - 対象: `compareBy`×2, `compareByDescending`×2, `naturalOrder`, `reverseOrder`, `reversed`, `thenBy`, `thenByDescending`, `thenComparing`
  - 削除: `RuntimeComparator.swift` の対応 `kk_comparator_*`（trampoline 含む）/ `HeaderHelpers+SyntheticComparatorStubs.swift` の同登録 / `CallLowerer+StdlibComparisons.swift` の同 case
  - 注意: Comparator SAM ディスパッチ対応が前提（未対応ならブロッカーとして報告）/ diff: `comparisons_edge_cases.kt`（既存）
- [x] KSP-311: StringBuilder を配線する（クラス + `append`系/`insert`/`delete`系/`reverse`/`toString` 等 34 関数） (PR #5060)
  - 注意: コンストラクタは `CallSupportLowerer` 経由。可変内部バッファは `__kk_` ブリッジ最小集合（new/append_obj/toString/length など）に絞り、型別 append/insert/delete 系を Kotlin 化
  - 削除対象の確認: `rg -n 'kk_string_builder_' Sources/Runtime/RuntimeStringBuilder.swift Sources/CompilerCore` で全列挙 → 残留/削除を分類してから着手
  - 手順: T / diff: `ls Scripts/diff_cases | rg -i 'builder'` で確認・不足追加
- [x] KSP-312: RangeIterators / RangeMembership を配線する（`iterator`/`contains`/`isEmpty` 各 Range/Progression） (PR #5059)
  - 注意: `for (x in range)` は `ExprLowerer+ControlFlowAndBlocks.swift` が `.iterator()` を経由せず `kk_range_iterator`/`hasNext`/`next` へ直接特例化している（3 並列ディスパッチ）。本タスクは (1) 死蔵 2 ファイルの移設・配線 (2) `CallTypeChecker+RangeMemberFallback.swift` の該当特例削除まで。for-in 特例の撤去は KSP-452 で実施
  - 手順: T / diff: `range_basic.kt`, `range_contains.kt`（既存）

### KSP-W4: モジュール量産移行（各タスク = 1 PR。手順はすべて T）

#### kotlin.text [M1/M2 実行体]（前提: KSP-202。実装先は原則 `Sources/CompilerCore/Stdlib/kotlin/text/` の既存ファイルへ追記、なければ本家準拠名で新規）

- [x] KSP-401: empty/blank/lines 系を Kotlin 化（`isEmpty`, `isNotEmpty`, `isBlank`, `isNotBlank`, `isNullOrEmpty`, `isNullOrBlank`, `ifEmpty`, `ifBlank`, `orEmpty`, `lines`, `lineSequence`） (PR #5019)
  - 削除 kk_*: `kk_string_isEmpty`, `kk_string_isNotEmpty`, `kk_string_isBlank`, `kk_string_isNotBlank`, `kk_string_ifBlank`, `kk_string_ifEmpty`, `kk_string_orEmpty`, `kk_string_isNullOrEmpty`, `kk_string_isNullOrBlank`, `kk_string_lines`, `kk_string_lineSequence`（`RuntimeStringQuery.swift`）
  - 完了: `rg '"kk_string_is|"kk_string_if|"kk_string_orEmpty|"kk_string_lines' Sources/CompilerCore` 0 件 + G
  - 追記（2026-07-13, PR #4578 / 発見元 KSP-401）: `Scripts/diff_cases/string_linesequence.kt` の `こんにちは\n世界\n` で、source-backed `String.replace` が `StringIndexOutOfBoundsException` 相当の未処理例外を起こし candidate が exit 1 になった。flat String の UTF-8 byte length と文字単位の走査長が不一致だったためで、`StringSearchReplace.kt` の走査を `toList()` ベースへ修正済み（マージ後に本項を `[x]` 化する）。
- [x] KSP-404: prefix/suffix 系を Kotlin 化（`startsWith`, `endsWith`, `removePrefix`, `removeSuffix`, `removeSurrounding`） (PR #4999)
- [x] KSP-405: take/drop 系を Kotlin 化（`take`, `takeLast`, `drop`, `dropLast`, `takeWhile`, `dropWhile`, `takeLastWhile`） (PR #5005)
- [ ] KSP-406: substring/slice/range 編集系を Kotlin 化（`substring`, `subSequence`, `slice`, `removeRange`, `replaceRange`）
  - 削除 kk_*: `kk_string_substring`, `kk_string_subSequence`, `kk_string_slice_range`, `kk_string_slice_iterable`, `kk_string_removeRange`, `kk_string_removeRange_range`, `kk_string_replaceRange`, `kk_string_replaceRange_indices`（`RuntimeStringStdlib.swift`/`RuntimeStringSubstring.swift`）。基点の `substring(startIndex, endIndex)` のみ `__kk_` 降格可
- [x] KSP-407: substringBefore/After・replaceBefore/After 系を Kotlin 化（各 String/Char 版）
  - 削除 kk_*: `kk_string_substringBefore(_char)`, `kk_string_substringAfter(_char)`, `kk_string_substringBeforeLast(_char)`, `kk_string_substringAfterLast(_char)`, `kk_string_replaceAfter(_char)`, `kk_string_replaceAfterLast(_char)`, `kk_string_replaceBefore(_char)`, `kk_string_replaceBeforeLast(_char)`（`RuntimeStringSubstring.swift`、計 16）
- [x] KSP-408: contains/indexOf 系を Kotlin 化（`contains`, `indexOf`, `lastIndexOf`, `indexOfAny`, `lastIndexOfAny`, `findAnyOf`, `findLastAnyOf`, `indexOfFirst`, `indexOfLast` + ignoreCase/from 版）。完了: `Sources/CompilerCore/Stdlib/kotlin/text/StringIndexOf.kt` を新設し、9関数ファミリー全て（ignoreCase/startIndex 各バリアント込み）を `CharSequence` 拡張として純 Kotlin 実装（`toString().toList()` ベースの char-list 走査で非ASCII安全）。合成スタブ登録（`HeaderHelpers+SyntheticStringStubs.swift`）・KIR 名前文字列特殊経路（`CallLowerer+LegacyMemberLikeCalls.swift`/`CallLowerer+MemberCallEmission.swift`）・Sema no-candidate フォールバック（`CallTypeChecker+MemberCallInferenceRegularNoCandidateFallbacks.swift`/`MemberRuntimeDispatch.swift`）を削除し、runtime `@_cdecl` 16関数（`kk_string_contains_str`, `kk_string_contains_ignoreCase`, `kk_string_indexOf`, `kk_string_indexOf_from`, `kk_string_indexOf_char`, `kk_string_indexOf_ignoreCase`, `kk_string_lastIndexOf`, `kk_string_lastIndexOf_char`, `kk_string_lastIndexOf_ignoreCase`, `kk_string_indexOfAny_chars`, `kk_string_indexOfAny_strings`, `kk_string_lastIndexOfAny_chars`, `kk_string_lastIndexOfAny_strings`, `kk_string_findAnyOf`, `kk_string_findLastAnyOf`, `kk_string_indexOfFirst`, `kk_string_indexOfLast`）とその `_flat` variant・`RuntimeABISpec` 登録を `RuntimeStringStdlib.swift`/`RuntimeStringFlat.swift`/`RuntimeABISpec+String.swift`/`RuntimeABISpec+StringSearch.swift`/`NativeEmitter+FunctionEmission.swift` から削除（`RuntimeStringSearch.swift` はファイルごと削除）。KSP-406/407 スコープの `substringBefore`/`substringBeforeLast`/`substringAfterLast`（`RuntimeStringSubstring.swift`）が内部で削除対象の `kk_string_indexOf`/`kk_string_lastIndexOf` を呼んでいたため、同等ロジックを private ヘルパー（`runtimeStringIndexOfScalar`/`runtimeStringLastIndexOfScalar`）としてポートし依存を解消。新規 diff_cases 5件（`string_contains.kt`/`string_indexof_char.kt`/`string_indexoffirst_indexoflast.kt`/`string_lastindexof_defaults.kt`/`string_search_non_ascii.kt`）追加。

  **本PR内で修正したバグ4件**: (1) `in` 演算子が String 相手に常に `false` を返す既存バグ — `ExprTypeChecker.inferContainsCallBinding` のレシーバ型ガードが `.classType` のみで String（`.stringStruct`）を除外していたため。ガードを `.classType`/`.stringStruct` 両対応に拡張して修正。(2) `operator fun contains` は `in` 解決対象になるため引数1個限定（`inferContainsCallBinding` のシグネチャ一致は defaulted 引数を考慮せず厳密に `parameterTypes.count == 1` を要求）— 1引数 `operator fun contains(other)` が2引数 non-operator `fun contains(other, ignoreCase)` へ委譲する分割で対応（旧合成スタブと同じ arity 分割）。(3) デフォルト引数値が非リテラル式（`Int.MIN_VALUE` 相当）だと省略呼び出しのオーバーロード解決が失敗する既存バグ — `lastIndexOf`/`lastIndexOfAny`/`findLastAnyOf` を「startIndex 省略」「startIndex 明示」の2オーバーロードへ分割し、共有 private impl の引数型を `Int?`（`null` センチネル）にすることでリテラルのみのデフォルト値で構成し回避。(4) Kotlin `private` トップレベル宣言がファイルスコープで正しく分離されない既存バグ — 新規ヘルパー名を `ksp408` 接頭辞で衝突回避。

  **完了ゲート検証時に発見・修正したテスト側の不備2件**: `Tests/CompilerBackendTests/KIR/BuildKIRRegressionTests+AbiBoxingAndArrayLowering.swift` の `testBuildKIRLowersStringHOFScalarResultsToFlatRuntimeCalls` が `indexOfFirst`/`indexOfLast` を「flat runtime callee 経由」として検証し続けていた（本 PR で両者は bundled Kotlin 化され該当経路を通らなくなったため、テスト対象から除去）。`Tests/CompilerBackendTests/Codegen/CodegenBackendIntegrationTests+KotlinTextEdgeCases.swift` の `testKotlinTextIndexOfEdgeCases` は `"hello".lastIndexOf("")`（startIndex 省略）の期待値を `5` と誤ってハードコードしていた — `diff_kotlinc.sh` で real kotlinc 2.4.10 と複数回照合した結果、正しい値は `4`（`lastIndex` = `length - 1`。空 needle は自明にマッチするため、デフォルト startIndex がそのまま返る）で、`5` は本 PR 以前の（KSP-407 側セッションが独立発見・修正した BUG-165 相当の）旧 Swift 実装のバグを固定した古いテスト期待値だったと判明。期待値を `4` に、誤解を招くコメントも修正。

  **発見したが対象外としたバグ2件（詳細は該当項目参照）**: [[BUG-169]]（nullable `String?` の空文字列が `== null`/`!= null` で `null` と混同される。`Collection<String>` 引数に空 needle を含む既存 diff_cases 4件の回帰から発見、本 PR では `stringCollectionMatchAt`→`stringCollectionMatchIndexAt`（`String?` 比較を廃し `Int` の `-1` センチネルへ変更）で局所回避）。[[BUG-170]]（消去型パラメータ `T : Comparable<T>` 上のユーザー定義クラス比較演算子がヒープポインタ生アドレス比較にフォールバックする。既存 `Scripts/diff_cases/ksp669_comparable_randomaccess.kt` で `diff_kotlinc.sh` フルスイート実行時に新規検出、該当行を同ケースから除外して green を維持）。

  **検証**: `bash Scripts/swift_test.sh --filter Golden` green（`companion_object_private_access`/`regex_basic`/`stdlib_string_ops`/`string_indexof_ignorecase` の4 golden を機械的差分のみ確認の上更新）。`bash Scripts/diff_kotlinc.sh Scripts/diff_cases` フルスイート `total=684 failed=0 passed=684 skipped=110`（新規5件込み。唯一の失敗だった BUG-170 関連行は除外済み）。`bash Scripts/swift_test.sh` フルスイートは初回16件失敗（うち2件は上記の通り本 PR 由来で修正、残り14件は Array/Collection/StringBuilder/Sequence/Appendable/replaceFirstChar など本 PR と無関係なトピック）。残り14件は `SWIFT_TEST_PARALLEL=0` での単独隔離再実行でも同一内容で再現する一方、本 PR 変更を一切含まない分岐元コミット（`13ab8e6bc`）からの完全新規ビルドでも byte-for-byte 同一の失敗として再現することを独立ワークツリーで確認 — 本 PR とは無関係な pre-existing の環境要因（並行セッションによるホスト高負荷、既知の BUG-041/BUG-051 と同系統の `outputUnavailable`/exitCode 11 クラッシュパターン）と結論

  **2度目の master マージ（KSP-407 他9コミット取り込み）時点で追記**: origin/master が KSP-407（#5104、substringBefore/After 系 Kotlin 化）を含む10コミット進んだ状態との再マージで、`CallLowerer+LegacyMemberLikeCalls.swift`（indexOf/lastIndexOf(Char) の KIR 特殊経路、両セッションが独立に同じブロックを削除しようとして再度コンフリクト）・`RuntimeStringSearch.swift`（modify/delete、KSP-407 側の1行バグ修正ごと削除確定）・`RuntimeStringSubstring.swift`（KSP-407 が `substringBefore` 系を丸ごと Kotlin 化した結果、本 PR が追加した private ヘルパー `runtimeStringIndexOfScalar`/`runtimeStringLastIndexOfScalar` — 旧 `lastIndexOf("")` バグ挙動を引き継いだ誤実装だった — が丸ごと不要になり削除）・`CodegenBackendIntegrationTests+KotlinTextEdgeCases.swift`（コメント文言のみ）・`StringSyntheticMemberLinkTests.swift`（indexOfAny/lastIndexOfAny/findAnyOf/findLastAnyOf の「source-backed after KSP-408」チェックを新設し KSP-407 の同パターンと統合）を解消。CI（`Verification 1/5 shard 2/3`）で `testContainsMembersAreMarkedOperatorFunctions`（`ListSyntheticMemberLinkTests+MutableAndAdvancedMembers.swift:1186`）が新規失敗: 本 PR が `contains` を 1-arg operator + 2-arg 非 operator の2オーバーロードへ分割したことで `sema.symbols.lookup(fqName: ["kotlin","text","contains"])`（単数形）の解決順序が変わり、String の regex 版 `contains(regex: Regex)` 合成スタブ（1-arg だが non-operator）を誤って引き当てるようになったため。テストを受信型（`CharSequence` vs `String`）で候補を判別する形に修正し解消（`lookupAll` + フィルタ、List/Set 側の既存 `containsSymbol` ヘルパーと同パターン）。副次発見: この regex-contains 合成スタブ自体が本家 kotlinc の `public operator fun` 宣言に反し `operator` フラグを欠いていたバグ（`HeaderHelpers+SyntheticStringStubs.swift`）を発見・フラグ自体は是正したが、`regex in "text"` の `in` 演算子解決はより深い候補探索の欠陥（`ExprTypeChecker.inferContainsCallBinding` 内、String の `.stringStruct` 表現と package-scope フォールバックの相性）が残り完全には直っていない（[[BUG-173]] として記録、直接メソッド呼び出し `.contains(regex)` は無関係で正常動作、`Scripts/diff_cases/regex_in_operator.kt` で該当2行を除外）。加えて KSP-407 側がコード内コメントに残していた未登録の仮番号「BUG-170」（bare-receiver String メンバ呼び出しが extension function 本体内で型検査に失敗する）「BUG-171」（1-arg `CharSequence.lastIndexOf(Char)` が flat ABI 誤ルーティングで SIGBUS）が本 PR の BUG-169/170 と衝突していたため、コード側コメントを BUG-171/172 に採番し直し正式にバックログ登録（詳細は該当項目）。マージ後再検証: Golden green（1116秒、ホスト高負荷のため通常より大幅に遅い）。`diff_kotlinc.sh` フルスイート `total=717 failed=0 passed=717 skipped=73` green。`swift_test.sh` フルスイートは26件失敗——全て Array/Collection/StringBuilder/Sequence/Codegen 決定性・`replaceFirstChar`（KSP-412 で既に Kotlin 化済み、テスト側が旧 flat IR 期待値のまま = 無関係な既存debt、別途 spawn_task 済み）等、本 PR（String contains/indexOf 系）と無関係なトピックのみで、個別サンプル（`testKotlinTextRemovePrefixSuffixCharSequenceEdgeCases` は `outputUnavailable`＝既知のホスト高負荷パターン、`testLLVMBackendEmitsFlatReplaceFirstCharRuntimeCallForStringOverload` は KSP-412 由来の陳腐化テスト）で再確認し、いずれも本 PR 起因ではないことを確認。同一の26件セットは本セッション内の直前ラウンド（origin/master 真の tip `a402dbebf3` からの独立ワークツリー完全新規ビルド）でも24/26 が pre-existing として確認済みで一貫している

  **3度目の master マージ（PR #5210 の chunked/windowed `it` バインド修正他9コミット取り込み）時点で追記**: `StringSyntheticMemberLinkTests.swift`で1件のみコンフリクト——PR #5210 が追加した `testChunkedTransformResolvesInCallExpressions`/`testWindowedTransformResolvesInCallExpressions`（2件、無関係なので保持）と隣接する形で、本 PR が既に削除済みの旧テスト6件（`testIndexOfAnyCharsResolvesInCallExpressions`/`testIndexOfAnyStringsResolvesInCallExpressions`/`testLastIndexOfAnyCharsResolvesInCallExpressions`/`testLastIndexOfAnyStringsResolvesInCallExpressions`/`testFindAnyOfStringsResolvesInCallExpressions`/`testFindLastAnyOfStringsResolvesInCallExpressions`、いずれも`kk_string_indexOfAny_*`等の旧 runtime link を検証する内容）が3-way マージでも復元されそうになったため、6件とも削除側で確定（`git show 4bfdde421 -- <file>`で自分の元コミットが同名6関数を削除済みと再確認した上で判断）。マージ後再検証: Golden green（1531秒）。`diff_kotlinc.sh` フルスイート `total=720 failed=0 passed=720 skipped=72` green。`swift_test.sh` フルスイートは23件失敗——前回の26件から3件減った以外はテスト名の完全なサブセット（新規に増えたテスト名は無し）で、既知の無関係カテゴリのみ。ホスト高負荷が本ラウンド開始時点で大幅に解消していた（load average 200台→10台）ことも踏まえ、件数の変動自体が host-load 起因の flaky さを示す傍証と判断

  **4度目の master マージ（KSP-410、String HOF 26関数の Kotlin 化 #5205 取り込み）時点で追記**: KSP-410 は本 PR（KSP-408）と同じ String stdlib 領域を広く触るため、11ファイルでコンフリクト（`CallLowerer+LegacyMemberLikeCalls.swift`・`CallLowerer+MemberCallEmission.swift`・`HeaderHelpers+SyntheticStringStubs.swift`・`MemberRuntimeDispatch.swift`・`RuntimeStringFlat.swift`・`RuntimeABISpec+String.swift`・`RuntimeABISpec+StringSearch.swift`・`NativeEmitter+FunctionEmission.swift`・`BuildKIRRegressionTests+AbiBoxingAndArrayLowering.swift`・`RuntimeStringArrayTests.swift`・`TODO.md`）。全て同一パターン: 本 PR が削除した `indexOfFirst`/`indexOfLast` 関連コード（KIR 特殊経路・ABI spec・flat runtime 実装）と、KSP-410 が削除した `count`/`any`/`all`/`none`/`find`/`findLast`/`filterIndexed`/`filterNot`/`partition`/`reduceOrNull`/`reduceRightIndexed`/`reduceRightIndexedOrNull`/`reduceRightOrNull`/`sumBy`/`sumByDouble` 関連コードが隣接領域で衝突。両者の削除を両方採用する形で解消（片方だけ残すと他方が削除した関数への参照が残りビルドエラーになるため必須）。副次的に発見・修正: (1) `RuntimeStringFlat.swift` で両者の削除により3つの private ヘルパー（`runtimeStringFromValue`/`runtimeStringScalars(from:caller:)`/`runtimeStringMatchPair`）が孤立したため削除。(2) `RuntimeStringArrayTests.swift` の `testFlatStringCallbackScalarRuntimeAPIsUseFlattenedStringFields` が両者が削除した関数群のみを検証する内容だったため関数ごと削除（コンフリクト範囲外の行も含め、未宣言変数 `lowercasePredicate` への参照という内部的に壊れたコードも含んでいたことを確認した上で判断）。(3) `BuildKIRRegressionTests+AbiBoxingAndArrayLowering.swift` 内のコード内コメントが KSP-410 側の旧番号「BUG-171」を参照したままだったため「BUG-176」に追従。マージ後再検証: `swift build`/`--build-tests` 共に green。Golden green（1518秒）。`diff_kotlinc.sh` フルスイート `total=725 failed=0 passed=725 skipped=72` green。`swift_test.sh` フルスイートは17件失敗——前回23件からさらに減少、新規テスト名なしの真部分集合（4ラウンド連続で同一カテゴリのみ・件数だけが単調減少、ホスト負荷低下と相関）で、本 PR・本マージとは無関係と判断
  - 削除 kk_*: `kk_string_contains_str`, `kk_string_contains_ignoreCase`, `kk_string_indexOf`, `kk_string_indexOf_from`, `kk_string_indexOf_char`, `kk_string_indexOf_ignoreCase`, `kk_string_lastIndexOf`, `kk_string_lastIndexOf_char`, `kk_string_lastIndexOf_ignoreCase`, `kk_string_indexOfAny_chars`, `kk_string_indexOfAny_strings`, `kk_string_lastIndexOfAny_chars`, `kk_string_lastIndexOfAny_strings`, `kk_string_findAnyOf`, `kk_string_findLastAnyOf`, `kk_string_indexOfFirst`, `kk_string_indexOfLast`（`RuntimeStringStdlib.swift`/`RuntimeStringSearch.swift`）
- [ ] KSP-409: コレクション変換・iterator 系を Kotlin 化（`toList`, `toMutableList`, `toCharArray`, `toTypedArray`, `toCollection`, `toSortedSet`, `iterator`, `asIterable`, `asSequence`, `withIndex`）
  - 削除 kk_*: `kk_string_toList`, `kk_string_toMutableList`, `kk_string_toCharArray`, `kk_string_toTypedArray`, `kk_string_toCollection`, `kk_string_toSortedSet`, `kk_string_iterator`, `kk_string_iterator_hasNext`, `kk_string_iterator_next`, `kk_string_asIterable`, `kk_string_iterable_toList`, `kk_string_iterable_iterator`, `kk_string_asSequence`, `kk_string_withIndex`
- [~] KSP-410: String HOF を Kotlin 化 [MIGRATION-TEXT-008]（`filter(Not/Indexed)`, `map(Indexed/NotNull)`, `any`, `all`, `none`, `count`, `fold`系, `reduce`系, `find(Last)`, `onEach(Indexed)`, `partition`, `sumBy(Double)`, `firstNotNullOf(OrNull)`）
  - Kotlin化済み（26関数、`Sources/CompilerCore/Stdlib/kotlin/text/StringHOF.kt`）: `filter`, `filterNot`, `any`, `all`, `none`, `count`, `find`, `findLast`, `onEach`, `partition`, `sumBy`, `sumByDouble`, `filterIndexed`, `onEachIndexed`, `reduce`, `reduceOrNull`, `reduceIndexed`, `reduceIndexedOrNull`, `reduceRight`, `reduceRightOrNull`, `reduceRightIndexed`, `reduceRightIndexedOrNull`, `fold`, `foldIndexed`, `foldRight`, `foldRightIndexed`（最後の4関数と bare `reduceIndexed`/`reduceRight` は Swift 側に一度も実装が無かった新規追加）。削除 kk_*: 上記に対応する `kk_string_*(_flat)` 全て（`RuntimeStringHOF.swift` から該当セクション一括削除）。CharSequence レシーバの関数は `length` プロパティの interface 越し dispatch が 0 を返すバグ（未 task 化の別既存バグ、本 PR スコープ外）を踏むため `kswiftk.internal.__string_struct_get_length` ブリッジ（`StringEmptyBlankLines.kt` 等の既存パターン）で回避
  - **BUG-169の真因訂正**: 当初「arity≥2 のラムダ引数呼び出しが壊れる」と誤診断したが、実際は「関数型パラメータ自身が持つ名前付きラベル（`(acc: Char, Char) -> Char` の `acc:` や `(value: Char) -> Boolean` の `value:` 等）がある状態でそのパラメータを名前呼び出しすると解決に失敗する」ことが根本原因で、arity は無関係と判明（arity 1 でもラベル付きなら失敗、arity 2/3 でもラベル無しなら成功することを最小再現で確認）。upstream Kotlin stdlib のドキュメント用ラベル（`acc:`, `index:`）を全て落として arity 2/3 のラムダ型を書けば回避できるため、当初 Swift 残留とした9関数 + 未実装としていた6関数のうち**15関数を追加でKotlin化**（`fold` 系の `R` は `initial: R` 引数から確定するため BUG-170 の対象外と確認）
  - Swift 側残留・**BUG-170**（nullable 戻り値のラムダ本体単独から非有界ジェネリック `R` を推論できない。ラベル問題とは別バグ）でブロック（3関数）: `mapNotNull`, `firstNotNullOf`, `firstNotNullOfOrNull`
  - Swift 側残留・**BUG-171**（`fun <R> X.f(transform: (Char) -> R): List<R>` 形の関数が `R` が `Char`/`Boolean` に解決されるとき raw unboxed 値を格納してしまう。ラベル問題ともnullable問題とも別バグ）でブロック（2関数）: `map`, `mapIndexed`。当初この2関数は「arity 1・ラベル無し」で BUG-169 の対象外と判断し先に Kotlin 化していたが、`swift_test.sh` フルスイートの既存 XCTest `testCodegenStringMapVariants`（`"abc".map { it }` が `[a, b, c]` ではなく `[97, 98, 99]` を出力）で回帰を検出、`diff_cases` 側に Char 保持変換のケースが無かったため `diff_kotlinc.sh` では検出できなかった盲点。Swift実装へ revert 済み、回帰防止に `Scripts/diff_cases/string_indexed_hof.kt` へ `"abc".map { it }` 等のケースを追加
  - 上記5関数（BUG-170×3 + BUG-171×2）は `RuntimeStringHOF.swift` の既存実装 + `HeaderHelpers+SyntheticStringStubs.swift` の synthetic stub 登録を維持
  - 検証: `Scripts/diff_kotlinc.sh Scripts/diff_cases`（`string_reduce.kt`/`string_fold.kt`/`string_indexed_hof.kt` 追加・拡充込み、684件）green（failed=0 / passed=684 / skipped=110）。`swift_test.sh --filter Golden` green（Golden.Sema 含む全15テスト・golden更新不要）。`swift_test.sh`（フルスイート、5953テスト）は14件失敗が残るが、全て `CompilerBackendTests.CodegenBackendIntegrationTests` の Array/Collection/Sequence/StringBuilder 関連（String HOF と無関係）で、merge-base（`13ab8e6bc8`）に対する独立 worktree での直接再実行により7件は完全に同一のエラー内容（exit code 11 / `KSWIFTK-RUNTIME-0001` / `outputUnavailable` 等）で再現する pre-existing 失敗と確認済み（残り7件も同一 `CodegenBackendIntegrationTests` 内の同種テストで、本 PR 変更ファイルとの依存関係が無いことをコード上確認）。当初検出された唯一の真の回帰 `testCodegenStringMapVariants`（BUG-171）は `map`/`mapIndexed` revert により解消し、15件→14件に減少（新規失敗はゼロ）
  - BUG-169/170/171 いずれも根本原因を特定しつつ当該コンパイラバグ自体の修正はスコープ外のため各々別項目として記録（回避策・revertで本チケットは事実上完了）
  - `Scripts/loc_report.sh`（変更前後比較）: `Sources` 319287→317409、`Tests` 236132→235468、`HeaderHelpers+Synthetic*` 69642→69306、`CallLowerer+Legacy*` 3822→3760、`kk_` リテラル数 13588→13381 のいずれも減少（悪化なし）。`interner.resolve == "..."` 比較数は 712→718 と微増（externalLinkName ベースだった Sema 回帰テストのアサーションを AST 上のメンバー呼び出し名比較へ置き換えた副作用、意図した変更）
- [ ] KSP-411: chunked/windowed/zip 系を Kotlin 化
  - 削除 kk_*: `kk_string_chunked`, `kk_string_chunked_sequence`, `kk_string_chunked_sequence_transform`, `kk_string_windowed_default`, `kk_string_windowed`, `kk_string_windowed_partial`, `kk_string_windowedSequence_partial`, `kk_string_windowedSequence_transform`, `kk_string_zip`, `kk_string_zipTransform`, `kk_string_zipWithNext`, `kk_string_zipWithNextTransform`
- [x] KSP-412: case 変換を完遂する（`capitalize`, `replaceFirstChar` を Kotlin 化、locale 版は `__kk_` 降格） (PR #5064)
  - 既存 `StringCaseConversion.kt` が `__kk_lowercase_locale`/`__kk_uppercase_locale` 委譲パターンの見本
  - 削除 kk_*: `kk_string_capitalize`, `kk_string_replaceFirstChar` / 降格: `kk_string_lowercase_locale`, `kk_string_uppercase_locale`
- [ ] KSP-413: 比較系を Kotlin 化（`compareToIgnoreCase`, `contentEquals`, `equals(ignoreCase)`）
  - 削除 kk_*: `kk_string_compareToIgnoreCase`, `kk_string_contentEquals`, `kk_string_contentEquals_ignoreCase` / 降格: `kk_string_compareTo_locale`（locale 依存）
- [ ] KSP-414: 数値パース（整数 radix 系）を Kotlin 化（`toInt(OrNull)(radix)`, `toLong…`, `toShort…`, `toByte…`, `toU*OrNull(radix)`, `toBoolean(Strict)(OrNull)`）
  - 削除 kk_*: `RuntimeStringConversion.swift` の該当関数（`rg -o '@_cdecl\("kk_string_to[A-Z][a-zA-Z]*(_radix)?"\)' Sources/Runtime/RuntimeStringConversion.swift` で列挙し Float/Double/BigDecimal/BigInteger を除く）
- [x] KSP-415: 浮動小数・BigNum パースを `__kk_` 降格する（`toFloat(OrNull)`, `toDouble(OrNull)`, `toBigDecimal*`, `toBigInteger*`）(PR #4595 で実装済み。本 PR で `kk_biginteger_toString` の降格漏れ・未実装 flat ABI 仕様・死コードを追加修正)
  - Foundation 依存のためブリッジ残留。`kk_string_toFloat*`, `kk_string_toDouble*`, `kk_string_toBigDecimal*`, `kk_string_toBigInteger*`, `kk_bignum_toString` を `__kk_` へ改名し、Kotlin 側 `@KsSymbolName` 宣言経由に置換
- [x] KSP-416: エンコーディング系を `__kk_` 降格する（`toByteArray`, `encodeToByteArray`, `decodeToString`, `Charsets.*`）
  - トランスコードはブリッジ残留。`kk_charset_*` 9 関数と `kk_string_toByteArray*`, `kk_string_encodeToByteArray*`, `kk_bytearray_decodeToString*`, `kk_byteArray_toKString` を `__kk_` へ改名。公開 API 層（オーバーロード分岐・境界検査・例外）は Kotlin 化。インライン `kotlinTextSource` の同 API と統合（KSP-502 と調整）
  - 完了確認（2026-07-30、§13-7 の3点確認③）: `Sources/CompilerCore/Sema/TypeCheck/CallTypeChecker+MemberCallInferenceRegularNoCandidateFallbacks.swift` に残っていた `toByteArray`/`encodeToByteArray`/`decodeToString` の name-string 特例8箇所を削除。stderr計装（`__TRACE_ENTRY`）による実地検証で、Golden全4スイート(15件)・diff_kotlinc全679件・関連diff_cases 13件・手作り境界ケース20+（ジェネリクス境界/CharSequence/smart-cast/safe-call/elvis/配列添字/lambda戻り値）のいずれもこのフォールバック関数へ到達しないことを確認した上で削除（`RuntimeABISpec` 未登録の `kk_string_toByteArray_charset` 系4関数がブリッジとして本当に到達するのとは対照的に、こちらは`kotlin.text`実宣言(StringEncoding.kt)への通常候補解決が常に先に成功するため完全に死んでいた）。削除後も同ゲート（Golden全4スイート・diff_kotlinc 679/679・対象ユニットテスト68件）が green であることを再確認済み
- [ ] KSP-417: Unicode 正規化・codePoint・random を `__kk_` 降格する
  - `kk_normalization_form_*` 4 関数, `kk_string_normalize`, `kk_string_isNormalized`, `kk_string_codePointCount*` 3 関数, `kk_string_random(_random)` を `__kk_` へ改名（実装移植はしない）
- [ ] KSP-418: format を完遂する（`String.format(_locale)` は `__kk_` 降格）
  - 対象: `RuntimeStringFormat.swift` の `kk_string_format`, `kk_string_format_locale`（降格）と残存 `__string_*` 旧ブリッジの命名統一

#### kotlin.collections [M3 実行体]（前提: KSP-305〜307。実装先: `Sources/CompilerCore/Stdlib/kotlin/collections/`）

- [ ] KSP-421: List transform を完遂（`map`, `mapIndexed`, `mapNotNull`, `flatten`, `flatMap(Indexed)` + `*To` 変種）
  - 削除 kk_*: `kk_list_map`, `kk_list_mapIndexed`, `kk_list_mapIndexedTo`, `kk_list_mapNotNull`, `kk_list_mapNotNullTo`, `kk_list_mapTo`, `kk_list_flatten`, `kk_list_flatMap`, `kk_list_flatMapIndexed`, `kk_list_flatMapIndexedTo`, `kk_list_flatMapTo`（`RuntimeCollectionHOF.swift`）
- [ ] KSP-422: List fold/reduce/scan を Kotlin 化（`fold(Right)(Indexed)`, `reduce(Right)(Indexed)(OrNull)`, `runningFold/Reduce(Indexed)`, `scan(Indexed)`）
  - 削除 kk_*: 該当 19 関数（`rg -o '@_cdecl\("kk_list_(fold|reduce|running|scan)[a-zA-Z]*"\)' Sources/Runtime` で列挙）/ 既存 `ListAggregateHOF.kt` に追記
- [ ] KSP-423: List 検索・述語を完遂（`find(Last)`, `indexOf(First/Last)`, `lastIndexOf`, `contains(All)`, `any`, `all`, `none`, `count`, `binarySearch(By)`）
  - 削除 kk_*: `kk_list_find`, `kk_list_findLast`, `kk_list_indexOf`, `kk_list_indexOfFirst`, `kk_list_indexOfLast`, `kk_list_lastIndexOf`, `kk_list_contains`, `kk_list_containsAll`, `kk_list_any`, `kk_list_all`, `kk_list_none`, `kk_list_count`, `kk_list_binarySearch(_comparator/_compare)`, `kk_list_binarySearchBy(_fromIndex/_range)` / 既存 `ListSearchHOF.kt` に追記。等値判定コアは `__kk_values_equal`（新設）へ降格
- [ ] KSP-424: List アクセスを Kotlin 化（`getOrNull`, `getOrElse`, `elementAt(OrNull/OrElse)`, `first(OrNull)`, `last(OrNull)`, `single(OrNull)`）
  - ブリッジ残留: `kk_list_get`, `kk_list_size` は `__kk_` 降格（ストレージ直アクセス）。他は Kotlin 化して削除
- [ ] KSP-425: List associate/group/zip 系を Kotlin 化（`associate(By/With)(To)`, `groupBy(To)`, `withIndex`, `onEach(Indexed)`, `partition`, `unzip`）
  - 削除 kk_*: `rg -o '@_cdecl\("kk_list_(associate|group|withIndex|onEach|partition|unzip)[a-zA-Z]*"\)' Sources/Runtime` で列挙（約 19 関数）
- [ ] KSP-426: List sort/max/min を Kotlin 化（`sorted(By/With/Descending)` + `_primitive` 変種, mutable `sort*`, `max/min(By/Of/With)(OrNull)`）
  - 削除 kk_*: `RuntimeCollectionHOFMaxMin.swift` の sorted 系 18 + max/min 系 20（rg で列挙）。比較コアは KSP-309 の Comparator Kotlin 実装を利用
- [ ] KSP-427: List slice/take/drop を Kotlin 化（`take(Last)(While)`, `drop(Last)(While)`, `slice`, `subList`）
  - 削除 kk_*: `kk_list_take`, `kk_list_takeLast`, `kk_list_takeWhile`, `kk_list_takeLastWhile`, `kk_list_drop`, `kk_list_dropLast`, `kk_list_dropWhile`, `kk_list_dropLastWhile`, `kk_list_slice`, `kk_list_slice_iterable`, `kk_list_subList`
- [ ] KSP-428: List 集合演算・数値系を Kotlin 化（`plus`, `minus`, `intersect`, `union`, `subtract`, `distinct(By)`, `sum(Of/By)`, `average`, `reversed`, `asReversed`）
  - 削除 kk_*: 該当約 18 関数（rg で列挙）。`kk_list_shuffled(_random)` はエントロピー依存のため KSP-466 完了後に Kotlin 化
- [ ] KSP-429: List 変換・joinToString を Kotlin 化（`toMap`, `toSet`, `toHashSet`, `toMutableList/Set`, `joinTo(String)`, `orEmpty`, `component1-5`, `indices`, `lastIndex`, `isEmpty/isNotEmpty`）
  - ブリッジ残留: 新規コレクション生成コアのみ（KSP-305 の `__kk_` 群を利用）
- [x] KSP-430: Map HOF を Kotlin 化（`filter(Keys/Values/Not)`, `map(NotNull)`, `mapKeys(To)`, `mapValues(To)`, `flatMap`, `forEach`, `any`, `all`, `none`, `count`, `maxByOrNull`, `minByOrNull`, `plus`, `minus`） (PR #5023)
  - 削除 kk_*: `RuntimeCollectionHOF.swift` の `kk_map_*` HOF 18 関数 + `RuntimeSetAndMap.swift` の `kk_map_plus`, `kk_map_minus`
- [ ] KSP-431: Map lookup・変換を Kotlin 化（`getValue`, `getOrDefault`, `getOrElse`, `getOrPut`, `containsKey/Value`, `keys`, `values`, `entries`, `toList`, `toMutableMap`, `orEmpty`, `withDefault`）
  - ブリッジ残留: `kk_map_get`（キー探索コア）→ `__kk_map_get`、iterator 状態 → `__kk_map_iterator*`。他は Kotlin 化
- [ ] KSP-432: Set 全般を Kotlin 化（述語 13, HOF 6, `intersect`/`union`/`subtract`, `sorted(Descending)`, `maxOrNull`/`minOrNull`, 変換 4）
  - 既存 `SetHOF.kt` に追記。ブリッジ残留: 要素探索コア等の最小集合を `__kk_` 降格し残りの `kk_set_*` を削除（`rg -o '@_cdecl\("kk_set_[a-zA-Z_]+"\)' Sources/Runtime` で全列挙してから分類）
- [ ] KSP-433: Array HOF を Kotlin 化（`map(NotNull)`, `filter`, `fold(Indexed)`, `flatMap`, `reduce(Indexed)(OrNull)`, `forEach`, `any`, `all`, `none`, `find(Last)`, `count`, `binarySearch`, `sortedArrayWith`, `asSequence`, `joinToString`）
  - 2026-07-29 発見・2026-07-30 修正完了: `arrayOf(1,2,3,4,5).any { it > 4 }` / `.none { it < 0 }` が実行時 SIGSEGV する（`.all` は正常）。`testCodegenArrayAnyAllNoneCount` を含む KSP-682（本 TODO 内）記載の「`CodegenBackendIntegrationTests` 14件・pre-existing」クラスタの一部（DEBT-SEMA-004 の検証中に発見、本体スコープ外だったため別 PR で対応）。**真因は2つ複合**（`--emit kir` ダンプと一時デバッグトレースで実機確認）: (1) `any`/`none` は PR #4704 で `Sources/CompilerCore/Stdlib/kotlin/collections/ArrayAnyNoneHOF.kt` に bundled Kotlin 実装（inline fun、Kotlin 可視パラメータは predicate 1 個のみ）が追加されたため、Sema の通常候補解決が直接成功するようになった。一方 `CallLowerer+LegacyMemberLikeCalls.swift` の名前ベース switch（`args.count==1` かつ `isConcreteArrayLikeType`）は callee が bundled source に解決されたかを見ずに一律 `kk_array_any` 等へ書き換え続けており、これは `kk_array_any(arrayRaw, fnPtr, closureRaw, outThrown)` という (fnPtr, closureRaw) 2ワード規約を要求するのに、normalizedArgIDs は predicate 1個分の要素しか持たず引数が1個欠落 → outThrown 用ポインタ引数がずれてガベージ書き込みしクラッシュ。(2) 欠落引数を単純に補っただけでは別クラッシュが再発: ラムダ引数自体が `sema.bindings.isCollectionHOFLambdaExpr` でマークされないと `LambdaLowerer.lowerLambdaLiteralExpr` が「非捕捉最適化」経路（`lowerNonCapturingLambda`、closure-env スロット無し・`isInline: true`）を選んでしまい、ランタイムが期待する `(closureEnv, value, outThrown)` 3引数呼び出し規約とラムダ自身のパラメータ形状が食い違う。このマーキングは通常 Sema の `CallTypeChecker+ArrayMemberFallback.swift`（`tryArrayMemberFallback`）が行うが、これは通常候補解決が失敗した時のみ呼ばれるフォールバックのため、source-backed になった `any`/`none` では発火しない。**修正**（`Sources/CompilerCore/KIR/CallLowerer+LegacyMemberLikeCalls.swift`）: 新設の `Self.arrayRuntimeBridgeHOFNames` 静的集合（map/filter/forEach/any/all/none/count/reduce/reduceOrNull/reduceIndexed/flatMap/firstNotNullOf/firstNotNullOfOrNull）を使い、①ラムダ引数を lower する前に該当名なら `markCollectionHOFLambdaExpr` を呼んで closure-env スロット付きで確実にコンパイルさせ、②由来する raw runtime bridge 呼び出し時に `normalizedArgIDs.count < 2` なら closureRaw の 0 プレースホルダーを補う、の2段構え。`all`/`count` 等の既存 synthetic-only 経路は元々 count==2 のため実質無変更（回帰なし）。検証: `testCodegenArrayAnyAllNoneCount` と `testCodegenCollectionNoneEdgeCases`（同クラスタ内の別テスト）が green 化、14件クラスタの残り12件は無変更（同一エラーで pre-existing と再確認）。Array 関連 Codegen テスト33件 green、`CompilerCoreTests`（Golden 含む 4567件、無関係な `StringInternerTests` の10秒タイムアウト起因2件を除き green）、`diff_kotlinc.sh Scripts/diff_cases` 675 passed/0 failed/110 skipped で green。新規回帰ケース `Scripts/diff_cases/array_hof_noncapturing_closure.kt`（any/all/none/count/map/filter/forEach/reduce の非捕捉ラムダ）を追加し実 kotlinc と PASS 確認済み。
  - 削除 kk_*: `RuntimeCollectionHOFArray.swift` の 20 関数（rg で列挙）
- [ ] KSP-434: Grouping を Kotlin 化（`groupingBy`, `eachCount(To)`, `fold(To)`, `reduce(To)`, `aggregate(To)`）
  - 削除 kk_*: `RuntimeCollectionHOFGrouping.swift` の 11 関数 + `HeaderHelpers+SyntheticGroupingStubs.swift` の該当登録
- [ ] KSP-435: Iterable/Collection 汎用を Kotlin 化（`kk_iterable_*` 12 関数, `kk_collection_*` 6 関数）
  - ブリッジ残留: 型タグディスパッチが必要な `kk_collection_size` 等は `__kk_` 降格を検討（着手時に rg で分類し、分類根拠をタスク PR に記載）
- [ ] KSP-436: 可変操作の最小ブリッジを確定する（MutableList/Set/Map の `add`/`remove`/`clear`/`set`/`put` 系 33 関数）
  - 原則ブリッジ残留（ストレージ直接変異）: `kk_mutable_*` を `__kk_` へ一括改名し、`removeIf`/`retainAll`/`replaceAll`/`fill`/`addAll` 系など述語・複合系のみ Kotlin 化。`CallLowerer+MemberCallEmission.swift` の該当特例を Kotlin 宣言経由に置換

#### kotlin.sequences [M4 実行体]（KSP-441 が先頭。他は 441 完了後に並列可）

- [ ] KSP-441: Sequence 遅延 transform 基盤を Kotlin 化（`Sequence`/`Iterator` インターフェース + `map`, `filter` 系 transform）
  - 注意: object 式（匿名クラス）でパイプラインを表現する。コンパイラの object 式・ジェネリクス対応が不足していれば**ブロッカーとして報告し中断**
  - 対象 kk_*: `RuntimeSequence.swift` の transform 系（`kk_sequence_map*`, `kk_sequence_filter*`, `kk_sequence_withIndex`, `kk_sequence_flatMap*`, `kk_sequence_onEach*`, `kk_sequence_requireNoNulls` 等。rg で全列挙）
- [ ] KSP-442: Sequence terminal を Kotlin 化（`first*`, `last*`, `single*`, `elementAt*`, `find(Last)`, `contains`, `indexOf*`, `any`, `all`, `none`, `count`, `min*`, `max*`, `sum`, `average`）
  - 前提: KSP-441 / 既存 `SequenceAggregateHOF.kt` に追記
- [ ] KSP-443: Sequence 変換・集合演算を Kotlin 化（`toList`, `toMutableList`, `toSet`, `toMutableSet`, `toHashSet`, `toSortedSet`, `toCollection`, `toMap`, `flatten`, `unzip`, `union`, `intersect`, `subtract`, `plus*`, `minus`, `ifEmpty`, `constrainOnce`, `orEmpty`）
  - 注意: インライン `kotlinSequencesSource`（toList/toMutableList/toSet）と統合（KSP-503 と調整）
- [ ] KSP-444: Sequence association・minBy/maxBy を Kotlin 化（`associate*(To)`, `groupBy(To)`, `partition`, `joinTo(String)`, `sumOf/By(Double)`, `min/max(By/Of/With)(OrNull)`）
  - 削除 kk_*: `RuntimeSequenceAssociation.swift` の全関数（rg で列挙）
- [ ] KSP-445: Sequence fold/scan を Kotlin 化（`fold(Indexed)`, `reduce(Right)(Indexed)(OrNull)`, `scan(Indexed)`, `runningFold/Reduce(Indexed)`, `sorted*`）
  - 削除 kk_*: `RuntimeSequenceFoldScan.swift` の全関数
- [ ] KSP-446: Sequence `*To` 宛先変種を Kotlin 化（`filterTo` 等 11 関数、`RuntimeSequenceBuilders.swift` 内 STDLIB-SEQ-021 群）
- [ ] KSP-447: sequence{}/iterator{} ビルダーを (c) 残留分類として確定する
  - coroutine 機構と不可分（`kk_sequence_builder_*`, `kk_iterator_builder_*` 11 関数）。`__kk_` 降格 + `docs/stdlib-pipeline.md` §9 の (c) 表へ記載のみ。Kotlin 化はしない

#### kotlin.ranges [M6 実行体]（前提: KSP-312）

- [ ] KSP-451: Range プロパティ・membership を完遂（`first`, `last`, `start`, `endInclusive/Exclusive`, `count`, `isEmpty`, `contains`, `sum`, `reversed` の Int/Long/Char 版）
  - 削除 kk_*: `kk_range_first`, `kk_range_last`, `kk_range_start`, `kk_range_end`, `kk_range_endExclusive`, `kk_range_count`, `kk_range_isEmpty`, `kk_range_contains`, `kk_range_sum`, `kk_range_reversed`, `kk_long_range_*` 同系, `kk_char_range_isEmpty`
- [ ] KSP-452: for-in の range 特例を `.iterator()` 経路へ統一する
  - 変更: `ExprLowerer+ControlFlowAndBlocks.swift` の range 直接特例を、KSP-312 で配線した Kotlin `iterator()`（インライン展開）に置換。性能退行は diff_kotlinc + 簡易ベンチで確認し、退行時はループ最適化パスの課題として報告
  - 削除 kk_*: `kk_range_iterator`, `kk_range_hasNext`, `kk_range_next`, `kk_long_range_iterator`
- [ ] KSP-453: IntRange HOF を Kotlin 化（`RuntimeRangeIntRangeHOF.swift` の約 30 関数: `toList`, `forEach`, `map*`, `filter*`, `reduce*`, `fold*`, `find*`, `first/last(OrNull)(_predicate)`, `any`, `all`, `none`, `chunked`, `windowed`, `take`, `drop`, `average`, `sorted`, `toIntArray`）
  - 実装方針: `Iterable<Int>` の汎用 HOF へ委譲する形で個別 kk_* を不要化
- [ ] KSP-454: LongRange/CharRange HOF を Kotlin 化（`RuntimeRangeLongRange.swift` の HOF 群 + `kk_char_range_toList/forEach/take/drop/sorted`）
- [ ] KSP-455: UInt/ULong Range を Kotlin 化（`RuntimeRangeUIntULongRange.swift` の全 HOF/プロパティ約 80 関数）
  - 前提: 符号なし型のジェネリクス/演算対応を着手時に確認。困難なら (b)→(c) 再分類を提案して中断
- [ ] KSP-456: progression 構築系を Kotlin 化（`step`, `downTo`, `until`, `*_progression_fromClosedRange`）
  - 削除 kk_*: `kk_op_step`, `kk_op_downTo`, `kk_op_rangeUntil`, `kk_int/long/uint/ulong/char_progression_fromClosedRange` ほか（`kk_op_rangeTo` は演算子コアのため残留可）
- [ ] KSP-457: range random 系を Kotlin 化（前提: KSP-466。`kk_range_random*`, `kk_long_range_random*`, `kk_char_range_random*`, `kk_uint/ulong_range_random*`, `kk_random_nextInt/nextLong_rangeObject`）

#### kotlin.comparisons [M5 実行体]（前提: KSP-309）

- [ ] KSP-461: Comparator 群を完遂する（`nullsFirst/Last` 各種, `reversed`, multi-selector `compareBy`×3, `compareValues(By)`×6, `CASE_INSENSITIVE_ORDER`, primitive selector 版）
  - 削除 kk_*: `RuntimeComparator.swift` の残存全関数（trampoline 含む 53 − KSP-309 分。`rg -o '@_cdecl\("kk_(comparator|compareValues|comparable)[a-zA-Z_]*"\)' Sources/Runtime` で列挙）。比較コア `kk_comparable_compareTo` のみ `__kk_` 降格可

#### kotlin.random [M7 実行体]

- [x] KSP-467: SecureRandom 互換層を `__kk_` 降格する（`kk_secure_random_*` 4 関数） (PR #5027)
  - Kotlin 化: `Sources/CompilerCore/Stdlib/kotlin/random/SecureRandom.kt` に `SecureRandom` クラスと companion `getInstance()`、`setSeed`、`generateSeed`、`nextBytes` を追加。`@KsSymbolName` 経由で `__kk_secure_random_get_instance` / `__kk_secure_random_set_seed` / `__kk_secure_random_generate_seed` / `__kk_secure_random_next_bytes` ABI ブリッジを呼ぶ
  - クリーンアップ: `Sources/CompilerCore/Sema/DataFlow/HeaderHelpers+SyntheticRandomStubs.swift` から `SecureRandom` の synthetic class/companion/method 登録を除去
  - 検証: `swift build` / `bash Scripts/swift_test.sh --filter SmokeTests` / `--filter Golden` / `RuntimeABIExternalLinkValidationTests` / `RuntimeSecureRandomTests` pass。`Scripts/diff_cases/secure_random.kt` を `kswiftc` で実行確認

#### kotlin.time [M8 実行体]

- [~] KSP-472: Instant/Clock/measureTime のブリッジを確定する（2026-07-08、一部配線）
  - Kotlin 化済み: `kk_instant_epoch_seconds`, `kk_instant_nano_of_second`, `kk_instant_is_distant_past/future`, `kk_instant_plus/minus_duration`, `kk_instant_compare`, `kk_instant_until` を `__kk_instant_*` bridge（`HeaderHelpers+SyntheticInstantStubs.swift`）へ降格し、`Sources/CompilerCore/Stdlib/kotlin/time/Instant.kt` の拡張プロパティ/演算子/関数から呼ぶ形に配線。`elapsed()` はブリッジなしで `this.until(Instant.now())` として実装。`kk_timedvalue_value`/`kk_timedvalue_duration` も同様に `__kk_timedvalue_*` bridge 化し `Sources/CompilerCore/Stdlib/kotlin/time/TimedValue.kt` へ配線（`HeaderHelpers+SyntheticDurationStubs.swift`）
  - 副次修正: `HeaderHelpers+SyntheticClockStubs.swift` が `HeaderHelpers+SyntheticInstantStubs.swift` と同じ Instant companion/property/method を重複登録していたバグを解消（Clock 関連の登録のみに縮小、Instant symbol/type の再取得のみ残す）
  - 進捗（PR 作成中）: `kk_instant_now`, `kk_instant_from_epoch_millis`, `kk_clock_system_now` を Kotlin source 拡張化。`Sources/CompilerCore/Stdlib/kotlin/time/Instant.kt` に `Instant.Companion.now()` / `fromEpochMilliseconds()`、`Sources/CompilerCore/Stdlib/kotlin/time/Clock.kt` に `Clock.System.now()` を追加し、`@KsSymbolName` 経由で `kk_instant_now` / `kk_instant_from_epoch_millis` / `kk_clock_system_now` ABI ブリッジを呼ぶ。`HeaderHelpers+SyntheticInstantStubs.swift` / `HeaderHelpers+SyntheticClockStubs.swift` から companion/nested-object への直接メンバ登録を除去。`kk_clock_now` は `Clock` がユーザー実装可能な interface であり `now()` の member dispatch が必須（KSP-CAP-003 とは無関係、引き続き対象外）。`kk_measureTime`/`kk_measureTimedValue` は `CallLowerer+StdlibLoops.swift` の KIR 特殊インライン展開が `stdlibSpecialCallKind`（関数名ベース）でディスパッチしており、direct stub 名を変更すると壊れるため対象外。`kk_timedvalue_new` は `measureTimedValue` の KIR lowering からのみ呼ばれる内部コンストラクタで、Kotlin source から到達しないため対象外
  - 検証: `swift build` / `swift test --filter InstantDistantPropertiesSyntheticTests` / `RuntimeABIExternalLinkValidationTests` / `CompanionObjectTests` / `RuntimeInstantTests` / `CodegenBackendIntegrationTests --filter TimeEdgeCases` pass。`Scripts/diff_cases/instant_basic.kt` / `clock_basic.kt` および `Instant.now()` の追加ケースを `kswiftc` で実行確認

#### kotlin.uuid [M12 実行体]

- [~] KSP-507: kotlin.uuid.Uuid の実 API 未実装分を追加する（KSP-310 訂正のフォローアップ） — PR #5006 で実装・検証済み、マージ後 [x] 化
  - 対象: `mostSignificantBits`/`leastSignificantBits` を `public` から `@PublishedApi internal` へ変更（KSwiftK がバンドル stdlib とユーザーコード間のモジュール境界可視性を実際に強制するか未検証のため、まず spike で確認する）
  - 対象: `toULongs`, `toUByteArray`, `fromUByteArray`, `fromULongs`, `toHexDashString`, `generateV4()` を追加
  - 対象外（別途再調査してから着手）: `generateV7()` / `generateV7NonMonotonicAt()` は `kotlin.concurrent.atomics.AtomicLong` と `kotlin.time.Clock`/`Instant` に依存。Atomics サポートの有無が未確認、Clock/Instant も KSP-472 で部分配線のみのため、実現可能性を先に確認する
  - 前提: KSP-309（Comparators 配線、`naturalOrder`）が未着手のため、`naturalOrder<Uuid>()` を使う置き換え例は現状 `Uuid.compareTo()` 直接呼び出しで代替中
  - ブロッカー候補（2026-07-09 発見、要再調査）: `toLongs`/`toULongs` と同型の `inline fun <T> foo(action: (X, Y) -> T): T` パターンで、ラムダ本体が (a) 引数をそのまま返す恒等関数（`{ m, l -> m }`）→ `KSWIFTK-TYPE-0001: Conflicting bounds ... T is not a subtype of Long` で失敗、(b) `Uuid.fromLongs(m, l)` や `Pair(m, l)` のようなネストしたコンストラクタ/コンパニオン呼び出し → `KSWIFTK-SEMA-0002/0003` で失敗、(c) `Boolean` を直接返す（`{ m, _ -> (...).toInt() == 4 }`）→ コンパイル・実行は成功するが文字列補間時に `true`/`false` ではなく `1`/`0` を出力（プリミティブが `T` を経由してボックス化される際の unboxing 不整合と推測、[[primitive-autoboxing-mutable-collection-add]] や [[comparison-unboxing-peer-type]] と同系統）。`toLongs` 自体は `m + l` のような二項演算のみを返す形（ジェネリック呼び出しの外側で比較・再構築する）であれば安全に動作することを確認済み（`Scripts/diff_cases/uuid_basic.kt` 参照）。`toULongs`/`toUByteArray` 等を実装する際は同じ制約に当たる可能性が高く、Sema/TypeCheck の型変数解決（`CallTypeChecker` 系）と ABI Lowering のプリミティブ boxing 経路の両方を先に調査すること
[x] KSP-508: `ByteArray.getUuid`/`uuid`/`putUuid` を実 API（`ByteBuffer.getUuid`/`putUuid`）に置き換える（PR #5011 でマージ済み）
  - 問題: KSP-476（#4605）が実装した `kotlin.uuid.ByteArray.getUuid(offset)` / `ByteArray.uuid(at)` / `ByteArray.putUuid(at, uuid)` は実際の `kotlin.uuid` パッケージには存在しない。kotlinc 2.4.0 の `kotlin-stdlib-sources.jar`（`jvmMain/kotlin/uuid/UuidJVM.kt`）で裏取りした実 API は `java.nio.ByteBuffer` の拡張関数 `ByteBuffer.getUuid()` / `ByteBuffer.getUuid(index: Int)` / `ByteBuffer.putUuid(uuid): ByteBuffer` / `ByteBuffer.putUuid(index, uuid): ByteBuffer`（いずれも `@SinceKotlin("2.4")` `@WasExperimental(ExperimentalUuidApi::class)`）であり、`ByteArray` ではなく `ByteBuffer` がレシーバ。
  - 対応: (a) を実施。`java.nio.ByteBuffer` の bundled 実装を新規追加し、`Uuid.kt` の拡張を `ByteBuffer` レシーバに置き換えた。`Scripts/diff_cases/uuid_put_uuid.kt` から `SKIP-DIFF` を解除。
  - 検証: `swift build` / `swift test --filter Uuid` / `bash Scripts/swift_test.sh --filter SmokeTests` / `git diff --check` / `bash Scripts/validate_runtime_abi_links.sh` pass。`bash Scripts/diff_kotlinc.sh` はローカルに `kotlinc` が無いため未実行（CI diff ジョブで確認）。

#### kotlin.io [M 番号なし・新設]（棚卸し 2026-07-01: File I/O 58 / Base64 26 / HexFormat 16 の計 100 @_cdecl）

- [ ] KSP-484: File I/O の syscall 層を `__kk_` 降格する
  - 対象（ブリッジ残留・改名のみ）: 構築 `kk_file_new(_parent_child)`, 読み書き `kk_file_readText/readBytes/readLines/writeText/appendText/writeBytes/appendBytes`, 存在判定 `kk_file_exists/isFile/isDirectory/canRead/canWrite/canExecute/length/lastModified`, FS 操作 `kk_file_delete/mkdirs/createNewFile/listFiles`, 走査 `kk_file_walk(TopDown/BottomUp)`, `kk_file_tree_walk_sortedBy`, ストリーム/リソース/temp 系（`rg -o '@_cdecl\("kk_(file|files|io|classloader|resource|input_stream|output_stream)[a-zA-Z_]*"\)' Sources/Runtime/RuntimeFileIO.swift` で全列挙）
  - 公開 API 層（`forEachLine`/`useLines`/`readLines` のイテレーション・例外規約・デフォルト引数分岐）は Kotlin 化し、ブロック単位 I/O のみブリッジに残す
  - 手順: T / diff: `file_*.kt` 21 ケース（既存）全 green 維持

#### kotlin.text.Regex [M 番号なし・新設]（棚卸し 2026-07-01: 39 @_cdecl。正規表現エンジン = NSRegularExpression はブリッジ残留）

- [ ] KSP-486: MatchResult/MatchGroup 層を Kotlin 化する（純ロジック約 20 関数）
  - 削除 kk_*: `kk_match_result_value/groupValues/range/groups/component1/component2/next`, `kk_match_group_collection_get_at/get/size`, `kk_match_group_value/range`, `kk_match_result_destructured(_match)`, `kk_match_result_destructured_component1..9`, `kk_regex_pattern`, `kk_regex_options`, `kk_regex_group_names`（`RuntimeRegex.swift`）
  - 内部のマッチ位置データ取得のみ `__kk_` 最小ブリッジに残す / 手順: T / diff: `regex_named_groups.kt` ほか既存 10 ケース
- [ ] KSP-487: Regex 公開 API 層を Kotlin 化し、エンジンを `__kk_` 降格する
  - Kotlin 化: `String.toRegex`×3 / `matches` / `contains` / `replace(First)` / `split` のオーバーロード分岐・入力検証（下敷き: 死蔵 `Stdlib/kotlin/text/Regex.kt` はコメントアウト状態 — 実質新規実装）
  - `__kk_` 降格: `kk_regex_create(_with_option/_with_options)`, `kk_regex_from_literal`, `kk_regex_find(All)`, `kk_regex_matchEntire`, `kk_regex_matches`, `kk_regex_containsMatchIn`, `kk_regex_replace_lambda`, `kk_string_*_regex` 系エンジン呼び出し
  - 削除: `HeaderHelpers+SyntheticRegexStubs.swift` の該当登録 / 手順: T

#### kotlin.properties [M 番号なし・新設]（棚卸し 2026-07-01: `RuntimeDelegates.swift`。`by` 式は `StdlibDelegateLoweringPass` が call site を直接書き換える構造）

- [ ] KSP-491: Lazy / Delegates を Kotlin 化する
  - 下敷き: 死蔵 `Stdlib/kotlin/LazyDelegate.kt`, `properties/Properties.kt`, `properties/Delegates.kt`, `properties/ObservableProperty.kt` → `Sources/CompilerCore/Stdlib/kotlin/properties/` へ移設
  - Kotlin 化: `ReadOnlyProperty`/`ReadWriteProperty` インターフェース、`ObservableProperty`（beforeChange/afterChange）、`Delegates.observable/vetoable/notNull`、`lazy(mode)` の `NONE`/`PUBLICATION` モード
  - ブリッジ残留: `SYNCHRONIZED` モードのロックのみ `__kk_lazy_sync_*`（新設）
  - 変更: `Sources/CompilerCore/Lowering/StdlibDelegateLoweringPass.swift` の `kk_lazy_create`/`kk_observable_create`/`kk_vetoable_create`/`kk_notNull_create` 書き換え特例を、Kotlin 宣言の通常解決（`getValue`/`setValue` operator 規約）へ置換
  - 削除 kk_*: `kk_lazy_create/of/get_value/is_initialized`, `kk_observable_create/get_value/set_value`, `kk_vetoable_*` 3, `kk_notNull_*` 3（`RuntimeDelegates.swift`）/ `HeaderHelpers+SyntheticPropertyDelegateStubs.swift` の該当登録
  - 注意: operator 規約による delegate 解決がコンパイラ未対応なら**ブロッカーとして報告し中断** / diff: `delegate_lazy.kt`, `delegate_observable.kt`, `delegate_vetoable.kt`, `delegates_not_null.kt`（既存）/ 手順: T

#### kotlin.reflect [M 番号なし・新設]（棚卸し 2026-07-01: メタデータレジストリ依存のためブリッジ色が濃い）

- [~] KSP-496: KClass 公開 API 層を Kotlin 化し、メタデータレジストリを `__kk_` 降格する
  - 完了: 下敷き2ファイルを `Sources/CompilerCore/Stdlib/kotlin/reflect/KClassBasicAPI.kt`/`KClassMemberIntrospection.kt` へ移設・実配線（ルート `Stdlib/` の死蔵版は削除）。Kotlin 化: `simpleName`/`qualifiedName`/`isInstance`/真偽値フラグ12種（isFinal/isOpen/isAbstract/isData/isSealed/isValue/isEnum/isInterface/isObject/isInner/isCompanion/isFun。チケット記載は「11種」だが実装対象は`CompilerKnownNames.swift`基準の12種）/`visibility`/`annotations`。`__kk_` 降格: rg で全列挙した `kk_kclass_*`/`kk_type_token_*`/`kk_ktype_*`/`kk_ktypeprojection_*`/`kk_kfunction_*`/`kk_kparameter_*`/`kk_kconstructor_*`/`kk_annotation_*`（`RuntimeStringArray.swift`/`RuntimeReflection.swift`、計 ~65 個。`cast`/`safeCast` の実体もリネーム済み）をリネームし、`RuntimeABISpec` 側も追従（未登録だった `kk_kclass_nested_classes` の欠落も是正）。
  - 「全部対応して」指示を受けた追加調査で、残り3カテゴリの根本原因を深掘りし、うち2件を実際に修正した（詳細は下記）。全カテゴリの完全移行には至っていないが、根本原因の解像度は大きく上がっている。
  - **修正済みの根本原因バグ（3件、いずれも `Sources/CompilerCore/` 内）**:
    1. `String::class`/`Char::class`/`Any::class` のように、`HeaderHelpers.ensureClassSymbol` が member 宣言（CharSequence 適合など）を持たせるためだけに登録する `kotlin.<Name>` 偽装 `.class` シンボルが存在する組み込み型で、`T::class` のスコープ解決がこの偽装シンボルを builtin 名フォールバックより先に見つけてしまい、`classRefTargetType` が偽装 nominal 型（`.classType`）になる問題。これにより `String::class.isInstance("x")` が `RuntimeTypeCheckToken.encode` で `nominalBase` として誤符号化され、常に `false` を返していた（正しくは `stringBase`）。修正: `RuntimeTypeCheckToken.encode`（`Sources/CompilerCore/KIR/RuntimeTypeCheckToken.swift`）に `encodeBuiltinDisguisedNominal` を追加し、`.nominal` 分類時にこの偽装を検出して builtin base へフォールバックするようにした。回帰テスト `testBuiltinClassRefTokenMatchesPrimitiveBase`（`Tests/CompilerCoreTests/KIR/RuntimeTypeCheckTokenTests.swift`）追加済み。
    2. 上記と同根で、`TypeSystem.isSubtype` が偽装 nominal 型（例: `String` の `kotlin.String` 偽装シンボル）と canonical builtin 型（`.stringStruct` 等）を無関係な型として扱っていたため、`fun <T : Any> KClass<T>.cast(value: Any?): T = value as T` を `String::class.cast(v)` のように呼ぶと、ジェネリック制約ソルバが `KSWIFTK-TYPE-0001: Conflicting bounds for type variable`（`Class#N is not a subtype of Class#N & String`）を出して失敗していた。修正: `TypeSystem`（`Sources/CompilerCore/Sema/TypeSystem/TypeSystem.swift`）に `stringClassSymbol`/`charClassSymbol`/`anyClassSymbol` を追加（`HeaderHelpers+SyntheticStringStubs.swift`/`+SyntheticCharStubs.swift`/`HeaderHelpers.swift` の登録箇所で設定）し、`Subtyping.swift` の `isSubtype` 冒頭で `normalizeBuiltinDisguisedClassType` により両辺を正規化するようにした。`fun <T : Any> KClass<T>.myCast(value: Any?): T = value as T` を `String::class.myCast(v)`（期待型 `String`）から呼ぶケースで実際にコンパイル・実行成功を確認済み。
    3. （KClass 無関係の汎用バグ、上記調査中に副産物として発見）`fun <T> foo(value: Any?): T { return value as T }` のように非 reified 型パラメータへ `as T` する「unchecked cast」で、`RuntimeTypeCheckToken.encode` が `.typeParam` を `unknownBase`(=0) として符号化し、`kk_op_is` の `default: return 0` に落ちて **常に ClassCastException を投げていた**（本来 JVM 型消去と同じく無条件成功すべき）。`is T`（非 reified）は既に `KSWIFTK-SEMA-0084` でコンパイルエラーになるためこの土台は `as`/`as?` 経由でしか到達しないことを確認済み。修正: `ExprLowerer+ControlFlowAndBlocks.swift` の `.asCast` lowering で、ターゲット型が非 reified 型パラメータの場合はランタイム呼び出しを発行せず `.copy` 命令で値をそのまま通すようにした。
  - **未完了（根本原因は特定したが、このセッションでは着手を見送った）**:
    - `members`/`constructors`/`primaryConstructor`/`properties`/`memberProperties`/`declaredMemberProperties`/`functions`/`memberFunctions`/`declaredMemberFunctions`/`nestedClasses`/`supertypes`: `RuntimeKFunctionBox`/`RuntimeKPropertyStub`等のランタイムハンドルが真の interface 適合性チェック（`is`/`as`）に未対応な点は変わらず。ただし **実現可能な道筋は判明した**: `Sources/Runtime/RuntimeCollectionHOF.swift`/`RuntimeCollectionHelpers.swift` に既に `IndexedValue`/`Map.Entry` 用の前例があり、`runtimeRegisterObjectType(rawValue:classID:)` + `runtimeRegisterTypeEdge(childTypeID:parentTypeID:)`（`RuntimeHelpers.swift`）という汎用機構が「ランタイム専用オブジェクトに Kotlin 側の nominal 型 ID を後付けでタグ付けし、supertype グラフに登録する」ことを既に実現している。かつ `KFunction <: KCallable` 等の supertype 関係は `HeaderHelpers+SyntheticPropertyDelegateStubs.swift` の `addSyntheticDirectSupertypes` で **Sema レベルでは既に正しくモデル化済み**（コンパイル時の型システムは対応しているが、ランタイム側のオブジェクトタグ付けと、`KCallable.name` 等の共有メンバーの「実体の型に応じた分岐呼び出し」が未実装、というのが正確なギャップ）。次にやる場合: (1) 各リフレクション interface の nominal 型 ID を `runtimeStableNominalTypeID(fqName:)` で計算し起動時に一度登録 (2) `RuntimeKFunctionBox` 等の生成箇所を `registerRuntimeObject(_:typeID:)` 経由に変更 (3) `name`/`visibility`/`annotations` 等の共有メンバーを「複数のBox型を順に `tryCast` する」ディスパッチ関数に書き換え。対象メンバーの数が多く、後方互換の検証（既存 diff ケースには本挙動をカバーするものが無い）も含めると一定規模の別タスクとして切り出すのが妥当。
    - `cast`/`safeCast`: 上記の根本原因修正 (2) により「型変数の境界矛盾」自体は解消したが、Kotlin ソース化するには `fun <T : Any> KClass<T>.cast(value: Any?): T = __kk_kclass_cast(this, value) as T` のように **スロー可能な `__kk_kclass_cast`（`outThrown` 引数を持つ3引数 Swift 関数、`RuntimeABISpec` 上 `isThrowing` デフォルト `true`）を bundled Kotlin ソースから `@KsSymbolName` 経由で呼ぶ**必要がある。この形（stdlib ソースからの throwing external 呼び出し）の前例が現状の bundled Kotlin ソース全体に一件も無く、コンパイラの一般呼び出し lowering が `RuntimeABISpec.isThrowing` を正しく参照して `outThrown` 引数を自動挿入するかが未検証（`CallLowerer+MemberCallEmission.swift` の `throwingCallees` はメンバーコール専用のハンドキュレートされたリストで、`__kk_kclass_cast` は含まれていない）。誤ると ABI 不整合（引数個数不一致によるスタック破壊）のリスクがあるため、このセッションでは検証済みの安全な移行手段が確立できず見送った。次にやる場合はまず「throwing な `@KsSymbolName` external を bundled stdlib から呼ぶ」ことの可否を独立した最小ケースで検証すべき。
    - `findAnnotation`: reified 型引数を要求する点に加え、見つかったアノテーションのランタイム表現 `RuntimeAnnotationBox`（`Sources/Runtime/RuntimeTypes.swift`）が引数を汎用文字列配列としてしか保持しないため、`findAnnotation<A>(): A?` を精密な `A?` 型で返しても `A` の宣言プロパティへの実アクセスは機能しない（members/constructors と同根の「ランタイムハンドルが本物の Kotlin オブジェクトとして振る舞わない」問題）。現状の compiler 特例は正直に `Any?` を返しているため、`if (found != null)` のような存在確認以上の用途は元々サポートされていない。
    - `findAssociatedObject`: 単体では戻り値が `Any?`（`T` へのキャスト不要）かつ実体が `runtimeObjectRaw=` プレフィックス経由で本物のオブジェクトハンドルを返す設計のため、Kotlin ソース化自体は上記2つより低リスクに見えたが、**既に `HeaderHelpers` 側で reified・inline・`@ExperimentalAssociatedObjects` opt-in 要求を満たす専用の synthetic シンボルが登録済み**（`Tests/CompilerCoreTests/Sema/ReflectFindAssociatedObjectSyntheticTests.swift` で検証されている）であることが判明。Kotlin ソースへの置き換えはこの opt-in 強制や reified 型引数の意味論を含めて忠実に再現する必要があり、当初想定より複雑と判断してこのセッションでは見送った（着手しかけた変更は復元済み）。
  - **見つかったが対象外として別タスクに切り出したバグ（2件、いずれも KClass 無関係の汎用コンパイラバグ）**: (1) ジェネリック関数内の文/式に `@Suppress("UNCHECKED_CAST")` を付けると `KSWIFTK-TYPE-0001`/`KSWIFTK-SEMA-0022` 等の誤エラーが発生する（HEAD でも再現する既存バグ、確認済み）。(2) `inline fun <reified T>` の本体で発生した例外が呼び出し元の `try`/`catch` で捕捉されずクラッシュする（インライン展開と例外処理範囲の相互作用が疑われる）。
  - 副産物として発見・修正した既存バグ（他の bundled Kotlin 拡張全般に影響しうる）: (1) `kotlin.reflect` が `ScopeBuilder.swift` のデフォルトインポートパッケージ一覧に無く、`kotlin.reflect` 配下の拡張がスコープ解決で見つからなかった (2) `BundledDeclarationIndex.receiverOwnerFQName` が `.kClassType`（`T::class` 用の内部専用型表現）を未処理で、`KClass<...>` レシーバの拡張が優先規則の索引に正しく載っていなかった (3) `RuntimeABISpec+Operator.swift` の `__kk_kclass_find_associated_object` 登録に `isThrowing: false` が抜けており、実体（2引数、`outThrown` 無し）と齟齬していた。
  - diff: `kclass_basic.kt`, `reflect_kclass_ktype.kt`, `kclass_type_model.kt`, `type_reflection.kt`, `reflection_dynamic_call.kt` green（移行後も kotlinc と一致）。`kclass_members.kt`/`kclass_ktype_basic.kt`/`annotation_reflection.kt` は変更前から kotlinc 側が別理由（`kotlin.reflect.full` 未 import 等）で失敗しており未変更（git stash で移行前と同一エラーを確認済み）。

#### kotlin.coroutines / Flow / Channel [(c)/(b) 分類確定 + (b) 群のみ移行]（棚卸し 2026-07-01: スタブ 23 ファイル 10,849 行 / Runtime 7 ファイル 279 @_cdecl）

> 引き継ぎ注記(2026-07-10): 旧 `STDLIB-CORO-001`（`[~]` のまま 2026-07-07 #4582 で削除）の残課題は KSP-498/499 + KSP-674〜679 が正式に引き継ぐ。SharedFlow/StateFlow 等の細分は KSP-W6 の concurrent 節を参照。

- [ ] KSP-499: Flow オペレータ (b) 群を Kotlin 化する
  - 前提: KSP-498 + suspend fun を含む bundled Kotlin ソースのコンパイル対応を確認（未対応なら**ブロッカーとして報告し中断**）
  - **既知のブロッカー（2026-07-08 コード確認済み）**: `Sources/CompilerCore/Lowering/CoroutineLoweringPass+Flow.swift` の
    `lowerFlowExpressions` が `map`/`filter`/`take`/`toList`/`first`/`merge`/`zip`/`combine`/`flatMapConcat/Merge/Latest`/
    `debounce` 等を、Sema が解決した callee symbol を無視して「レシーバの flow provenance + 呼び出し名の文字列一致」だけで
    `kk_flow_*` へ構造的に書き換える（`FlowLoweringNames` 初期化コード参照）。bundled Kotlin 側にこれらの名前で実装を
    追加しても Lowering 段階で無条件に上書きされ呼ばれない。着手前に、このパスを対象外にする変更（同一 PR 必須）と
    ダミー実装差し替えテストでの再検証が必要。詳細: `docs/stdlib-pipeline.md` §9 KSP-498 セクション「Flow (b) 移行の前提条件」
  - 対象: `kk_flow_to_list`, `kk_flow_fold`, `kk_flow_first`, `kk_flow_merge`, `kk_flow_zip`, `kk_flow_combine` + per-element オペレータ（`RuntimeCoroutineFlow.swift` の 34 関数から (b) 分を rg で列挙）。`kk_flow_create/emit/collect` は (c) ブリッジ経由
  - diff: `flow_basic.kt`, `flow_builders.kt`, `flow_advanced_operators.kt` ほか既存 6 ケース / 手順: T
  - 🟡 **前提解決の根本修正を実施中（2026-07-08）**: 当初「着手不可」と判断したブロッカーのうち、以下の基盤修正が完了・実機検証済み。KSP-499 本体（実際の (b) 群 Kotlin 実装 + stub/cdecl/ABI 削除）はこれから着手。
    1. **Lowering が解決済みシンボルを無視して呼び出し名で書き換える問題 → 🟢 修正済み (PR #4988, 2026-07-22 更新)**: `FlowLoweringPass.swift` と `CoroutineLoweringPass+FlowInstructionRewrite.swift`（`rewriteFlowInstructions`）、さらに `CoroutineLoweringPass+Flow.swift`（`lowerFlowExpressions`）に、Sema が実宣言（`SemaModule.bundledIndex`、bundled Kotlin ソース由来）へ解決した呼び出しは構造的書き換えをスキップするガードを追加。加えて `CallTypeChecker+MemberCallInferenceCollectionFlow.swift`（`tryBuiltinFlowMemberCall` 呼び出し前）と `CallLowerer+MemberCalls.swift`（KIR 層の `transform`/`single` 特例）にも同様のガードを追加し、Sema・KIR・Lowering の3層すべてで「bundled/ユーザーの実宣言が解決済みシンボルとして存在するなら、ハードコードされた Flow intrinsic 特例より優先する」を実現。前提として `Flow`/`SharedFlow`/`StateFlow`/`MutableSharedFlow`/`MutableStateFlow` に実ジェネリクス型引数（`HeaderHelpers+SyntheticIterableRegistry.swift` の `Iterable<E>` と同パターン）を導入済み（従来は `ClassType.args: []` で要素型は side-table 管理のみだった）。実機再検証: 同じ実証手順（`suspend fun <T> Flow<T>.toList(): List<T> = listOf()` を **bundled** Stdlib 側（`BundledDeclarationIndex` はユーザーファイルでなく bundled ファイルのみを対象とするため要注意）に置いて `flowOf(1, 2, 3).toList()` を実行）で `RESULT=[]`・`MARKER_HIT=true` を確認 — ダミー実装が正しく優先されるようになった。副次発見: `.call`（トップレベル関数形式）は元々各分岐で `symbol == nil` を要求済みだったが、`.virtualCall`（`someFlow.map { }` という実際に最も使われるメンバー呼び出し構文）にはこのチェックが存在せず、新規追加が必要だった。検証: フル `swift_test.sh`（4385テスト、既知フレーキー1件除き green）・Golden 全件（295 Sema ケース含めゼロ diff）で確認。`diff_kotlinc.sh` 669 ケースは Stage 1（Bug A 単体）時点で green 確認済みだが、Stage 3（本ゲート追加後）の全件再確認は複数の並行セッションによるマシン競合のため未完了 — 次回セッションで低負荷時に再実行すること。
    2. **`kk_flow_fold`/`kk_flow_reduce`/`kk_flow_count` はコンパイラ側から到達不能**: Runtime の `@_cdecl` 実装・`RuntimeABISpec` エントリ・`Tests/RuntimeTests/RuntimeFlowTests.swift` の直接呼び出しテストは存在するが、Sema/KIR/Lowering のどこにもこれらを emit するコードが無い（`rg 'kk_flow_fold|kk_flow_reduce|kk_flow_count' Sources/CompilerCore` が 0 件）。つまり現状 `Flow<T>.fold(...)` は `.kt` から呼び出す経路が存在せず、「既存ネイティブ挙動を Kotlin に置き換える」という前提自体が `fold` には当てはまらない。既存 diff 6 ケースもこれらを使用していない。
    3. **ジェネリック高階関数のラムダ内演算子/メンバ解決バグ（suspend 有無に関係なく再現）** — 🟢 **演算子部分は修正済み（2026-07-08）**: 根本原因は `CallTypeChecker+LambdaReturnTypeOverload.swift` の `lambdaLiteralExpectedType()` がラムダ引数の期待型を計算する際、呼び出し先の非ラムダ引数から既に推論済みの型引数代入（例 `f(41) { ... }` の `T=Int`）を適用せず、未代入の `TypeParamType` をそのままラムダ引数の型にしていたこと。対処: `Resolution.swift` に `OverloadResolver.probeArgumentTypeSubstitution(...)`（非ラムダ引数だけから型変数代入を導出する軽量プローブ、`decomposeSubtypeConstraint`/`ConstraintSolver` を流用）を追加し、`CallTypeChecker+LambdaReturnTypeOverload.swift` に `applyInferredArgumentTypeArgs()`（`applyExplicitTypeArgs`/`applyReceiverClassTypeArgs` に続く第3の代入パス）を追加して配線。`f(41) { v: Int -> v + 1 }` は修正済みで動作を確認。AST 側のラムダ引数明示型注釈の保持（`skipTypeAnnotationIfPresent` が捨てている）は見送り — 本修正で repro は解決するため不要と判断。
       - 🟡 **残存する別問題（未修正、意図的に見送り）**: メンバー関数呼び出し（`.uppercase()`, `.length`, ユーザー定義クラスの任意メソッド等 — Int/Long 等プリミティブの特例ディスパッチ経路を通らないもの）は、ジェネリック HOF のラムダ本体内では `KSWIFTK-SEMA-0002: No viable overload found for call.` で依然失敗する。根本原因を特定済み: `Resolution.swift` の `evaluateCandidate` が、ラムダ本体の呼び出しに対して「外側の未束縛な型パラメータ（例 `R`）」を `expectedType` としてそのまま渡し、`signature.returnType <: R` を通常の（フリーではない）制約として追加するため、`describe(): String` のような具体型を返す候補が「`String` は `R` の部分型ではない」として誤って `.rejected` される。`inferLambdaLiteralExpr`（`ExprTypeChecker+NameLambdaAndCallableRefInference.swift`）には既に同種の「期待戻り値型が無制約の型パラメータなら制約を課さない」ガードが存在するが、`evaluateCandidate` 側には無い。**一度この同じガードを `evaluateCandidate` に追加して検証したが、`Int.plus(1)` のような別の正常ケースを壊し（誤って文字列関連の codegen 経路に迷い込み無限に近い遅延を起こす）、安全に一般化できないことを確認済みのため revert 済み**。真の修正には「`expectedType` の無制約型パラメータが REALLY 無関係な外側スコープのものか」をもっと正確に判定する必要があり、本タスクの想定規模を超える別問題として切り出す。KSP-499 の Kotlin 実装（Stage 4-6）では、アキュムレータ関数は `operation(acc, v)` のような関数値呼び出し（この問題の対象外）で書き、生成コード内でジェネリック型 T 自体のメンバー関数呼び出しは極力避ける設計とする。
    4. **bundled Stdlib に `suspend` の前例が無い** — 🟢 **検証済み（2026-07-08）**: `Sources/CompilerCore/Stdlib/kotlinx/coroutines/flow/` に一時ファイルを置いて実機確認。bundled ソースでの `suspend fun`（ジェネリック含む）は正常にコンパイル・実行できる（`suspend fun <T, R> f(v: T, g: suspend (T) -> R): R` 相当が動作）。前例が無かっただけで、経路自体に問題は無い。
    - 対応が必要な前段作業（いずれも本タスクの想定規模を超える別タスク相当・進行中）: (a) 上記 Lowering パスを「bundled/実体宣言優先」に改修 — **完了 (PR #4988)** (b) ジェネリック高階関数のラムダ内演算子/メンバ解決バグの修正 — 演算子部分は修正済み、メンバー呼び出し部分は既知の限界として残存 (c) KSP-498 の正式な分類表作成 — 未着手。

### KSP-W5: 後始末（W3/W4 の対応タスク完了後）

- [ ] KSP-501: `BundledKotlinStdlib.kotlinCollectionsSource` を .kt 化する（`count`/`any`/`all`/`none`/`sumOf`/`maxByOrNull`/`minByOrNull` → `collections/ListAggregateHOF.kt` へ移設。live ツリーとの重複なしは 2026-07-01 に確認済み）
- [x] KSP-502: `kotlinTextSource` を .kt 化する（`repeat`/`reversed`/`padStart`/`padEnd`/`encodeToByteArray`×3/`decodeToString`×4/`indent`×2 → `text/` 配下へ。**注意**: `trimIndent`/`trimMargin`/`prependIndent`/`replaceIndent`/`replaceIndentByMargin` は KSP-302 で処理済みのはず — 残っていれば重複させず統合）
  - 完了確認（2026-07-30、KSP-416 完了作業のついでに検証）: `repeat`/`reversed`/`padStart`/`padEnd` は `Stdlib/kotlin/text/StringBasics.kt`、`toByteArray`/`encodeToByteArray`×3/`decodeToString`×4/`Charsets` は `Stdlib/kotlin/text/StringEncoding.kt`、`indent`×2（+ KSP-302 分の `trimIndent`/`trimMargin`/`prependIndent`/`replaceIndent`/`replaceIndentByMargin`）は `Stdlib/kotlin/text/StringIndentFormat.kt` に実ロジックとして存在し、`BundledKotlinStdlib.kotlinTextSource` は空文字列、`excludedBundledStdlibFiles` にも非登録であることを確認済み
- [ ] KSP-503: `kotlinSequencesSource`/`kotlinTimeSource` を .kt 化し、`BundledKotlinStdlib.swift` と `FrontendPhases.swift` の `residualSources` 注入を削除する
  - 完了: `rg 'BundledKotlinStdlib' Sources` 0 件 + G
- [ ] KSP-504: ルート `Stdlib/` 死蔵ツリー（35 ファイル）を整理する
  - 手順: (1) `Package.swift` の `resources: [.copy("Stdlib")]` が `Sources/CompilerCore/Stdlib` を指すこと（ルートではない）を確認 (2) 各 .kt を「対応 KSP タスクの下敷きに使う / 即削除」に分類（W3/W4 の該当タスクへ移設済みのものから削除） (3) `git rm -r Stdlib/`
  - 完了: ルート `Stdlib/` が存在しない + G
- [ ] KSP-505: `excludedBundledStdlibFiles` 機構を撤廃し、ファイル名を本家準拠へリネームする
  - 前提: W3 全完了。手順: (1) セットが空であることを確認して機構ごと削除 (2) `text/Strings.kt`, `collections/Collections.kt` 等 kotlin-stdlib 本家のファイル構成へ統合リネーム（`docs/stdlib-pipeline.md` §6） (3) U で golden 更新

### KSP-W6: 追補モジュール移行（ギャップ監査 2026-07-10。手順は全て T。粒度ルール適用済み = 1タスク1PR）

> 2026-07-10 監査で判明した「(b) 分類なのに KSP タスクが無い」領域 + (c) 再分類監査（厳格原則: Swift 残留は言語コア/GC・continuation・メタデータ/OS syscall のみ）で b-reclass になった領域の実行体。各タスクの削除対象・特例位置は監査時点で実コード検証済み — 着手時は rg で再固定する。

#### コア util（kotlin 直下）

- [ ] KSP-601: let/also/takeIf/takeUnless を Kotlin 化する（非レシーバ形ラムダのみで CAP 不要。実装先 `kotlin/Standard.kt` 新設。kk_* ゼロ（全インライン特例）— 削除対象は `HeaderHelpers+SyntheticScopeFunctionStubs.swift` の該当登録と `ScopeFunctionKind` 該当分岐。インライン特例の撤去可否は KSP-INF-007 の実測とセットで判断）
- [ ] KSP-602: run/with/apply を Kotlin 化する（前提: KSP-CAP-008。`apply` は**スタブ未登録・名前特例のみ**で動作中のため宣言を新設。削除対象は同スタブの with/run 登録 + `CallLowerer+ScopeFunctionLowering.swift` の該当分岐）
- [ ] KSP-603: context/contextOf を Kotlin 化する（experimental。同スタブの context×arity1-6 / contextOf 登録。diff: `context`/`contextOf` ケース新規追加）
- [ ] KSP-604: repeat を .kt 化する（`kotlin/Standard.kt`。kk_* ゼロ・Sema 特例は RF-SEMA-002 でメタデータ駆動化済み。`HeaderHelpers+SyntheticStdlibLoopStubs.swift`（93行）削除 + `CallLowerer+StdlibLoops.swift` のインライン展開特例の維持/撤去を実測判断）
- [ ] KSP-605: require/check/error を Kotlin 化する（削除 kk_*: `kk_require`, `kk_require_lazy`, `kk_check`, `kk_check_lazy`, `kk_error`。実装先 `kotlin/Preconditions.kt`。前提確認: `ContractNonNullEffect` の smart-cast 効果を bundled 宣言でも維持できること（不可なら新規 CAP 起票）。注意: `RuntimePreconditions.swift` は TODO()/synchronized と同居 — 巻き込み禁止）
- [ ] KSP-606: requireNotNull/checkNotNull を**新規実装**する（Sema 未登録の欠落 API。孤児 runtime `kk_check_not_null(_lazy)`/`kk_require_not_null(_lazy)` 4件は削除。diff: 新規ケース追加）
- [ ] KSP-607: assert を Kotlin 化する（削除 kk_*: `kk_precondition_assert`, `kk_precondition_assert_lazy`。残留: `__kk_assertions_enabled`（新設）のみ）
- [ ] KSP-608: Pair/Triple クラス本体を Kotlin 化する（削除 kk_* 9: `kk_pair_first/second/to_string/toList` + `kk_triple_first/second/third/to_string/toList`。残留: `__kk_pair_new`/`__kk_triple_new`。実装先 `kotlin/Tuples.kt`）
- [ ] KSP-609: `to` infix を Kotlin 化し name-string 特例を全廃する（前提: KSP-608。特例削除: `CallTypeChecker+MemberCallInferenceRegularPrimitiveSpecials.swift` の Any.to 特例 / `+MemberCallInferenceRegularNoCandidateFallbacks.swift` の二重実装 / `CollectionLiteralLoweringPass+CallRewriteFactories.swift` の to→kk_pair_new・Triple ctor 書き換え / `CallLowerer+MemberCallSupport.swift` の "to" エントリ。diff: `to` 単独ケース追加）
- [ ] KSP-610: KotlinVersion を Kotlin 化する（kk_* 9 → 残留 `__kk_kotlin_version_current`（ビルド時定数注入）のみ。着手時に runtime 現在値 (2,3,20) と CLAUDE.md の 2.3.10 の食い違いを確認・是正。diff 新規）
- [ ] KSP-611: Closeable/AutoCloseable/use を Kotlin 化する（実装先 `kotlin/io/Closeable.kt`。残留 `__kk_auto_closeable_create` のみ。use の try-finally インライン特例（`CallLowerer+ScopeFunctionLowering.swift`）は当面維持し、撤去は KSP-601 と同判断）
- [ ] KSP-612: DeepRecursiveFunction を Kotlin 化する（**Sema 特例整理が主眼**: CallTypeChecker 4箇所（うち1組は重複疑い）+ HOFAdapter/ClosureAdapters の2箇所。ブリッジ4関数は全部 `__kk_` 残留 — トランポリンが存在意義。runtime の fatalError 4箇所の catch 可能化は DEBT-RT 系タスクへ。diff 新規）
- [ ] KSP-613: runCatching 残存特例を撤去する（KSP-304 完了後も残る名前特例を通常解決へ。着手時 `rg runCatching Sources/CompilerCore --type swift` で全列挙して固定）

#### io / system

- [ ] KSP-614: print/println を Kotlin 化する（削除 kk_* 4: `kk_println_newline`, `kk_println_any`, `kk_print_noarg`, `kk_print_any` → 残留 `__kk_print_raw` 1本に統合。改行付与・toString は Kotlin 側。特例削除: CallTypeChecker の println 即断 / CollectionLiteralLoweringPass+LookupTables / OperatorLoweringPass / EnumNameAccessLoweringPass — 後2者は挙動保持の代替設計必須。実装先 `kotlin/io/Console.kt`）
- [ ] KSP-615: readLine/readln/readlnOrNull を Kotlin 化する（削除 kk_* 3 → 残留 `__kk_readline_raw`。null 許容/例外分岐は Kotlin 側）
- [ ] KSP-616: TODO() を Kotlin 化する（削除 kk_* 2: `kk_todo`, `kk_todo_noarg`。ブリッジ不要 — NotImplementedError へ委譲。KSP-605 と runtime 同居ファイル注意）
- [ ] KSP-617: exitProcess/getTime 系を `__kk_` 降格する（`kk_system_exitProcess`, `kk_system_getTimeMillis/Micros/Nanos`, `kk_system_currentTimeMillis`, `kk_system_nanoTime`, `kk_system_process_start_nanos` — OS 窓口の改名 + 公開層 .kt 化。**併せて到達不能デッドコード `kk_system_measureTimeMillis/measureTimeMicros/measureNanoTime` 3関数を削除**（`StdlibSpecialCallKind` 優先で構造的に呼ばれない）。diff: getTimeMillis 系追加）
- [ ] KSP-618: synchronized を整理する（`kk_synchronized` → `__kk_synchronized` 降格 + 公開層 Kotlin 化。diff: synchronized ケース新規）
- [ ] KSP-619: kotlin.io 例外を Kotlin 化する（`FileSystemException` 基底を前倒し実装 + `AccessDeniedException`/`FileAlreadyExistsException`。ブリッジ残留ゼロ・特例ゼロ。**併せて BUG-016（RuntimeFileIO が型でなくメッセージ文字列の汎用 Throwable を送出 — 型付き catch 不成立の疑い）を実クラス送出へ修正**。実装先 `kotlin/io/FileSystemException.kt`。diff: 型付き catch ケース新規）

#### collections

- [ ] KSP-620: joinToString/joinTo の List/Array 版を統一する（孤児 `kk_string_joinToString` の正式タスク第1弾。bundled `StringSplitJoin.kt` の `List<T>.joinToString` と合成スタブの二重定義解消 — 前提: KSP-INF-011 のガード漏れ修正。削除 kk_*: `kk_list_joinToString`, `kk_array_joinToString`。残留 `__kk_string_joinToString`。**併せて呼び出し元ゼロの `CallLowerer+CollectionStdlibMemberCalls.swift` をファイルごと削除**）
- [ ] KSP-621: joinToString/joinTo の Iterable/Sequence 版を統一する（前提: KSP-620。削除 kk_* 4: `kk_iterable_joinTo`, `kk_iterable_joinToString`, `kk_sequence_joinTo`, `kk_sequence_joinToString` + `CallLowerer+UnresolvedMemberCalls.swift` の収束特例。diff: Iterable 版・joinTo 単独ケース追加）
- [ ] KSP-622: buildList を Kotlin 化する（前提: KSP-CAP-008。下敷き: 死蔵 `Stdlib/kotlin/collections/CollectionBuilders.kt` 実装済み。削除 kk_*: `kk_builder_list_add`, `kk_builder_list_addAll` → 残留 `__kk_build_list(_with_capacity)`。特例: `CollectionLiteralLoweringPass+CallRewriteFactories.swift` の build* 書き換え該当分岐）
- [ ] KSP-623: buildSet/buildMap を Kotlin 化する（前提: KSP-622。削除 kk_*: `kk_builder_set_add`, `kk_builder_set_addAll`, `kk_builder_map_put` → 残留 `__kk_build_set`/`__kk_build_map`）
- [ ] KSP-624: buildString を Kotlin 化する（前提: KSP-622, KSP-311。`builderDSLKind`（`CallTypeChecker+BuilderDSL.swift`）の該当分岐 + `CallLowerer.swift` の append 引数ボクシング特例撤去。`kk_build_string` 系 4 → `__kk_` 降格 or StringBuilder 経由化）
- [ ] KSP-625: ArrayDeque を Kotlin 化する（削除 kk_*: `kk_arraydeque_first/last/isEmpty/toString` は Kotlin 化、`kk_arraydeque_new/addFirst/addLast/removeFirst/removeLast/size` + get は `__kk_arraydeque_*` 最小残留（リングバッファ直変異）。特例: `isArrayDequeLikeType`（CallLowerer+ReceiverTypePredicates）ほか4箇所。実装先 `collections/ArrayDeque.kt`）
- [ ] KSP-626: IndexedValue/forEachIndexed を Kotlin 化する（IndexedValue を data class 化（現状 `kk_pair_first/second` 流用）+ `kk_list_forEachIndexed`/`kk_list_withIndex` 削除。diff: forEachIndexed/withIndex ケース追加）
- [ ] KSP-627: コレクション typealias を .kt 化する（`ArrayList`/`HashSet`/`HashMap`/`LinkedHashMap` typealias 4 + `LinkedHashSet` 具象クラス。**typealias のパーサ/Sema 対応は確認済み** — `HeaderHelpers+SyntheticCollectionTypeAliases.swift`（272行）を宣言数行に置換する最小工数タスク。残留ゼロ（LinkedHashSet ctor は `__kk_emptySet`/`__kk_iterable_toMutableSet` 流用））
- [ ] KSP-628: List→配列変換（object+signed 9）を Kotlin 化する（`toTypedArray`/`toCharArray`/`toBooleanArray`/`toShortArray`/`toDoubleArray`/`toFloatArray`/`toIntArray`/`toLongArray`/`toByteArray` = `kk_list_to*Array` 9 削除 → 残留は各配列型のアロケーションコアのみ。特例: `unresolvedCollectionMemberNames` ほか）
- [ ] KSP-629: List→配列変換（unsigned 4）を Kotlin 化する（前提: KSP-628。`kk_list_toUByteArray/toUShortArray/toUIntArray/toULongArray` 削除）
- [ ] KSP-630: Iterator.forEach/withIndex を**新規実装**する（実装も計画も無かった真空地帯。参照実装: `kk_list_forEach`（RuntimeCollectionHOF）/ IndexedValue。実装先 `collections/Iterators.kt`。diff 新規）
- [ ] KSP-631: Iterator.asSequence を**新規実装**する（前提: KSP-CAP-001/002 + KSP-441。参照: `kk_iterable_asSequence`）
- [ ] KSP-632: IterableRegistry 残余の HOF を Kotlin 化する（KSP-435 対象外の登録分: `reduceRight(Indexed)`/`sumBy(Double)`/`plusElement`/`minusElement`/`minus`。着手時 `rg 'registerIterable' Sources/CompilerCore/Sema/DataFlow/HeaderHelpers+SyntheticIterableRegistry.swift` で固定）
- [ ] KSP-633: IterableRegistry の殻を .kt 化する（`MutableIterable`/`AbstractCollection`/`AbstractMutableCollection` interface/abstract class 宣言）
- [ ] KSP-634: maxWith/minWith を KSP-461 の明示対象に追記する（現状 rg パターン包含の推定のみ — KSP-461 のタスク文へ明示列挙を追加する編集タスク）

#### math / numbers

- [ ] KSP-635: kotlin.math の純ロジック群を Kotlin 化する（abs/sign/min/max + PI/E 定数 — **名前特例ゼロで最もクリーンな移行対象**。実装先 `kotlin/math/Math.kt` 新設。対象 kk_*: `kk_math_abs*` 4 / `kk_math_sign*` 4 / `kk_math_min*`・`kk_math_max*` 各6 / `kk_math_PI`/`kk_math_E`。diff は math_* 15ケース既存）
- [ ] KSP-636: kotlin.math の丸め系を Kotlin 化する（ceil/floor/round/truncate/withSign — Foundation 依存の有無を着手時確認し、純ロジック化できない分のみ `__kk_` 降格。対象 kk_* 約12）
- [ ] KSP-637: kotlin.math の超越関数を `__kk_math_*` 降格する（sin/cos/tan/asin/acos/atan/atan2/exp/expm1/ln/ln1p/log/log2/log10/sinh/cosh/tanh/acosh/asinh/atanh/cbrt/hypot/pow/sqrt/IEEErem/nextTowards の Double/Float 版 — libm 窓口の改名 + 公開層 .kt 化。約52 kk_*。`rg -o '@_cdecl\("kk_math_[a-zA-Z0-9_]*"\)' Sources/Runtime` で着手時固定）
- [ ] KSP-638: roundToInt/roundToLong/ulp/nextUp/nextDown を整理する（**`HeaderHelpers+SyntheticMathStubs.swift` と `+SyntheticCoercionStubs.swift` の二重登録を一本化**した上で `__kk_` 降格。kk_* 10）
- [ ] KSP-639: coerce の Int/Long/Double/Float を本物の Kotlin 実装にする（**`ranges/RangeCoercion.kt` の `= this` フェイク12関数を実装で置換**。3並列 KIR 特例（`CallLowerer+PrimitiveMemberCalls.swift` / `+SafeMemberCalls.swift` / `+LegacyMemberLikeCalls.swift` の coerce 分岐）+ Sema 特例（`CallTypeChecker+MemberCallInferenceRegularPrimitiveSpecials.swift` — コメントの Byte/Short 記載とコードの乖離も解消）を撤去。runtime `RuntimeNumericBitManip.swift` の coerce 24関数削除。残留ゼロ見込み）
- [ ] KSP-640: coerce の unsigned 4型を Kotlin 化する（前提: KSP-639。スタブ残存の `kk_ubyte/ushort/uint/ulong_coerceIn/coerceAtLeast/coerceAtMost` 12 + range 版 `kk_int/long_coerceIn` 2 を削除）
- [ ] KSP-641: coerce の Comparable 総称版・ClosedFloatingPointRange range 版を**新規実装**する（本家 API `T.coerceIn(min?, max?)` / `coerceIn(ClosedFloatingPointRange)` が現状全く存在しない。前提: KSP-639, KSP-652）
- [ ] KSP-642: rotateLeft/rotateRight を Kotlin 化する（Int/Long 版 kk_* 4 削除。shl/shr/or で完結・残留ゼロ。diff: rotate ケース新規）
- [ ] KSP-643: countOneBits/countLeadingZeroBits/countTrailingZeroBits を Kotlin 化する（**BUG-015 修正込み: Long 版は Sema 通過後に KIR で握りつぶされる壊れたパス** — Kotlin 実装で Int/Long 両対応に。Int 版 kk_* 3 削除。diff: count 系新規）
- [ ] KSP-644: takeHighestOneBit/takeLowestOneBit/highestOneBit/lowestOneBit を Kotlin 化する（Int/Long 版 kk_* 8 削除・残留ゼロ）
- [x] KSP-645: kotlin.experimental の Byte/Short 版 and/or/xor/inv を**新規実装**する（本家仕様は Byte/Short 用。現行の Int 版登録（`HeaderHelpers+SyntheticExperimentalBitwiseStubs.swift`）は汎用 Int/Long 特例が先に解決する完全デッドコードのため削除 — CLEANUP-STUB-101 と重複しない側で実施）。**完了（2026-08-06、Kotlin ソース化は実施せず現状維持で確定）**: (1) 削除対象の `HeaderHelpers+SyntheticExperimentalBitwiseStubs.swift` は CLEANUP-STUB-101（#5038）で既に削除済みで、作業ツリーにも `HEAD` にも存在しない（`kk_bitwise_and/or/xor`・`kk_op_inv` は Int/Long/UInt/ULong/UByte/UShort の汎用 bitwise 経路が共用しているため削除対象外）。(2) **Kotlin ソースへの移行は現行の型モデルでは実装不能**であることを確認した。KSwiftK の `PrimitiveType`（`Sources/CompilerCore/Sema/TypeSystem/TypeModels.swift`）には signed の byte/short が存在せず、`Byte`/`Short` の型注釈は `resolveBuiltinTypeName`（`Sema/TypeCheck/Helpers.swift`）・`Sema/DataFlow/Analysis.swift`・`CompilerKnownNames.swift` のいずれでも `types.intType` へ解決される（`toByte()`/`toShort()` の戻り型も `intType`）。このため `Byte.and(Byte): Byte` と `Short.and(Short): Short` を同一パッケージに宣言すると同一シグネチャと判定され `KSWIFTK-SEMA-0001`（Duplicate JVM-erased callable declaration、`HeaderHelpers+JVMErasedCallableConflict.swift`）になる。片方（Byte 版のみ）に絞って `Stdlib/kotlin/experimental/BitwiseOperations.kt` を実際に追加して実機検証したところ、`CallTypeChecker+MemberCallInferenceRegularPrimitiveSpecials.swift` の Int 特例が先に解決するため宣言は完全デッドコードになり（`0x1234 and 0xFF00` は 4608 のまま = Kotlin 実装の `.toByte()` 切り詰めは適用されない）、逆に特例側を外して Kotlin 実装へ寄せると Byte ≡ Int である以上 Int の bitwise が 8bit へ切り詰められて壊れる。つまり本タスクの前提（Byte/Short が独立した型として存在する）が成立していない。(3) 一方で**現状の挙動自体は本家 kotlinc と完全一致**しており機能欠落はない。新規 diff ケース `Scripts/diff_cases/byte_short_bitwise_basic.kt`（infix/メソッド両形式・`-1`/`Byte.MIN_VALUE`/`Byte.MAX_VALUE`/`Short` 境界値・`256.toByte()` 等の切り詰め入力を含む 40 行）を追加し `bash Scripts/diff_kotlinc.sh` で PASS を確認。回帰テスト `Tests/CompilerCoreTests/Sema/ExperimentalBitwiseFunctionTests.swift` で解決経路・結果型・「Int の bitwise が切り詰められないこと」を固定した（Byte/Short を独立プリミティブ化する際はここが最初に落ちる）。真の Kotlin ソース化は型システムへの byte/short プリミティブ追加（リテラル型付け・算術昇格・narrowing・ABI/codegen・boxing・配列要素型まで波及）が前提で、その必要性を示す独立したバグとして BUG-183 を起票した
- [ ] KSP-646: isNaN/isFinite/isInfinite を Kotlin 化する（IEEE754 ビットパターン判定。kk_* 6 削除。diff: isNaN/isFinite ケース新規 — 現状カバレッジ薄）
- [ ] KSP-647: toBits/toRawBits/fromBits を `__kk_` 降格する（ビットパターン変換窓口。kk_* 6 + fromBits トップレベル登録。nextTowards は KSP-637 側）

#### time / sequences / ranges

- [ ] KSP-648: TimeMark/ComparableTimeMark を Kotlin 化する（削除 kk_*: `kk_time_mark_elapsed_now/has_passed_now/has_not_passed_now/plus_duration/minus_duration/minus_mark/compare` 7 — Duration 演算主体。実装先 `kotlin/time/TimeMark.kt`。diff 新規）
- [ ] KSP-649: TimeSource/Monotonic を Kotlin 化し ValueTimeMark を**新規実装**する（残留: `__kk_time_source_mark_now`/`__kk_time_source_monotonic_mark_now`/`__kk_time_source_as_clock`（単調クロック読み）。**本家 `TimeSource.Monotonic.ValueTimeMark`（value class）はリポジトリに存在しない — 新規追加**。前提: value class 対応の確認 + KSP-648）
- [ ] KSP-650: TestTimeSource/AbstractLongTimeSource/AbstractDoubleTimeSource を Kotlin 化する（削除 kk_*: `kk_test_time_source_new/plus_assign/mark_now/read` 4。前提: KSP-648）
- [ ] KSP-651: SequenceFactories を移設する（死蔵 `Stdlib/kotlin/sequences/SequenceFactories.kt`（112行・sequence{} ビルダー込み・完成度高）を bundled ツリーへ。kk_*: `kk_sequence_of`/`kk_empty_sequence`/`kk_sequence_generate(_noarg)` 4 → `__kk_` 降格 + Sema 未登録の孤立 `kk_sequence_of_single` の生死判定。特例: CallTypeChecker 6箇所（二重実装疑い含む）+ CollectionLiteralLoweringPass 3箇所 + `CallLowerer.swift` の generateSequence ハードコード撤去。前提: KSP-CAP-001（sequence{} 部分）。KSP-441 と同時か直後）
- [ ] KSP-652: ClosedRange<T>/ClosedFloatingPointRange/OpenEndRange の interface を .kt 化する（`HeaderHelpers+SyntheticRangeInterfaceStubs.swift`（382行）+ `+SyntheticRangeProgressionStubs.swift` の OpenEndRange 登録。kk_* ゼロ・残留ゼロの純宣言。具象 IntRange 等の conformance 配線は KSP-451 と同一 PR か後続で調整）

#### 例外・言語コア表面（(c) 再監査 2026-07-10 で b-reclass 確定分）

- [x] KSP-653: Throwable 本体とコンストラクタ群を Kotlin 化する（クラス階層宣言 + 0/1/2引数 ctor。残留: `__kk_throwable_new(_with_cause)`（GC 確保の1行ブリッジ）。注意: `kk_is_cancellation_exception` は coroutine 側 (c) で対象外）
- [ ] KSP-654: Throwable メンバを Kotlin 化する（message/cause getter・initCause・addSuppressed/getSuppressed/suppressedExceptions。前提: KSP-653。残留: `__kk_throwable_setCause`/`__kk_throwable_appendSuppressed`/`__kk_throwable_suppressedRaw`）
- [ ] KSP-655: stackTraceToString/printStackTrace を Kotlin 化する（前提: KSP-654 + runtime の renderedMessage を「生フレーム取得（`__kk_throwable_rawStackFrames` 新設）」と「整形（Kotlin 側で cause/suppressed チェーンを辿る）」に分離するリファクタ）
- [ ] KSP-656: 例外サブクラス階層の宣言を .kt 化する（IllegalArgumentException 等 `registerSyntheticExceptionConstructor` ループの置換。前提: KSP-653）
- [x] KSP-657: Array ファクトリ系を Kotlin 化する（`HeaderHelpers+SyntheticArrayStubs.swift`（(c) 一括計上だった 2043 行）内の b-reclass 第1弾）。`arrayOf` / `emptyArray` / `arrayOfNulls` の合成スタブ登録を削除し、`Stdlib/kotlin/ArrayIntrinsics.kt` に bundled Kotlin intrinsic（`@KsSymbolName` で `kk_array_of` / `kk_empty_array` / `kk_array_of_nulls` にリンク）として再宣言。primitive array factory 群（`intArrayOf` 等）は第2弾以降で対応。
- [x] KSP-658: Array contentEquals/contentToString/copyOf/copyOfRange を Kotlin 化する。generic `Array<T>` の `contentEquals`/`contentToString`/`copyOf()`/`copyOf(newSize)`/`copyOfRange` を bundled Kotlin source（`Stdlib/kotlin/collections/ArrayContentAndCopy.kt`）へ移行（要素等価は `kotlin.internal.__valuesEqual`、記憶域は `arrayOfNulls` 経由でブリッジ不要）。generic `contentEquals`/`contentToString` の synthetic stub 登録（`HeaderHelpers+SyntheticArrayStubs.swift`）と runtime 実装 `kk_array_contentEquals`/`kk_array_contentToString`（`RuntimeArrayDequeAndUtility.swift`）+ RuntimeABI エントリを削除。source-backed 呼び出しが legacy name-based lowering で `kk_array_copyOf*` に横取りされないよう KIR（`CallLowerer+LegacyMemberLikeCalls.swift` の `isSourceBackedArrayCopyCall` guard）と Lowering（`CollectionLiteralLoweringPass+VirtualCallRewrite.swift`/`+CallRewrite.swift`）を修正。`kk_array_copyOf`（toTypedArray 依存）/`kk_array_copyOf_newSize`/`kk_array_copyOf_newSize_init`/`kk_array_copyOfRange`（unsigned 配列 registration が再利用）は残置。副次発見: 明示 nullable 型引数付き generic vararg の型推論欠陥（pre-existing。master の KSP-673 / #5045 で解消済み）。検証（Linux）: `--filter Golden` green / `validate_runtime_abi_links.sh` green / `git diff --check` clean / 追加した array diff case は `kswiftc` 実行出力が本家一致。`swift_test.sh`（全件）は本 PR 変更起因の失敗なし（唯一の失敗は `CodegenBackendIntegrationTests/testCodegenArrayAnyAllNoneCount` の `kk_array_any` ラムダ Signal 11 で、`.agents/skills/testing-completion-gates` に既知の環境要因（本ボックスの Swift 6.2.4 + LLVM 14 と CI の Swift 6.3 + LLVM 18 の差）として記載のもの。clean master でも同一再現し、CI の Verification 2-4/5（Backend/Runtime）は green）。`diff_kotlinc.sh` はローカルに kotlinc 未提供のため CI（Verification 5/5）で検証
- [x] KSP-659: Array sorted*/binarySearch を Kotlin 化する（比較コアは KSP-309 の Comparator Kotlin 実装を利用）。完了: `Sources/CompilerCore/Stdlib/kotlin/collections/ArraySortingHOF.kt` を新設し、ジェネリック `Array<T>` の `sortedArray`/`sortedArrayDescending`/`sortedArrayWith`/`binarySearch`（自然順・Comparator）と全プリミティブ配列（Int/Long/Byte/Short/Char/Double/Float/UByte/UShort/UInt/ULong）版を bundled Kotlin ソース化。Comparator は `kotlin/comparisons/Comparators.kt`（KSP-309）を利用。合成スタブ登録・KIR 特殊経路・runtime `@_cdecl`・`RuntimeABISpec` から下記 `kk_*` を削除: `kk_array_binarySearch`/`kk_array_binarySearch_compare`/`kk_{int,long,byte,short,char,double,float,boolean,uByte,uShort,uInt,uLong}Array_binarySearch`/`kk_array_sortedArray`/`kk_array_sortedArrayDescending`/`kk_array_sortedArrayWith`。**併せて修正したバグ**: `runtimeCompareNullableValues`（`Sources/Runtime/RuntimeComparator.swift`）が raw 値 `0`（boxed zero/false）を `null` と誤判定していた欠陥を修正（null 判定を `runtimeNullSentinelInt` のみに限定）。これにより `Array<Int>.binarySearch(0)` 等の「0/false 要素検索」が正しく機能し、`compareValues(null, 0)` の順序も是正。回帰: `Scripts/diff_cases/array_binary_search.kt` 拡充・`Scripts/diff_cases/array_sorted_variants.kt` 新設（いずれも kotlinc と一致）、`Tests/RuntimeTests/RuntimeComparatorTests.swift` に `testCompareValuesBoxedZeroIsNotNull`/`testCompareValuesNullOrdersBelowBoxedZero` 追加。検証: `swift build`・`bash Scripts/swift_test.sh`・`--filter Golden`・`bash Scripts/diff_kotlinc.sh Scripts/diff_cases`・`bash Scripts/validate_runtime_abi_links.sh`・`git diff --check` green
- [x] KSP-660: Array 符号なしビュー変換を Kotlin 化する（**併せて BUG-019（ByteArray.joinToString/contentEquals 未スタブ）を吸収**。`kotlin.jvm.isArrayOf` は CLEANUP-STUB-098 側で削除）。符号付き↔符号なし配列の 8 view 変換（asUByteArray/asUShortArray/asUIntArray/asULongArray/asByteArray/asShortArray/asIntArray/asLongArray）を bundled Kotlin（`Stdlib/kotlin/collections/UArrays.kt`）へ移行し、合成スタブ登録（`HeaderHelpers+SyntheticArrayStubs.swift`）を削除。runtime identity-view は `kk_*` から stdlib 専用 `__kk_*` ブリッジへ降格（`RuntimeArrayBasics.swift` の `@_cdecl` と `RuntimeABISpec+Collection.swift`）。BUG-019 は #4671 で `string_tobytearray.kt` の gap が解消済みであることを確認（`transform` overload の gap は BUG-158 に切り出し）
- [x] KSP-661: Char 判定系を Kotlin 化する（isDigit/isLetter/isLetterOrDigit/isWhitespace/isUpperCase/isLowerCase/isDefined を bundled Kotlin `kotlin.text.CharPredicates` へ移行。残留: `__kk_char_unicode_category`/`__kk_char_is_uppercase`/`__kk_char_is_lowercase` テーブル参照ブリッジ）
- [ ] KSP-662: Char 変換系を Kotlin 化する（uppercaseChar/lowercaseChar/titlecase/digitToInt(OrNull)/digitToChar。ロケール依存 2 関数は `__kk_*_locale` ブリッジ残留（`java.util.Locale` interop 自体は (a) 削除方針）。前提: KSP-661）
- [ ] KSP-663: Char サロゲート演算を Kotlin 化する（isSurrogate 系/code 変換。前提: KSP-661）
- [x] KSP-664: AbstractIterator + プリミティブ Iterator 殻を .kt 化する（本家同型の純 Kotlin・ブリッジゼロ。`HeaderHelpers+SyntheticIteratorStubs.swift`（272行）削除）。完了: `Sources/CompilerCore/Stdlib/kotlin/collections/AbstractIterator.kt`（State マシン付き本家同型実装）と `PrimitiveIterators.kt`（Boolean/Byte/Short/Int/Long/Float/Double/CharIterator の殻）を追加し、`+SyntheticIteratorStubs.swift` を削除・`+SyntheticIterableRegistry.swift` の合成登録呼び出しを撤去。ブリッジ（external / `@KsSymbolName` / `kk_*`）ゼロ。副次修正: `AbstractIterator<T> : Iterator<T>` のように supertype 型引数が自クラスの型パラメータを参照するケースで型引数が解決されず override covariance が壊れるコンパイラバグを `Inheritance.swift`（enclosing 型パラメータをスコープに導入）で修正（回帰: `OpenFinalOverrideGenericReturnTests` green）。`String.iterator()`/`CharSequence.iterator()` の戻り値型を source-backed `CharIterator` に接続する post-bundled パッチを `+SyntheticStringStubs.swift` / `Phase.swift` に追加。検証: `swift_test.sh --filter Golden` 全 green（Lexer/Parser/Sema/Diagnostics、602s）、iterator/継承/override 関連 198 テスト green、`validate_runtime_abi_links.sh` green、`git diff --check` クリーン、`for` ループでの List/String/IntArray 反復が実機で正常動作。full `swift_test.sh` はローカルの runtime isolation lease 直列化で長時間化するため CI に委譲、diff_kotlinc はローカルに kotlinc 無しのため CI diff ジョブに委譲。派生ランタイム欠陥を BUG-155 / BUG-156 に切り出し（いずれも master では link / Sema 不能で本 PR の回帰ではない）
- [ ] KSP-665: `HeaderHelpers+SyntheticStringTypeHelpers.swift`（299行）を撤去する（Sequence/Iterable/Collection/List の型シェル取得ヘルパー — interface の .kt 化で自然不要化。`+SyntheticIterableRegistry.swift` との並行実装解消）
- [x] KSP-666: 注釈を .kt 化する（第1弾: kotlin 直下の共通 opt-in マーカー — ExperimentalUnsignedTypes/ExperimentalMultiplatform/ExperimentalSubclassOptIn 等 + uuid/encoding/reflect の Experimental マーカー。コンパイラの FQName 認識は宣言出自と無関係のため移設可能 — golden `native_annotations.kt` の実使用パターンで検証）
- [x] KSP-667: 注釈を .kt 化する（第2弾: kotlin.native + ObjC 系 — ObjCName/CName/HiddenFromObjC/ShouldRefineInSwift/FreezingIsDeprecated 等 15種）
- [x] KSP-668: 注釈を .kt 化する（第3弾: kotlin.experimental 系 + `HeaderHelpers+SyntheticMetaprogAnnotationHelpers.swift` の b-reclass 分。JVM 固有注釈は (a) 分割してから）
  - 完了 (branch `devin/ksp-668`): `kotlin.experimental.ExperimentalTypeInference` を bundled Kotlin source（`Stdlib/kotlin/experimental/TypeInference.kt`）へ移設。合成登録は `HeaderHelpers.swift`（contracts 経路）と `HeaderHelpers+SyntheticKotlinAnnotationStubs.swift` から削除。あわせて `collectAllHeaders` が bundled/residual stdlib ファイルをユーザーファイルより先に処理するよう順序を修正し、source-backed stdlib 宣言の解決を fileID 割り当て順に非依存化（`Phase.swift`）。挙動保存（opt-in / @Target union）を回帰テスト `ExperimentalAnnotationSourceMigrationTests` と既存 `AdvancedTypeInferenceTests` で固定。`kk_*` の削除・降格なし（注釈は runtime シンボルを持たない）。
  - `kotlin.experimental.ExperimentalNativeApi` は先行マージされた KSP-667 の `Stdlib/kotlin/experimental/NativeExperimentalAnnotations.kt` に収容済みのため本タスクでは重複を持たない。
  - スコープ外（別タスク）: `ExpectRefinement` は §9 分類 (c)（common opt-in marker）で残置。`HeaderHelpers+SyntheticMetaprogAnnotationHelpers.swift` は全体が (c) 注釈インフラ（AnnotationTarget/AnnotationRetention/DeprecationLevel/RequiresOptIn.Level enum と property/constructor helper 群 = AnnotationTargetValidation・opt-in 機構が依存）であり、単独で切り出せる (b) 部分集合は存在しないため table 移行は据え置き（本家形の enum 化は注釈クラス群と結合するため別弾）。
- [x] KSP-669: Comparable/RandomAccess の interface 宣言を .kt 化する（`HeaderHelpers+SyntheticComparableAndCollectionStubs.swift` の自己登録分のみ。プリミティブ型への Comparable 適合付与は c-hard 残留。死コード `compareToOrNull` は CLEANUP-STUB-098 側）。2026-07-24 完了: `kotlin.Comparable<in T>` を `Sources/CompilerCore/Stdlib/kotlin/Comparable.kt`、`kotlin.collections.RandomAccess` を `Sources/CompilerCore/Stdlib/kotlin/collections/RandomAccess.kt` に nominal 宣言として移設。`HeaderCollection.reusableSyntheticDeclarationSymbol` を interface 対応に拡張し、両ファイルを `reusableSyntheticSourceDeclarationKey` の allowlist に追加して synthetic shell シンボルを再利用（バンドル時に `.synthetic` フラグが外れ source-backed 化）。`compareTo` 演算子・プリミティブ Comparable 適合（c-hard）・range upper bound などの residual type hook は `docs/stdlib-pipeline.md` の (c) 区分どおり `HeaderHelpers+SyntheticComparableHelpers.swift`/`+SyntheticComparableAndCollectionStubs.swift` に残置（`compareTo` を .kt メンバとして宣言すると `T : Comparable<T>` 束縛の型パラメータ受け手に対するメンバ解決が壊れるため、bare-T レシーバ encoding の synthetic 登録を維持）。Golden（`comparable_interface`/`type_constraints`/`f_bound_generics`/`maxof_*`/`minof_*`/`in_range_operators` の `kotlin.Comparable` が `flags=synthetic`→`flags=_`）を更新。回帰ケース `Scripts/diff_cases/ksp669_comparable_randomaccess.kt` を追加。`compareToOrNull` は不変。作業中に既存の runtime バグ BUG-161 を発見（バグバックログに追跡）

#### concurrent / coroutines（(c) 再監査 2026-07-10 の b-reclass 分。全 Atomic タスク共通注意: kk_atomic_* はスタブ側 prefix 補間 emit のため rg 完了チェックは補間を考慮）

- [x] KSP-670: AtomicBoolean を Kotlin 委譲化する（`AtomicMigration.kt` の委譲パターンで get/set/getAndSet 等 — CAS ループ非依存分のみ。`kk_atomic_bool_*` のうち load/store/exchange はブリッジ残留）
- [x] KSP-671: Atomic reverse 変種と compareAndSet 公開層を Kotlin 化する（fetchAndAdd/fetchAndIncrement/fetchAndDecrement + compareAndSet を `AtomicInt`/`AtomicLong` について `AtomicMigration.kt` で委譲化。委譲先 addAndFetch/incrementAndFetch/decrementAndFetch/compareAndExchange の CPU 命令コアはブリッジ残留。`kk_atomic_int_*` は Java `AtomicInteger` interop と接頭辞共有のため削除不可＝除去 kk_ シンボルなし。KSP-670 と同様に休眠委譲 + `isRuntimeBackedAtomicSyntheticRetainedOverlap` にスタブ保持を追加 → KSP-672 で member-call 解決が修正され live 委譲化、当該スタブ保持は廃止）
- [x] KSP-672: Atomic 配列 *At 系の委譲分を Kotlin 化する（loadAt/storeAt 等の公開層 + 境界検査を Kotlin 側へ。コア CAS 命令はブリッジ残留。39関数中の委譲可能分を着手時固定）
- [x] KSP-673: Atomic CAS ループ系 13 関数を Kotlin 化する（getAndUpdate/updateAndGet/fetchAndUpdate + 配列版 fetchAndUpdateAt/updateAt/updateAndFetchAt。前提: **KSP-CAP-004**（`AtomicMigration.kt` コメントの保留解除））
  - 2026-07-24 完了: 13 関数の CAS リトライループ本体を bundled Kotlin source へ移行（スカラ = `Stdlib/kotlin/concurrent/AtomicMigration.kt`、配列 = `Stdlib/kotlin/concurrent/atomics/AtomicArrayMigration.kt`）。`kotlin.concurrent[.atomics]` は default import 外のため、`kotlin.ranges` の Range ソースリゾルバと同型の receiver 限定リゾルバ（`CallTypeChecker+MemberCallInferenceRegularResolution.swift` の `collectAtomicSourceExtensionCandidates`）を新設し、synthetic stub をスキップさせて source 実装に解決。Kotlin 型と `java.util.concurrent.atomic.AtomicInteger` が同一 `kk_atomic_int_*` prefix を共有するため両系を同時移行。合成登録（`HeaderHelpers+SyntheticAtomicStubs.swift` の `registerAtomicGetAndUpdateMethods` / scalar `includeGetAndUpdate` / array `includeFetchAndUpdate` / ref-array の fetchAndUpdateAt・updateAt・updateAndFetchAt）、公開 runtime cdecl 13 本（`Runtime/RuntimeAtomic.swift`）、`RuntimeABISpec+Atomic.swift` / `RuntimeABISpec+ABIParity.swift` の該当エントリ、lowering の AtomicReference closure アダプタ（`CallLowerer+MemberCallEmission.swift` / `CallLowerer+ClosureAdapters.swift`）を削除。低レベルブリッジ（load/store/exchange/compareAndSet/compareAndExchange・array *At 系・increment/decrement/add）は残留。**発見バグ同時修正**: generic 型引数を nullable lambda から推論する際 `lub([String, String?])` が `Any?` へ過剰拡大し `Conflicting bounds` を出す欠陥を `Sema/TypeSystem/Subtyping.swift` の `lub` で修正（nullable を再付与する `lubReapplyingNullability`）。回帰: `Scripts/diff_cases/generic_nullable_lub_inference.kt`。検証（PR #5045）: CI（Swift 6.3）で `swift build` / `swift build -c release` / `bash Scripts/swift_test.sh` / `--filter Golden` / `bash Scripts/diff_kotlinc.sh Scripts/diff_cases` / `bash Scripts/validate_runtime_abi_links.sh` すべて green。ローカル（Swift 6.2.4）では変更領域（atomic codegen/runtime 67件・type-system/lub・Golden Sema・ABI リンク検証・ABI spec version）を green 確認（collection codegen 系のネイティブ実行クラッシュは master でも再現する 6.2.4 ツールチェーン起因の既存事象で本変更とは無関係）。削除シンボルの残存参照ゼロ（`rg` 補間考慮）。master マージ後に `RuntimeABISpec.specVersion` を再生成。**発見バグ同時修正 (2)**: KSP-671（PR #5046）が `AtomicInt`/`AtomicLong` の `compareAndSet` を package スコープ拡張として source 化した結果、本 PR の CAS ループが `AtomicBoolean`/`AtomicReference`/`AtomicInteger` から無修飾 `compareAndSet(...)` を呼ぶと `KSWIFTK-SEMA-0002` になる欠陥が表面化（無修飾呼び出しのスコープ探索が、レシーバの合わない同名 package 拡張だけを候補にして解決失敗し、implicit receiver のメンバー探索へ落ちずに診断を出していた）。`CallTypeChecker+ImplicitReceiverExtensionFallback.swift` に `tryBindImplicitReceiverMemberCallForInapplicableScopeCandidates` を追加し、スコープ候補が全て「レシーバ不一致の拡張」の場合に限り implicit receiver のメンバー解決へフォールバック（従来エラーだった経路のみ救済するため既存の解決結果は不変）。回帰: `Scripts/diff_cases/implicit_receiver_member_vs_extension.kt` + `CompilerCoreTests+ImportsAndExtensionResolution.swift` の `testUnqualifiedCallPrefersImplicitReceiverMemberOverForeignExtension`
- [x] KSP-674: Flow ビルダーを Kotlin 化する（`kk_flow_as_flow`/`kk_flow_empty`/`kk_flow_of` — (c) ブリッジ `kk_flow_create`+`kk_flow_emit` の合成で表現。前提: **KSP-CAP-010** + KSP-CAP-012）
  - 完了(2026-07-24): `flowOf`/`emptyFlow`/`Iterable.asFlow` を bundled Kotlin ソース（`Sources/CompilerCore/Stdlib/kotlinx/coroutines/flow/Builders.kt`）へ移行し、`flow { }`（`kk_flow_create`）+ `emit`（`kk_flow_emit`）の合成で表現。削除した専用 `kk_*` シンボル: **`kk_flow_of` / `kk_flow_empty` / `kk_flow_as_flow`**（Runtime cdecl・`RuntimeABISpec`・Sema 合成登録・専用 lowering 書き換え・専用 callee 名をすべて削除）。残留ブリッジ: `kk_flow_create` / `kk_flow_emit` / `kk_flow_collect`（および Flow operator/terminal プリミティブ）。
  - 前提外バグ修正1（ユーザー選択 B により本 PR に同梱）: **flow ビルダーの emitter 側クロージャキャプチャが壊れていた**（`kk_flow_create` の第2引数 continuation が定数 `0` にハードコードされ、キャプチャ変数が emitter へ配線されずゴミ値/未定義参照になっていた）。修正: コンパイラ側で continuation を生成し `kk_coroutine_launcher_arg_set` にキャプチャ値を格納 → launcher thunk + continuation を `kk_flow_create` へ渡す（`CoroutineLoweringPass+CallRewriting.swift`）。Runtime 側は continuation を `RuntimeFlowHandle` に保持し、キャプチャあり/なしで emitter ABI を切替、派生ハンドルへ伝播（`RuntimeCoroutineFlow.swift`）。本家形 `flowOf(vararg)`/`asFlow()` はキャプチャ必須のため本修正が前提。
  - 前提外バグ修正2（本 PR に同梱）: **coroutine lowering の合成 SymbolID 空間衝突**。bundled `Builders.kt` 追加で synthetic 宣言数が増えた際、`coroutine_context_switching.kt` 等の一般 coroutine プログラムが SIGSEGV（`withContext` の block 関数ポインタが `%0`=continuation ポインタに解決されていた）。原因: lowered 関数 / launcher thunk のシンボルを整数カウンタ（`allocateSyntheticSymbol`）で採番する一方、continuation パラメータは `sema.symbols.define`（dense sema id）で採番しており、2 つの ID 空間が重複。codegen の `.symbolRef` 解決は同一 id の parameter を function より優先する（`NativeEmitter+EmissionConstants.swift`）ため、関数ポインタが parameter 値に化けていた。修正: sema がある場合 `allocateSyntheticSymbol` が `sema.symbols.define` で dense slot を予約し、全 synthetic KIR シンボルを単一 id 空間に統一（`CoroutineLoweringPass.swift` ほか）。回帰: `Scripts/diff_cases/flow_builders_coroutine_symbols.kt`（flow builder + `withContext` 併用）を追加し kotlinc 差分 green を実測。
  - Sema: `flowOf`/`emptyFlow` の builtin 特別扱い（型のみを束縛し callable target を持たない fixed-flow 特殊処理）と `unresolvedCallFallbackType` の該当分岐を削除。実宣言が見えれば通常の overload 解決で bundled 宣言に束縛され、import が無い場合は kotlinc 同様に unresolved-reference 診断になる。`asFlow` は collection receiver 時に `kotlinx.coroutines.flow.asFlow` の実（非合成）拡張へ束縛。`flow`/`channelFlow`/`callbackFlow` の特別扱いは維持。
  - 回帰テスト: `LoweringPassRegressionTests+Flow.swift` に flowOf/emptyFlow/asFlow + operator + 外側 val キャプチャの実行テストを追加、`RuntimeFlowTests.swift` は削除 cdecl 依存を撤去し fixed-value cold flow ヘルパを `kk_flow_create`+continuation 経路へ書き換え、`Scripts/diff_cases/flow_builders_kotlin.kt`（kotlinc 差分 green を実測確認）を追加。検証ゲート: `swift_test.sh`（full / `--filter Golden`）、`diff_kotlinc.sh Scripts/diff_cases`、`validate_runtime_abi_links.sh`、`git diff --check`、`loc_report.sh` を実行。残存参照 grep（`kk_flow_of`/`kk_flow_empty`/`kk_flow_as_flow` + 補間パターン）はコメント以外ゼロを確認。
- [ ] KSP-675: SharedFlow を Kotlin 化する（`kk_mutable_shared_flow_create/emit/try_emit`, `kk_shared_flow_collect/replay_cache` — replay buffer・購読者管理は Kotlin の状態遷移で表現可（2026-07-10 再監査で b-reclass 確定）。前提: KSP-674）
- [ ] KSP-676: StateFlow と share_in/state_in を Kotlin 化する（`kk_mutable_state_flow_create/emit/try_emit`, `kk_state_flow_value`, `kk_flow_share_in/state_in/release/retain`。前提: KSP-675。c-hard 残留: `kk_flow_stopped`/`kk_flow_emit_with_timestamp`）
- [x] KSP-677: Mutex/Semaphore の (b) 分 9 関数を Kotlin 化する（withLock 等ラッパー層。カーネル同期コア 6 関数は c-soft 残留 — 分離仕様は `docs/stdlib-pipeline.md` §9 の再監査記録に従う）。完了: (b) 9 関数（`Mutex()`/`Mutex.isLocked`/`Mutex.tryLock`/`Mutex.withLock`, `Semaphore()`/`Semaphore.availablePermits`/`Semaphore.tryAcquire`/`Semaphore.withPermit`, `Lock.withLock`）を bundled Kotlin source 化（`Stdlib/kotlinx/coroutines/sync/Sync.kt`・`Stdlib/kotlin/concurrent/Lock.kt`）。factory/accessor/try 系は demoted `__kk_*` bridge へ委譲、`withLock`/`withPermit` は canonical generic `suspend fun <T> ...(action: () -> T): T` で lock/unlock・acquire/release を合成、`Lock.withLock` は general closure ABI の `__kk_lock_withLock` bridge へ委譲。専用 `kk_mutex_withLock`/`kk_semaphore_withPermit` は削除、`kk_mutex_create`/`kk_mutex_isLocked`/`kk_mutex_tryLock`/`kk_semaphore_create`/`kk_semaphore_availablePermits`/`kk_semaphore_tryAcquire`/`kk_lock_withLock` は `__kk_*` へ降格。c-soft 残留 6: `kk_mutex_lock`/`kk_mutex_unlock`/`kk_semaphore_acquire`/`kk_semaphore_release`/`kk_read_write_lock_read`/`kk_read_write_lock_write`。**副次修正（本 PR 内）**: generic 高階関数 `fun <T> f(action: () -> T): T` が Unit 本体ラムダから `T` を推論できず `KSWIFTK-TYPE-0001`/`KSWIFTK-SEMA-0002` になるバグを修正（`ExprTypeChecker+NameLambdaAndCallableRefInference.swift` の `inferLambdaLiteralExpr` で、未解決型パラメータの期待戻り値型をラムダ本体の expectedType として押し下げないよう変更）。回帰テスト: `BundledStdlibExecutionTests`（generic Unit/value ラムダ・Mutex.withLock・Semaphore.withPermit）+ `Scripts/diff_cases/generic_unit_lambda_inference.kt`。**発見した別バグは BUG-049 として追跡**（`launch { }` 本体からキャプチャ付き suspend 呼び出しが `KSWIFTK-CORO-0003` になる coroutine lowering の feature gap。`coroutine_mutex_semaphore.kt` はこのため引き続き SKIP-DIFF）。検証: `swift build`・`swift_test.sh --filter Golden`・`validate_runtime_abi_links.sh`・上記回帰テスト green。`diff_kotlinc.sh` は当環境に `kotlinc` 不在のため未実行（環境制約）
- [x] KSP-678: Channel の (b) 分 7 関数を Kotlin 化する（iterator 層等。suspension コア 3 関数は c-soft 残留）
- [x] KSP-679: coroutineScope/supervisorScope の公開ラッパー 4 を Kotlin 化する（内部プリミティブ `kk_coroutine_scope_*`/`kk_supervisor_scope_*` は (c) のまま委譲。前提: KSP-CAP-012）
  - 完了(2026-07-24): 公開ラッパー `coroutineScope`/`supervisorScope` を `Sources/CompilerCore/Stdlib/kotlinx/coroutines/CoroutineScope.kt` の bundled Kotlin 実装へ移行し、scope lifecycle は (c) プリミティブ `kk_coroutine_scope_new`/`kk_supervisor_scope_new`/`kk_coroutine_scope_cancel`/`kk_coroutine_scope_wait` へ委譲。合成 stub 登録（`HeaderHelpers+SyntheticCoroutineRegistry.swift`）と lowering/型検査の特例（`CoroutineLoweringPass.swift`/`CoroutineLoweringPass+LauncherSupport.swift`/`CallTypeChecker.swift`）を削除。旧 convenience runtime 関数 `kk_coroutine_scope_run`/`kk_coroutine_scope_run_with_cont`/`kk_supervisor_scope_run`/`kk_supervisor_scope_run_with_cont` と対応する ABI spec・テストを削除。`kk_coroutine_scope_wait` を Kotlin `Throwable?` 契約に合わせ null sentinel を返すよう修正。ラッパーは既知の型推論制約（KSP-499: ジェネリック HOF のラムダ本体で入れ子ジェネリック呼び出しを行うと `evaluateCandidate` が外側の未束縛型パラメータを過剰制約する）を避けるため、従来の合成契約どおり `Any` を返す非ジェネリック形とした。同 PR で入れ子 suspend 関数値呼び出しの回帰（0/1 引数 invoke thunk の ABI 誤認、`kk_kxmini_run_blocking_with_cont` の outThrown/scope 伝播欠落、成功時に caller continuation の thrown slot を消去しない stale exception 漏れ）も修正。回帰は `Scripts/diff_cases/coroutine_scope_wrapper_regression.kt` と既存 `coroutine_scope.kt`/`supervisor_scope_basic.kt` で固定。**追加修正(2026-07-26)**: suspend 高階関数を自動 inline しなくなったことで、`InlineLoweringPass` の名前フォールバックが解決済みかつ非 inline な callee（`Mutex.withLock`）を同名 inline オーバーロード（`Lock.withLock`）へ差し替え、`__kk_lock_withLock` ブリッジを誤った ABI で呼び出して SIGSEGV する問題（`Scripts/diff_cases/mutex_basic.kt`）を修正。名前フォールバックはモジュール内に解決先関数が存在しない呼び出しに限定し、回帰テスト `LoweringPassRegressionTests.testNonInlineCallIsNotRedirectedToSameNamedInlineOverload` を追加。

#### delegates / reflect

- [ ] KSP-680: delegate インターフェース群を .kt 化する（ReadOnlyProperty/ReadWriteProperty/PropertyDelegateProvider。前提: KSP-CAP-007）
- [ ] KSP-681: ObservableProperty/Delegates 残余を Kotlin 化する（KSP-491 の範囲を超える残り約20系統。前提: KSP-491, KSP-680）。BUG-017はKSP-CAP-013のPR #4976で独立に修正済みのため本タスクの前提から外れた
- [x] KSP-682: KProperty0/1/2・KMutableProperty0/1 の殻を .kt 化する（前提: **KSP-CAP-009**（supertype 位置の関数型リテラル）→ #4987 対応済み。c-soft 解除。**併せて BUG-018（`kotlin.reflect.full.createInstance` が宣言のみでリンクエラー確定）の削除 or 実装を判断**）。完了: `Sources/CompilerCore/Stdlib/kotlin/reflect/KProperties.kt` を新規追加し `KProperty0/1/2`・`KMutableProperty0/1/2` を本家形（`KProperty0<out V> : KProperty<V>, () -> V` 等）の bundled Kotlin source として宣言。`HeaderHelpers+SyntheticPropertyDelegateStubs.swift` / `HeaderHelpers+SyntheticBucketedStubRegistry.swift` の合成 KProperty 殻登録と Function-supertype パッチ（`KProperty0/KMutableProperty0/KMutableProperty1FunctionSupertypePatch`）を `BundledDeclarationIndex` 存在チェックでガードし、source がある時は登録しない（source なし compilation は従来 fallback を維持）。BUG-018 は削除と判断: `registerCreateInstanceFunction`（`kotlin.reflect.full.createInstance`）は実装本体・emit・呼び出し元が一切無い dead declaration で、参照すると必ずリンクエラーになるため synthetic 宣言ごと削除（`rg`/`git grep` で残存参照ゼロを確認、`ReflectCreateInstanceSyntheticTests` の not-registered 回帰は維持）。`kk_*` シンボルの削除は無し: KProperty ランタイムブリッジ（`__kk_kproperty_stub_*`）は property-reference lowering（`CallLowerer+KCallableMemberCalls.swift` の `KProperty.name`）が依然使用するため保持。**併せて発見したコンパイラ修正**: function-type supertype（`KProperty0<V> : () -> V` 等）が nominal な `kotlin.Function{N}` インターフェースへ解決されず supertype として束縛されない問題を `Sources/CompilerCore/Sema/DataFlow/Inheritance.swift` の `resolveNominalSymbolAndTypeArgs` で修正（`.functionType` を `Function{N}` シンボル + `[.out(戻り値)] + 引数.map { .in($0) }` の型引数へ写像。suspend / context receiver 付きは nominal 等価物が無いため未束縛のまま）。これが無いと source-backed `KProperty0/1/2` の `FunctionN` 継承（property reference の `invoke` 継承）が解決できないため本 PR に含めた。なお同ファイルで当初併せて修正していた「bare 型パラメータ（`interface B<T> : A<T>`）が supertype 型引数として解決されない」問題は、master 側の KSP-664（#5044）が同等の修正（`enclosingTypeParameters`）を先に着地させたため、master マージ時に master 側実装へ寄せて重複を解消した。回帰テスト: `GenericInterfaceInheritanceTests`（generic upcast・継承メンバ解決・function-type supertype 束縛）を新規追加、`ReflectKProperty0/1/2`・`ReflectKMutableProperty0/1` の各 SyntheticTests を source-backed 期待値（`.synthetic` フラグ無し・`KProperty` supertype 引数は宣言 variance に従い `.invariant(V)`・mutable variant では `Function0/1` が KProperty0/1 経由の推移的 supertype）へ更新、`Scripts/diff_cases/kproperty_shells.kt` を追加。既知の別バグ（property-reference アクセサの実行時クラッシュ）は master でも再現する pre-existing のため BUG-162 として backlog に記録（本 PR スコープ外）。検証: `swift build`（debug）green・`swift build -c release --product kswiftc` green（whole-package の release build は `GoldenHarnessSupport` の `@testable import CompilerCore` により本 PR と無関係に pre-existing で失敗、本 PR では未変更）。`Scripts/swift_test.sh --filter Golden`（golden 1件 `reflect_kmutable_property0` 再生成後）・`ReflectKProperty*`/`ReflectKMutableProperty*`/`ReflectCreateInstance*`/`GenericInterfaceInheritance` 各 suite・`git diff --check`・`Scripts/validate_runtime_abi_links.sh` green。full `Scripts/swift_test.sh` は `CodegenBackendIntegrationTests` 14件（collection/sequence HOF の runtime crash・callee assertion）が失敗するが、いずれも true master（KProperties.kt 退避）でも同一署名で再現する当環境（Swift 6.2.4）の pre-existing 失敗で本変更起因ではない（CI は 6.3 が authoritative）。`diff_kotlinc.sh` は当環境に `kotlinc` 未導入のため CI 実行

### CLEANUP-STUB 追補（(a) 削除。2026-07-10 監査。採番は履歴最終 095 の続き。手順は RF-STUB-002 レシピ）

> 「本家で deprecated/obsolete かつ KSwiftK でも未実装」の二重死と fiction。**W6 の移行より先に実施を推奨**（移行対象面積が減る）。

- [x] CLEANUP-STUB-096: kotlin.native.concurrent のレガシー群を削除する (PR #5029)
  - 対象: `HeaderHelpers+SyntheticNativeConcurrentRegistry.swift` から Legacy AtomicInt/AtomicLong/AtomicNativePtr, FreezableAtomicReference 公開層, kotlin.native.concurrent.AtomicReference, MutableData, DetachedObjectGraph, WorkerBoundReference, atomicLazy, ObsoleteWorkersApi 系 3 (`waitForMultipleFutures`/`waitWorkerTermination`/`withWorker`), ensureNeverFrozen, freeze/isFrozen 公開層を削除
  - 残留: `kk_freeze_object`/`kk_is_frozen` は TransferMode 用に残留
  - テスト更新: `NativeConcurrentSyntheticStubTests.swift` から削除対象のテストを除去、`NativeConcurrentAPISurfaceInventoryTests.swift` の在籍リストを 15 エントリに縮小
  - 検証: `swift build` / `bash Scripts/swift_test.sh --filter SmokeTests` / `--filter Golden` / `--filter NativeConcurrent` / `--filter RuntimeABIExternalLinkValidationTests` pass。`bash Scripts/validate_runtime_abi_links.sh` pass
- [~] CLEANUP-STUB-097: NativeDataStubs のレガシー群を削除する — PR #5030
  - 対象: `HeaderHelpers+SyntheticNativeDataStubs.swift` から `registerSyntheticNativeBitSetStubs` / `registerSyntheticNativeImmutableBlobStubs` / `registerSyntheticNativeVector128Stubs`（`Vector128` 型エイリアス + `vectorOf`）を削除
  - 追随削除: `HeaderHelpers+SyntheticNativeInteropStubs.swift` から上記関数の呼び出しを削除。`HeaderHelpers+SyntheticNativeInteropHelpers.swift` から `cPointerType` / `deprecatedImmutableBlobAnnotations` / `deprecatedImmutableBlobFactoryAnnotations` / `deprecatedImmutableBlobPointerAnnotations` / `appendDeprecatedImmutableBlobAnnotations` / `deprecatedNativeVector128TypeAliasAnnotations` / `deprecatedNativeVectorOfAnnotations` を削除
  - テスト削除: `NativeBitSetSurfaceTests.swift` / `NativeImmutableBlobSurfaceTests.swift` / `NativeVector128SurfaceTests.swift` / `GoldenCases/Sema/cinterop_vectorof_int.kt` + `.golden` を削除
- [x] CLEANUP-STUB-098: Function 型の fiction ほかを削除する（`Function1.andThen`/`Function1.compose`/`Function2.curried` — 本家に存在しない Java 由来誤移植・参照ゼロ（`HeaderHelpers+SyntheticFunctionTypeStubs.swift` + `RuntimeFunctionTypes.swift`）+ `compareToOrNull`（ブリッジ未設定の死コード）+ `kotlin.jvm.isArrayOf`） — PR #5033
- [x] CLEANUP-STUB-099: JS/Wasm 専用 opt-in マーカー6種を削除する（ExperimentalJsCollectionsApi/ExperimentalJsExport/ExperimentalJsReflectionCreateInstance/ExperimentalJsStatic/ExperimentalWasmJsInterop + ExperimentalWasmInterop — `HeaderHelpers+SyntheticExperimentalMarkerStubs.swift` 内） — PR #5035
  - 対象: `HeaderHelpers+SyntheticExperimentalMarkerStubs.swift` から
    `ExperimentalJsCollectionsApi` / `ExperimentalJsExport` / `ExperimentalJsReflectionCreateInstance` / `ExperimentalJsStatic` / `ExperimentalWasmJsInterop` / `ExperimentalWasmInterop` を削除
  - 追随削除: `CompilerKnownNames.swift` から `experimentalJsExport` / `experimentalJsStatic` / `experimentalJsReflectionCreateInstance` / `experimentalJsCollectionsApi` の enum ケースを削除
  - テスト更新: `ExperimentalMarkerStubTests.swift` から上記6種のテストを削除
- [~] CLEANUP-STUB-100: Atomic の Java interop fiction を削除する（恒等関数の `kk_atomic_int/long/bool/ref_asJavaAtomic` 4 + `kk_atomic_int/long/ref_array_asJavaAtomicArray` 3 + `kk_java_atomic_long_array_asKotlinAtomicArray` + 未使用重複 `kk_reentrant_read_write_lock_new`） — PR 作成中
- [x] CLEANUP-STUB-101: 監査で確定したデッドコードを一括削除する（`kk_math_round_mode` 系15（Sema 登録ゼロ・到達不能）/ `kotlin.experimental` Int 版登録一式（汎用特例が先に解決）/ `kk_check_not_null(_lazy)`/`kk_require_not_null(_lazy)` 4 / CLEANUP-STUB-084 取り残しの `registerSyntheticJvmAnnotationClass`・`registerSyntheticBooleanAnnotationPropertyAndConstructor` 2関数 / `kk_sequence_of_single` の生死判定。※`kk_system_measureTime*` 3 と `CallLowerer+CollectionStdlibMemberCalls.swift` は KSP-617/620 と重複するため先行した方で実施） — PR #5038
- [x] CLEANUP-STUB-102: cinterop 未配線外殻を削除する（`HeaderHelpers+SyntheticCInteropStubs.swift` 3,065行中、実働12関数（ポインタ⇔Long 変換・pin/unpin・配列⇄CValues・文字列変換 — `__kk_` 降格で残留）以外の alloc/nativeHeap/Arena/MemScope/StableRef/CPointer.get/set/pointed/value/reinterpret/Vector128 アクセサ等、externalLinkName 未設定で「コンパイルは通るが動かない」外殻を削除。必要になったら本家 .def ベースで再実装する方針（2026-07-10 決定）。`+SyntheticNativeInteropHelpers.swift`（1292行）の get/set/pointed 系ビルダーも道連れ削除） — PR #5058, verified `swift build`, `SmokeTests`, `NativeCInterop` green
- [x] CLEANUP-STUB-103: 削除タスク未起票の (a) 21 ファイルを確定し、CLEANUP-STUB-104 〜 CLEANUP-STUB-124 へ個別起票する（対象: BigInteger / Concurrency / Dynamic / FileIO / FileTreeWalk / FileWalkDirection / FilesUtility / JsFunction / LocaleConstructor / NativeFunctionAnnotation / OnErrorAction / PathStubs 本体+分割3 / PlatformObjectHelpers / ReadWriteLock / Serialization / Test / URI / URL）
- [ ] CLEANUP-STUB-104: `HeaderHelpers+SyntheticBigIntegerStubs.swift` を削除する
  - 対象ファイル: `Sources/CompilerCore/Sema/DataFlow/HeaderHelpers+SyntheticBigIntegerStubs.swift`（650行）
  - 削除内容: `registerSyntheticBigIntegerStubs(...)` および `java.math.BigInteger` クラス・コンストラクタ（String）・companion `valueOf`・四則/remainder/pow/abs/gcd/toInt/toLong 等のメンバ登録を削除
  - 呼び出し元: `HeaderHelpers+SyntheticBucketedStubRegistry.swift:272`（`bucket: .targetOutCleanup, name: "BigInteger"`）を削除
  - 連動整理: bundled `Sources/CompilerCore/Stdlib/java/math/BigInteger.kt`、Runtime `Sources/Runtime/RuntimeBigInteger.swift`（`kk_biginteger_*` 58件）/ `Sources/Runtime/RuntimeStringConversion.swift`（`kk_string_to_biginteger` 等3件）、`Sources/RuntimeABI/RuntimeABISpec+BigInteger.swift` / `RuntimeABISpec+String.swift` 内 BigInteger 登録 / `RuntimeABISpec+ABIParity.swift` 該当行
  - テスト影響: `Tests/CompilerCoreTests/Sema/BigIntegerSyntheticLinkTests.swift`、`StringToBigIntegerFunctionTests.swift`、`StringToBigIntegerOrNullFunctionTests.swift`、`Tests/CompilerCoreTests/Integration/KotlinCompilationBigIntegerTests.swift`、`Tests/RuntimeTests/RuntimeBigIntegerTests.swift`、Golden `big_integer_basic.golden` 等の削除または更新
- [ ] CLEANUP-STUB-105: `HeaderHelpers+SyntheticConcurrencyStubs.swift` を削除する
  - 対象ファイル: `Sources/CompilerCore/Sema/DataFlow/HeaderHelpers+SyntheticConcurrencyStubs.swift`（186行）
  - 削除内容: `registerSyntheticConcurrencyStubs(...)` および `java.lang.Thread` / `kotlin.concurrent` パッケージ内 Thread/Timer/Executor 等の synthetic 登録を削除
  - 呼び出し元: `HeaderHelpers+SyntheticBucketedStubRegistry.swift:284`（`bucket: .targetOutCleanup, name: "Concurrency"`）を削除
  - 連動整理: `java.util.concurrent` 互換部分は Atomic 系（CLEANUP-STUB-100/096 側）と重複しないよう整理
  - テスト影響: `Tests/CompilerCoreTests/Sema/ConcurrencySyntheticStubTests.swift` など対象テストの削除/縮小
- [ ] CLEANUP-STUB-106: Dynamic 用 `HeaderHelpers+SyntheticDynamicStubs.swift` は削除済みのため追跡のみ
  - 対象: `Sources/CompilerCore/Sema/DataFlow/HeaderHelpers+SyntheticDynamicStubs.swift`（DEADCODE-CORE-009/010 で削除済み）
  - 削除内容: `registerSyntheticDynamicStubs` および `registerDynamicIterator` は既に削除されている
  - テスト影響: なし（`HeaderHelpers+SyntheticBucketedStubRegistry` 等からの残り呼び出しがないか確認）
- [ ] CLEANUP-STUB-107: `HeaderHelpers+SyntheticFileIOStubs.swift` を削除する
  - 対象ファイル: `Sources/CompilerCore/Sema/DataFlow/HeaderHelpers+SyntheticFileIOStubs.swift`（2319行）
  - 削除内容: `registerSyntheticFileIOStubs(...)` および `java.io.File` クラス・コンストラクタ・`readText`/`writeText`/`readLines`/`appendText`/`forEachLine`/`bufferedReader`/`delete`/`mkdirs`/`listFiles`/`walk`/`name`/`path`/`exists`/`isFile`/`isDirectory` 等の登録を削除
  - 呼び出し元: `HeaderHelpers.swift:1241`、`HeaderHelpers+SyntheticBucketedStubRegistry.swift:201`（`name: "FileIO"`）、`HeaderHelpers+SyntheticFileTreeWalkStubs.swift` / `HeaderHelpers+SyntheticOnErrorActionStubs.swift` 内のコメント参照を整理
  - 連動整理: bundled `Stdlib/kotlin/io/FileIO.kt`（および `FileStreamExtensions.kt`/`FileTraversal.kt`）の出番確認、Runtime `Sources/Runtime/RuntimeFileIO.swift`（`kk_file_*`/`kk_files_*` 等）、`Sources/RuntimeABI/RuntimeABISpec+FileIO.swift`
  - テスト影響: `Tests/CompilerCoreTests/GoldenCases/Sema/file_*.golden`・`file_tree_walk.golden`、`Tests/CompilerBackendTests/Codegen/*File*` テスト群、`Tests/RuntimeTests/RuntimeFileTreeWalkTests.swift`、`Scripts/diff_cases/file_*.kt` 等の整理
- [ ] CLEANUP-STUB-108: `HeaderHelpers+SyntheticFileTreeWalkStubs.swift` を削除する
  - 対象ファイル: `Sources/CompilerCore/Sema/DataFlow/HeaderHelpers+SyntheticFileTreeWalkStubs.swift`（291行）
  - 削除内容: `registerSyntheticFileTreeWalkStubs(...)` および `kotlin.io.FileTreeWalk` クラス・`walkTopDown`/`walkBottomUp`/`walk`/`maxDepth`/`filter`/`onEnter`/`onLeave`/`onFail`/`toList`/`forEach` 等の登録を削除
  - 呼び出し元: `HeaderHelpers.swift:1244`、`HeaderHelpers+SyntheticBucketedStubRegistry.swift:210`（`name: "FileTreeWalk"`）を削除
  - 連動整理: Runtime `Sources/Runtime/RuntimeFileIO.swift` 内 FileTreeWalk 関連 `kk_file_tree_walk_*`、該当 ABI 登録
  - テスト影響: `Tests/CompilerCoreTests/Sema/FileTreeWalkClassTests.swift`、`Tests/RuntimeTests/RuntimeFileTreeWalkTests.swift`、Golden `file_tree_walk.golden`/`.kt`、diff case `file_walk.kt` の削除または更新
- [ ] CLEANUP-STUB-109: `HeaderHelpers+SyntheticFileWalkDirectionStubs.swift` を削除する
  - 対象ファイル: `Sources/CompilerCore/Sema/DataFlow/HeaderHelpers+SyntheticFileWalkDirectionStubs.swift`（113行）
  - 削除内容: `registerSyntheticFileWalkDirectionStubs(...)` および `kotlin.io.FileWalkDirection` enum（`TOP_DOWN`/`BOTTOM_UP`）の登録を削除
  - 呼び出し元: `HeaderHelpers.swift:1243`、`HeaderHelpers+SyntheticBucketedStubRegistry.swift:207`（`name: "FileWalkDirection"`）を削除
  - テスト影響: `Tests/CompilerCoreTests/Sema/FileWalkDirectionEnumTests.swift` の削除または更新
- [ ] CLEANUP-STUB-110: `HeaderHelpers+SyntheticFilesUtilityStubs.swift` を削除する
  - 対象ファイル: `Sources/CompilerCore/Sema/DataFlow/HeaderHelpers+SyntheticFilesUtilityStubs.swift`（520行）
  - 削除内容: `registerSyntheticFilesUtilityStubs(...)` および `java.nio.file.Files` singleton・`createFile`/`delete`/`copy`/`move`/`createDirectory`/`createDirectories`/`size`/`getLastModifiedTime`/`isRegularFile`/`isDirectory`/`exists`/`walk`/`list`/`newDirectoryStream`/`createTempFile`/`createTempDirectory` 等の登録を削除
  - 呼び出し元: `HeaderHelpers.swift:1246`、`HeaderHelpers+SyntheticBucketedStubRegistry.swift:215`（`name: "FilesUtility"`）を削除
  - 連動整理: Runtime `Sources/Runtime/RuntimeFileIO.swift` 内 `kk_files_*`（48件）、`Sources/RuntimeABI/RuntimeABISpec+FileIO.swift`/`RuntimeABISpec+Path.swift` 該当 ABI
  - テスト影響: diff case `files_utility.kt`、`file_isDirectory_test.kt` 等の整理
- [ ] CLEANUP-STUB-111: `HeaderHelpers+SyntheticJsFunctionStubs.swift` を削除する
  - 対象ファイル: `Sources/CompilerCore/Sema/DataFlow/HeaderHelpers+SyntheticJsFunctionStubs.swift`（77行）
  - 削除内容: `registerSyntheticJsFunctionStubs(...)` / `registerSyntheticJsFunction(...)` および Kotlin/JS `js(...)` 外部関数の synthetic 登録を削除
  - 呼び出し元: `HeaderHelpers.swift:1228`、`HeaderHelpers+SyntheticBucketedStubRegistry.swift:174`（`name: "JsFunction"`）を削除
  - テスト影響: 本家 Kotlin/JS 専用のためテストは少ない；`swift build` と Sema inventory テストの確認
- [ ] CLEANUP-STUB-112: `HeaderHelpers+SyntheticLocaleConstructorStubs.swift` を削除する
  - 対象ファイル: `Sources/CompilerCore/Sema/DataFlow/HeaderHelpers+SyntheticLocaleConstructorStubs.swift`（401行）
  - 削除内容: `registerSyntheticLocaleConstructorStubs(...)` および `java.util.Locale` クラス・コンストラクタ / `forLanguageTag` / `getDefault` 等の登録を削除
  - 呼び出し元: `HeaderHelpers+SyntheticBucketedStubRegistry.swift:263`（`name: "LocaleConstructor"`）を削除
  - 連動整理: Runtime `Sources/Runtime/RuntimeI18N.swift`（`kk_locale_*` 28件）、`Sources/RuntimeABI/RuntimeABISpec+Locale.swift`/`RuntimeABISpec+String.swift` 内 Locale ABI 登録
  - テスト影響: diff case `locale_basic.kt`、`Tests/CompilerBackendTests/Codegen/CodegenBackendIntegrationTests+LLVMLinkingAndArtifacts.swift` などで Locale 使用箇所の確認
- [ ] CLEANUP-STUB-113: `HeaderHelpers+SyntheticNativeFunctionAnnotationStubs.swift` を削除する
  - 対象ファイル: `Sources/CompilerCore/Sema/DataFlow/HeaderHelpers+SyntheticNativeFunctionAnnotationStubs.swift`（85行）
  - 削除内容: `registerSyntheticNativeGetterStubs(...)` / `registerSyntheticNativeSetterStubs(...)` および `kotlin.js.nativeGetter`/`nativeSetter`/`nativeInvoke` 注釈クラスの登録を削除
  - 呼び出し元: `HeaderHelpers+SyntheticBucketedStubRegistry.swift:302`（`name: "NativeGetter"`）、`HeaderHelpers+SyntheticBucketedStubRegistry.swift:281`（`name: "NativeSetter"`）を削除
  - テスト影響: `Tests/CompilerCoreTests/Sema/NativeGetterAnnotationTests.swift`、`NativeSetterAnnotationTests.swift`、`NativeByteArraySetterSurfaceTests.swift`、`NativePrimitiveByteArraySetterSurfaceTests.swift`、`NativeUnsignedByteArraySetterSurfaceTests.swift` の削除または更新
- [ ] CLEANUP-STUB-114: `HeaderHelpers+SyntheticOnErrorActionStubs.swift` を削除する
  - 対象ファイル: `Sources/CompilerCore/Sema/DataFlow/HeaderHelpers+SyntheticOnErrorActionStubs.swift`（120行）
  - 削除内容: `registerSyntheticOnErrorActionStubs(...)` および `kotlin.io.OnErrorAction` enum（`SKIP`/`TERMINATE`）の登録を削除
  - 呼び出し元: `HeaderHelpers.swift:1245`、`HeaderHelpers+SyntheticBucketedStubRegistry.swift:213`（`name: "OnErrorAction"`）を削除
  - 連動整理: `RuntimeFileIO.swift` 内 `copyTo` 再帰処理で使われる OnErrorAction ordinal（`kk_file_copy_*`）があれば整理
  - テスト影響: `Tests/RuntimeTests/RuntimeFileCopyRecursivelyTests.swift` の確認
- [ ] CLEANUP-STUB-115: `HeaderHelpers+SyntheticPathStubs.swift`（本体）を削除する
  - 対象ファイル: `Sources/CompilerCore/Sema/DataFlow/HeaderHelpers+SyntheticPathStubs.swift`（2102行）
  - 削除内容: `registerSyntheticPathStubs(...)` および `kotlin.io.path.Path` クラス・companion `createTempFile`/`createTempDirectory`/`list`/`walk`/`readBytes`/`readText`/`writeText`/`writeBytes`/`copyTo`/`resolve`/`parent`/`fileName`/`extension` 等の登録を削除
  - 呼び出し元: `HeaderHelpers.swift:1247`、`HeaderHelpers+SyntheticBucketedStubRegistry.swift:219`（`name: "Path"`）を削除
  - 連動整理: 3つの split ファイル（CLEANUP-STUB-116〜118）も併せて削除；Runtime `Sources/Runtime/RuntimePath.swift`（`kk_path_*` 273件、`kk_uri_*`/`kk_url_*` も含む）、`Sources/RuntimeABI/RuntimeABISpec+Path.swift`（114件）
  - テスト影響: `Tests/CompilerCoreTests/Sema/Path*FunctionTests.swift`（5ファイル）、`PathWalkOptionEnumTests.swift`、`Tests/CompilerBackendTests/Codegen/CodegenBackendIntegrationTests+PathCreateSymbolicLink.swift`、`Scripts/diff_cases/path_basic.kt`、Golden 該当ケースの整理
- [ ] CLEANUP-STUB-116: `HeaderHelpers+SyntheticPathStubs+GenericFunctionRegistration.swift` を削除する
  - 対象ファイル: `Sources/CompilerCore/Sema/DataFlow/HeaderHelpers+SyntheticPathStubs+GenericFunctionRegistration.swift`（548行）
  - 削除内容: `registerPathUseLinesFunction(...)` 等の overload 群、`useLines`/`readLines`/`forEachLine` 等の generic function 登録ヘルパーを削除
  - 呼び出し元: `HeaderHelpers+SyntheticPathStubs.swift` 内 1364行付近の呼び出しを削除
  - テスト影響: Path 系テストと連動
- [ ] CLEANUP-STUB-117: `HeaderHelpers+SyntheticPathStubs+SymbolRegistration.swift` を削除する
  - 対象ファイル: `Sources/CompilerCore/Sema/DataFlow/HeaderHelpers+SyntheticPathStubs+SymbolRegistration.swift`（315行）
  - 削除内容: `registerPathConstructor(...)` 等 Path コンストラクタ登録ヘルパーを削除
  - 呼び出し元: `HeaderHelpers+SyntheticPathStubs.swift` 内 599行付近の呼び出しを削除
  - テスト影響: Path 系テストと連動
- [ ] CLEANUP-STUB-118: `HeaderHelpers+SyntheticPathStubs+TypeCreation.swift` を削除する
  - 対象ファイル: `Sources/CompilerCore/Sema/DataFlow/HeaderHelpers+SyntheticPathStubs+TypeCreation.swift`（337行）
  - 削除内容: `registerPathCopyActionContextSurface(...)` 等 `CopyActionContext` / `WalkOptions` 等の型登録ヘルパーを削除
  - 呼び出し元: `HeaderHelpers+SyntheticPathStubs.swift` 内 190行付近の呼び出しを削除
  - テスト影響: Path 系テストと連動
- [ ] CLEANUP-STUB-119: `HeaderHelpers+SyntheticPlatformObjectHelpers.swift` を削除する
  - 対象ファイル: `Sources/CompilerCore/Sema/DataFlow/HeaderHelpers+SyntheticPlatformObjectHelpers.swift`（216行）
  - 削除内容: `registerSyntheticObjectProperty(...)` 等 Java `Class` / `Platform` object 向け helper を削除
  - 呼び出し元: `HeaderHelpers+SyntheticStringStubs.swift`（313, 3245行付近）と `HeaderHelpers+SyntheticTODOAndIOStubs.swift`（434-466行付近）から呼ばれているため、削除前に live 側へ移設/インライン化
  - テスト影響: 上記 live ファイルを使う Sema / Golden テストへの影響を確認；削除前に参照切り替え
- [ ] CLEANUP-STUB-120: `HeaderHelpers+SyntheticReadWriteLockStubs.swift` を削除する
  - 対象ファイル: `Sources/CompilerCore/Sema/DataFlow/HeaderHelpers+SyntheticReadWriteLockStubs.swift`（216行）
  - 削除内容: `registerSyntheticReadWriteLockStubs(...)` および `java.util.concurrent.locks.ReentrantReadWriteLock` クラス・`kotlin.concurrent.read` 等の登録を削除
  - 呼び出し元: `HeaderHelpers+SyntheticBucketedStubRegistry.swift:293`（`name: "ReadWriteLock"`）を削除
  - 連動整理: Runtime `Sources/Runtime/RuntimeSync.swift` 内 `kk_reentrant_read_write_lock_*` / `kk_read_write_lock_*`（13件）、`RuntimePreconditions.swift`（2件）、該当 ABI 登録（`RuntimeABISpec+Coroutine.swift` 等）を整理
  - テスト影響: `Tests/CompilerCoreTests/Sema/ReadWriteLockSyntheticLinkTests.swift`、`LockSyntheticMemberLinkTests.swift`、`Tests/CompilerCoreTests/KIR/BuildKIRRegressionTests+ExpressionAndAdvancedScenarios+ReadWriteLock.swift`、`Tests/RuntimeTests/RuntimeReadWriteLockTests.swift` の削除/更新
- [ ] CLEANUP-STUB-121: `HeaderHelpers+SyntheticSerializationStubs.swift` を削除する
  - 対象ファイル: `Sources/CompilerCore/Sema/DataFlow/HeaderHelpers+SyntheticSerializationStubs.swift`（723行）
  - 削除内容: `registerSyntheticSerializationStubs(...)` および `kotlinx.serialization`/`kotlinx.serialization.json` の `Json`/`JsonBuilder`/`JsonConfiguration`/`Serializers` 等の登録を削除
  - 呼び出し元: `HeaderHelpers+SyntheticBucketedStubRegistry.swift:254`（`name: "Serialization"`）を削除
  - 連動整理: Runtime `Sources/Runtime/RuntimeSerialization.swift`（`kk_serialization_*`/`kk_json_*` 46件）、`Sources/RuntimeABI/RuntimeABISpec+Serialization.swift`
  - テスト影響: `Tests/RuntimeTests/RuntimeSerializationTests.swift`、`Tests/CompilerCoreTests/Sema/LibMetadataSerializationTests*.swift`、diff cases `json_serialization.kt`/`dataclass_serialization.kt`/`collection_serialization.kt`/`custom_serializer.kt` の整理
- [ ] CLEANUP-STUB-122: `HeaderHelpers+SyntheticTestStubs.swift` を削除する
  - 対象ファイル: `Sources/CompilerCore/Sema/DataFlow/HeaderHelpers+SyntheticTestStubs.swift`（184行）
  - 削除内容: `registerSyntheticTestFrameworkStubs(...)` および `kotlin.test` 注釈（`Test`/`Before`/`After`/`Ignore` 等）・`assertEquals`/`assertTrue`/`assertNull`/`fail` 等の登録を削除
  - 呼び出し元: `HeaderHelpers.swift:1203`（`kotlinPkg` 指定）、`HeaderHelpers+SyntheticBucketedStubRegistry.swift:119`（`name: "TestFramework"`）を削除
  - 連動整理: Runtime `Sources/Runtime/RuntimeTest.swift`（`kk_test_*` 12件）、`Sources/RuntimeABI/RuntimeABISpec+Test.swift`
  - テスト影響: `Tests/CompilerCoreTests/Sema/TestFrameworkSyntheticStubTests.swift`、diff cases `test_framework_basic.kt`/`test_array_new_functions.kt` 等 `kotlin.test` 関連ケースの整理
- [ ] CLEANUP-STUB-123: `HeaderHelpers+SyntheticURIStubs.swift` を削除する
  - 対象ファイル: `Sources/CompilerCore/Sema/DataFlow/HeaderHelpers+SyntheticURIStubs.swift`（178行）
  - 削除内容: `registerSyntheticURIStubs(...)` および `java.net.URI` クラス・コンストラクタ / `toURL` / `resolve` 等の登録を削除
  - 呼び出し元: `HeaderHelpers+SyntheticBucketedStubRegistry.swift:257`（`name: "URI"`）を削除
  - 連動整理: Runtime `Sources/Runtime/RuntimeURI.swift`（`kk_uri_*` 29件）、`Sources/Runtime/RuntimePath.swift` 内 URI 変換（4件）、`Sources/RuntimeABI/RuntimeABISpec+FileIO.swift` 内 URI ABI 登録
  - テスト影響: `Tests/CompilerCoreTests/Integration/KotlinCompilationURITests.swift`、`Tests/RuntimeTests/RuntimeURITests.swift`/`RuntimeURLTests.swift`、diff case `url_basic.kt` 内 URI 使用箇所の確認
- [ ] CLEANUP-STUB-124: `HeaderHelpers+SyntheticURLStubs.swift` を削除する
  - 対象ファイル: `Sources/CompilerCore/Sema/DataFlow/HeaderHelpers+SyntheticURLStubs.swift`（332行）
  - 削除内容: `registerSyntheticURLStubs(...)` および `java.net.URL` クラス・コンストラクタ / `readText` / `readBytes` / `openConnection` 等の登録を削除
  - 呼び出し元: `HeaderHelpers+SyntheticBucketedStubRegistry.swift:260`（`name: "URL"`）を削除
  - 連動整理: Runtime `Sources/Runtime/RuntimeNetwork.swift`（`kk_url_*`/`kk_uri_*` 36件）、`Sources/Runtime/RuntimePath.swift` 内 URL 変換（4件）、`Sources/RuntimeABI/RuntimeABISpec+FileIO.swift` 内 URL ABI 登録
  - テスト影響: `Tests/CompilerCoreTests/Integration/KotlinCompilationURLTests.swift`、`Tests/RuntimeTests/RuntimeURLTests.swift`、diff case `url_basic.kt` の整理

### バグバックログ（BUG-NNN。既存・未修正バグの追跡。PR 状態は各タスクの記載時点）

> このセクションは既存の未修正バグと、同じPR内で安全に修正できなかったバグの追跡用。新たに発見した修正可能なバグは、最小再現と回帰テストを含めて発見したPR内で修正し、報告だけのためにここへ追加しない。

- [x] BUG-017: 当初の診断は不正確だった。実際に調査すると `lazy()`/`lazy(mode)`/`lazyOf` の戻り値型登録は元から正しく `kotlin.Lazy` を指しており、真因は (1) 呼び出し側の型注釈 `Lazy<Int>` の解決（`resolveTypeRef`）がスコープ／import優先順位を無視し `kotlin.properties.Lazy` に誤って解決していたこと（KSP-CAP-013で修正）と (2) `lazy(mode) { }` の2引数オーバーロードが `lazyModeSymbol` の旧fqNameに起因してデフォルトimportスコープから到達不能だったこと、の2点。KSP-681を待たずKSP-CAP-013のPR #4976で修正済み（マージ済み。HEAD で `Scripts/diff_cases/generic_function_expected_type.kt` と `Scripts/diff_cases/lazy_direct_call_and_explicit_type.kt` の実行、`Tests/CompilerCoreTests/Sema/GenericFunctionExpectedTypeConstraintTests.swift` の 3 テスト green を再確認済み）
- [x] BUG-043: PR #4572（StringBuilder source 移行）で `buildString { … }` / `buildStringBuilder { … }` ラムダ内の StringBuilder メンバ呼び出しのうち、`append`/`appendLine`/`insert`/`delete`/`length`/`appendRange` の6種のみ `CollectionLiteralLoweringPass+CallRewriteFactories.swift:636-649` でグローバル builder state（`RuntimeBuilderDSL.swift` の `runtimeBuilderState`）向けに書き換えられ、`reverse`/`clear`/`toString`/`deleteAt`/`deleteCharAt`/`setCharAt`/`set`/`get`/`capacity`/`ensureCapacity`/`trimToSize`/`setRange`/`insertRange`/`replace`/`deleteRange` は書き換え対象外。書き換えられなかった呼び出しは source-backed `StringBuilder.kt` の通常メソッドへフォールバックするが、`runtimeExecuteStringBuilderAction`（`RuntimeBuilderDSL.swift:345-347`）はラムダの暗黙レシーバとして常に `0`（ダミー値）を渡すため、`this` が無効なハンドル `0` になり実行時に壊れる（最小再現: `buildString { append("x"); reverse() }`）。修正には `RuntimeBuilderState`（`RuntimeBuilderDSL.swift:32-`）へ上記14メソッド相当のグローバル状態版プリミティブを追加し `@_cdecl` エクスポート・`RuntimeABISpec` 登録・書き換えリスト拡張が必要で、単純なバグ修正の範囲を超える新規ランタイム面。BUG-042（ローカル環境でのフルリンク不可）により実行検証もできないため、このセッションでは実装を見送り。発見元: PR #4572 の Devin AI レビュー（comment_id 3532565207 / 3532565261 / 3536548604 / 3536548807）をコード追跡で確認
  - 解消: 本バグが依存していたグローバル builder state 経由の StringBuilder DSL 書き換え（`runtimeExecuteStringBuilderAction` / `runtimeBuilderState` 向けの `kk_build_string(_builder)` DSL エントリポイント）は、その後の StringBuilder source 化により完全に撤去された。現在の `buildString`/`buildStringBuilder`（`Stdlib/kotlin/text/StringBuilder.kt`）は純 Kotlin で実 `StringBuilder()` オブジェクトを生成しレシーバラムダを正しい `this`（有効なハンドル）で呼ぶため、ダミー `0` レシーバ問題は存在しない。最小再現 `buildString { append("x"); reverse() }` および 14 メソッド相当を `kswiftc` で実行し正しい出力を確認。回帰固定として `Tests/CompilerBackendTests/Codegen/CodegenBackendIntegrationTests+StringBuilderEdgeCases.swift` の `testCodegenBuildStringReceiverMethodsUseValidThisHandle` と diff case `Scripts/diff_cases/buildstring_receiver_methods.kt` を追加。なお `replace`/`insertRange` は暗黙レシーバでのオーバーロード解決に失敗する別件（`with(StringBuilder()) { … }` でも再現。`this.replace(...)` は解決可）で本項とは無関係なため回帰テストからは除外し別途フォローアップとする。
- [x] BUG-046: 明示的なラムダ引数型注釈（`{ a: Int, b: Int -> ... }`）が構文解析時に破棄される（`BuildASTPhase+ExpressionParserLambdaObjectCallable.swift` の `parseLambdaParamNames` はカンマ区切りセグメントから先頭の識別子だけを拾い `: Type` 部分を読み捨てる。`.lambdaLiteral` AST ノード自体が `params: [InternedString]`（名前のみ）で型情報を保持できない構造）。期待型（SAM 変換先や関数型パラメータ）が引数型を完全に決定できる場合は無症状だが、決定できない場合（例: 型引数なし・期待型なしの raw `fun interface`/`Comparator` SAM コンストラクタ）はラムダ引数が期待型由来の unsubstituted 型パラメータ型に束縛され、注釈で意図した具象型は無視される。最小再現: `fun interface Cmp<T> { fun compare(a: T, b: T): Int }` の後 `val c = Cmp { a: Int, b: Int -> a - b }`（型引数なし）が `KSWIFTK-SEMA-0002: No viable overload found for operator 'minus'` になる（本家 kotlinc は注釈を信頼して通る）。なお SAM/期待型が一切ない素のラムダ単体（`val f = { a: Int, b: Int -> a - b }`）は例外的に別の不健全フォールバック（未確定オペランド型に対する二項演算子解決が `Any` 型を許容してしまう）を通ってコンパイル自体は成功するが、`f("x", "y")` のような型不一致呼び出しを検出できず LLVM codegen で不正な整数演算命令を生成してクラッシュする（`LLVM ERROR: Cannot select`）。発見元: KSP-CAP-005（Comparator SAM コンストラクタ調査）。**修正済み**: パーサ（`parseLambdaParams`）が各パラメータセグメントの `: Type` を `TypeRefParserCore.parseTypeRefPrefix` で解析し、`ASTArena` のサイドテーブル（`lambdaParamTypeRefs`。`loopLabels`/`whenSubjectVarNames` と同じ方式で `.lambdaLiteral` のシグネチャは不変）へ記録するようにした。Sema 側 `inferLambdaLiteralExpr` は注釈が付いたパラメータについて期待型由来の型より注釈を優先し、`CallTypeChecker+SAMCalls` の SAM コンストラクタ経路は型引数も期待型も無い場合に注釈から interface の型引数を推論する。これにより素のラムダも `(Int, Int) -> Int` として束縛され、`f("x", "y")` は codegen まで到達せず Sema で型エラーになる。注釈が具体的な期待引数型と矛盾する場合（`val g: (String) -> Int = { s: Int -> ... }`、`Cmp<String> { a: Int, ... }`、`listOf(1,2,3).map { s: String -> ... }`）は注釈を採用せず `KSWIFTK-SEMA-0025` を出す（期待型側の引数型が未置換の型パラメータを含む場合のみ注釈が優先される）。回帰テスト: `Tests/CompilerCoreTests/Sema/LambdaParamTypeAnnotationTests.swift`、`Scripts/diff_cases/lambda_param_type_annotation.kt`
- [x] BUG-047: top-level / class member プロパティ初期化子が複数のトップレベル `{ }` ブロックを含む式（`if/else`、ブロック分岐を持つ `when` 等）で終わる場合、最初のブロック以降が完全に失われる。`KotlinParser+Statements.swift` の `parseTail`（`inBlock: false` 呼び出し）が top-level `{` を検出するたびに `parseBlock()` で1ブロックだけ切り出して即座に `break` するため（`try`/`catch`/`finally` 継続のみ例外的に処理）、2つ目以降のブロック（`else { ... }` 等）はトークン列に一切追加されずプロパティ宣言のCSTから脱落する。最小再現: `val topLevelIf = if (1 > 2) { \"yes\" } else { \"no\" }` の後 `fun main() { println(topLevelIf) }` を実行すると `\"no\"` ではなく `0` を出力する（`else` 節が失われ、条件 false 側の値が未定義動作になる）。同条件 `1 < 2`（true 側）では偶然 `\"yes\"` を出力するため見かけ上動作しているように見える点に注意。関数宣言の式本体・ローカル変数の初期化子は影響を受けない（単一パスの式パーサ経由のため）。発見元: KSP-CAP-005（`propertyHeadTokens` 修正の副作用検証中に発見・単一トレーリングラムダのケースとは別原因と特定）。**完了（2026-07-27）**: `KotlinParser+Statements.swift` の `parseTail` に `if` キーワード追跡（`sawIfKeyword`）を追加し、top-level `{ }` ブロックを切り出した直後の次トークンが `else` の場合はループを継続するよう修正（既存の `try`/`catch`/`finally` 継続と同じ仕組み）。`else if` の連鎖もこれで最後の `else` まで CST に取り込まれる。回帰テスト: `Tests/CompilerCoreTests/Integration/CompilerCoreTests+PropertyBlockBranchParsing.swift`（top-level if/else・else if 連鎖・class member if/else・try/catch/finally の非回帰）と `Scripts/diff_cases/property_block_branch_initializer.kt`（実 kotlinc と PASS 確認済み）。なお「top-level の `if` 初期化子プロパティを2つ以上宣言すると LLVM codegen が SIGSEGV する」現象を作業中に確認したが、これはブロック有無に関係なく（`val a = if (c) 1 else 2` を2つ並べるだけで）再現する本件と無関係な既存問題（当該ローカル環境は LLVM 14 / CI は LLVM 18）で、BUG-163 として別記録
- [x] BUG-049: `launch { }`（`CoroutineScope.launch`）の trailing lambda 本体から、外側の可変変数をキャプチャする suspend 関数を呼ぶと `KSWIFTK-CORO-0003: Coroutine launcher 'launch' passed 0 capture argument(s) but referenced suspend function expects N` でコンパイルに失敗する。`Sources/CompilerCore/Lowering/CoroutineLoweringPass+LauncherSupport.swift` の launcher lowering は、起動対象の suspend lambda がキャプチャ引数を要求する場合を未実装で（同ファイルに `"CoroutineScope.launch does not yet support a suspend lambda that captures outer variables"` の明示エラーもある）、launch 本体からネストした suspend 呼び出しへキャプチャ変数を転送する経路が無い。Mutex/Semaphore とは無関係の一般的な既存バグで、最小再現: `suspend fun <T> doWork(action: () -> T): T = action()` の後 `runBlocking { var c = 0; launch { doWork { c++ } }.join(); println(c) }`（同じ呼び出しを `launch` の外＝`runBlocking` 直下に置くと正常動作する）。発見元: KSP-677（Mutex.withLock / Semaphore.withPermit を Kotlin source 化し、`Scripts/diff_cases/coroutine_mutex_semaphore.kt` の `launch { mutex.withLock { counter++ } }` で顕在化。KSP-677 では別途 generic ラムダ戻り値型推論バグ〔`KSWIFTK-TYPE-0001`/`KSWIFTK-SEMA-0002`〕を修正済みで、当該 diff case の Sema 段は通るようになったが、この CORO-0003 の coroutine lowering feature gap が残るため引き続き SKIP-DIFF）。→ 修正済み: `launch { }` / `launch(dispatcher) { }` / `CoroutineScope.launch { }` のいずれでもキャプチャを launcher continuation 経由でネストした suspend 呼び出しへ転送するよう lowering を実装（`CallLowerer` のキャプチャ注入を dispatcher 位置対応に拡張し、`CoroutineScope.launch` に capture 対応の runtime ABI `kk_coroutine_scope_launch_with_cont` を追加）。回帰は `Scripts/diff_cases/coroutine_launch_capture.kt`（kotlinc parity）と runtime テスト `testCoroutineScopeLaunchWithContForwardsCaptureArgs` で固定。`coroutine_mutex_semaphore.kt` は CORO-0003 とは別の既存 runtime GC-under-parallelism クラッシュ（100+ 並列）と `delay` 依存のため引き続き SKIP-DIFF。DEBT-DIFF-003 の structured concurrency 残課題と同系統
- [x] BUG-048: callable reference の SAM 変換（例: `Comparator<Int>(::myCompare)`）がコンパイルは通るが実行時に `KSwiftK panic [KSWIFTK-RUNTIME-0001]: Virtual dispatch failed: method not found in vtable/itable` でクラッシュする。Sema 側は `isSamConvertibleArgument`（`CallTypeChecker+SAMCalls.swift`）がラムダリテラルと同様 callable reference も SAM 変換対象としてマークするが、KIR lowering 側の `LambdaLowerer.swift` の `lowerCallableRefExpr` が `sema.bindings.isSamConversion(exprID)` を一切参照しておらず、`lowerLambdaLiteralExpr` が使う `lowerSamWrapperValue`（interface を実装した itable 付き wrapper object を合成する経路）を経由しない。結果として callable reference は常に素のクロージャ値として lower され、呼び出し側が itable dispatch を試みると失敗する。最小再現: `fun myCompare(a: Int, b: Int): Int = a - b` の後 `val c = Comparator<Int>(::myCompare); println(c.compare(3, 5))` を実行するとクラッシュする（同じ内容をラムダリテラル `Comparator<Int> { a, b -> myCompare(a, b) }` で書けば正常動作）。ユーザー宣言 `fun interface` でも同様に再現し synthetic Comparator 固有の問題ではない。発見元: KSP-CAP-005（Comparator SAM コンストラクタ調査）。**完了（2026-07-27）**: ①Sema（`CallTypeChecker+SAMCalls.swift`）は SAM 変換対象式に対して underlying function type しか記録しておらず、ラムダリテラルと違い callable reference は式型が関数型のままで変換先 interface 型を復元できなかったため、`SemanticsModels.swift` に `samInterfaceTypes`（`bindSamInterfaceType` / `samInterfaceType(for:)`）を追加し SAM コンストラクタ推論時に束縛。②`LambdaLowerer.swift` の `lowerCallableRefExpr` の先頭で `sema.bindings.isSamConversion(exprID)` を判定し、新規 `lowerCallableRefSamWrapperValue` がラムダ呼び出し規約 `(captures..., params...) -> R` の thunk（`kk_sam_ref_thunk_<exprID>`）を合成して参照先を呼び出し、既存の `lowerSamWrapperValue` に委譲して itable 付き wrapper object を生成するようにした（bound receiver `obj::method` は capture 経由で thunk の先頭引数に渡る）。固定: `Scripts/diff_cases/sam_constructor_callable_ref.kt`、`Tests/CompilerCoreTests/Lowering/LoweringPassRegressionTests.swift` の `testSamConvertedCallableRefLowersToInterfaceWrapper`。**副次発見（本 PR のスコープ外・別バグとして記録）**: BUG-164（`fun interface` 型パラメータを取る関数への callable reference 実引数が Sema で弾かれる）
- [x] BUG-139: bundled `RangeMembership.kt` の `rangeIsEmpty<T : Comparable<T>>` が source-backed stdlib の型検査をハングさせる（`kswiftc --emit kir Scripts/diff_cases/hello.kt` が完了しない）。最小再現: `Sources/CompilerCore/Stdlib/kotlin/ranges/RangeMembership.kt` を注入した状態で `Scripts/diff_cases/hello.kt` をコンパイル。Int/Long/Char 専用 helper への分割で回避 — 発見元 `CI-4669` / PR #4669 open。2026-07-23 バグバックログ一括対応セッションで現行 HEAD を再確認: `RangeMembership.kt` は既に `rangeIsEmptyInt`/`rangeIsEmptyLong`/`rangeIsEmptyChar` の型別 private helper に分割済み（汎用 `<T : Comparable<T>>` 版は存在しない）。回避策が適用済みであることをソース確認した
- [x] BUG-141: object 式が実装した interface の格納プロパティ（`override val`、カスタム getter 無し）を、その interface 型の変数を介して外部（呼び出し元）から読むと、未解決シンボル `_<プロパティ名>` により `KSWIFTK-LINK-0001` リンクエラーになる（object 自身のメンバ関数呼び出しは正常に dispatch される — 本件はプロパティのみ）。最小再現:
  ```kotlin
  interface Holder { val value: Int }
  fun make(): Holder = object : Holder { override val value: Int = 42 }
  fun main() { println(make().value) }
  ```
  外側ローカル変数のキャプチャは一切関与しない（キャプチャ無しの上記最小ケースでも同一の未解決シンボルエラーを確認済み）。発見元: KSP-CAP-001（object 式メンバ関数のローカル変数キャプチャ対応）の回帰テスト作成中に偶然遭遇。KSP-CAP-001 のスコープ（メンバ関数本体のキャプチャ）とは無関係な、格納プロパティの interface 経由 dispatch 自体の欠陥。修正済み: interface プロパティを itable の getter メソッドスロット（method スロットの後ろ `[vtableSize, vtableSize + propertyCount)` に採番）としてモデル化し、メンバ関数と同じ itable dispatch（`kk_object_register_itable_method` / `kk_itable_lookup_dynamic`）へ統合。実装側（object 式・named class）の格納プロパティには field を読む getter accessor を合成し、custom getter は既存 accessor を流用。interface 型 receiver からのプロパティ read を `.itableDynamic` へ変更。回帰テスト: `Scripts/diff_cases/interface_stored_property_dispatch.kt` + `CodegenBackendInterfacePropertyDispatchTests`（object 式 / named class / interface 引数 / custom getter）
- [x] BUG-142: ユーザー定義 Iterator/Iterable クラスが `MutableList` または `IntArray` フィールドを保持し、そのフィールドに対して `size`/`get`/`iterator` 等を呼び出すと、実行時に `RuntimeValue.legacyRawValue.getter` で null/不正ポインタ読み出しにより SIGSEGV する（`RuntimeArrayBox.elements` 経路）。for-in lowering 自体は正しく生成されるが、イテレータ実体内のコレクション操作でクラッシュする。最小再現（手動 `hasNext()`/`next()` でも再現）:
  ```kotlin
  class EntryIterator(private val values: MutableList<Entry>) {
      private var index = 0
      operator fun hasNext(): Boolean = index < values.size
      operator fun next(): Entry = values[index++]
  }
  class EntryBag(private val values: MutableList<Entry>) {
      operator fun iterator(): EntryIterator = EntryIterator(values)
  }
  fun main() {
      val bag = EntryBag(mutableListOf(Entry(1, 2), Entry(3, 4), Entry(5, 6)))
      val it = bag.iterator()
      var sum = 0
      while (it.hasNext()) { val e = it.next(); sum += e.first + e.second }
      println("manual=$sum")
  }
  ```
  同様に `IntArray` フィールドを持つ `IntArrayIterator`/`IntArrayBag` でもクラッシュ。`listOf`/`mutableListOf` そのものの for-in は動作するため、クラスフィールドとして格納されたコレクションの object layout / boxing に起因するものと推定。修正には `RuntimeArrayBox` / オブジェクトフィールド書き込み経路の調査が必要で、KSP-CAP-002 の for-in lowering 範囲を超える。発見元: PR #4986 の KSP-CAP-002 検証中。2026-07-27 修正: 真因はランタイムではなく (1) 式位置の `++`/`--`（`values[index++]`）が parser で読み捨てられミューテーションが消えていたこと（`index` が進まず無限ループ→暴走確保でクラッシュ）、(2) `val a = b` が `b` のレジスタにエイリアスされ、後続の `b += 1` が `a` まで書き換えていたこと。式位置 `++`/`--` を `{ val tmp = x; x += 1; tmp }` 相当へ desugar し、bare 変数読み出し初期化子をスナップショットコピーするよう修正。回帰: `Scripts/diff_cases/increment_expression_position.kt` と `CodegenBackendIntegrationTests+IncrementExpressionPosition.swift`
- [x] BUG-143: 同一ファイル内で、トップレベル関数のシグネチャ（引数型・戻り値型）が同ファイル内で後方に宣言された class/interface/object 等を forward-reference すると `KSWIFTK-SEMA-0025: Unresolved type` になる（クラスと同名かどうかは無関係な一般的な制限）。原因: `HeaderCollection.collectHeader`（`Sources/CompilerCore/Sema/DataFlow/HeaderCollection.swift` の `.funDecl` ケース）が `file.topLevelDecls` を単一パスで先頭から処理しながら各宣言の型注釈を `resolveTypeRef` でその場で解決しており、シンボル名の事前一括登録（forward declaration）パスが無い。最小再現: `fun make(seed: Long): Box = Box(seed.toInt())` の直後に `class Box(val value: Int)` を宣言（`make`/`Box` が異なる名前でも再現し、名前衝突とは無関係であることを確認済み）。発見元: KSP-CAP-006（クラスと同名トップレベル関数の共存を許可するタスク）の検証中、`fun Box(seed: Long): Box` を `class Box` より前に置く順序で確認（`class` が先なら該当箇所の型注釈は前方参照にならないため再現しない）。当該タスクのスコープ外 — 修正には `collectHeader` のヘッダ収集を「全宣言のシンボル名を先に登録 → シグネチャ型注釈を解決」の2パス構成へ再設計する必要があり、影響範囲が広く本PRの安全な修正方針を超える。**完了（2026-07-27）**: ヘッダ収集を2パス構成に再設計。`Phase.collectAllHeaders` の先頭に前方宣言パス（`HeaderCollection.predeclareNominalTypeHeaders`）を追加し、全ファイルのトップレベル nominal 型（class/interface/object/typealias）のシンボルを先に登録してから既存のヘッダ収集を行うようにした（`collectHeader` は登録済みシンボルを `predeclaredSymbol` で受け取り再定義しない）。typealias は「別名の underlying が未解決のまま alias シンボル自体を型として使う」誤解決を避けるため、残りの宣言より前に収集する。これによりトップレベル関数/プロパティのシグネチャに加え、メンバのシグネチャからの後方宣言型参照、および後方宣言 typealias の参照も解決される。検証: 新規 Sema 回帰テスト5件（`Tests/CompilerCoreTests/Sema/DataFlowAndSemaRegressionTests.swift`）と新規 diff ケース `Scripts/diff_cases/forward_reference_declaration.kt`
- [x] BUG-144: `DataFlowSemaPhase.resolveTypeRef`（`Sources/CompilerCore/Sema/DataFlow/BodyAnalysis.swift`）の構造的再帰深度ガード（`maxStructuralRecursionDepth = 512`、`KSWIFTK-SEMA-TYPE-DEPTH` を診断として返す設計）が、実際にガード発火（深さ512）へ到達する前に実行スレッドのスタックを使い切り、診断を返す代わりにプロセスがクラッシュする（signal 10 = SIGBUS）。最小再現: `Tests/CompilerCoreTests/Sema/DataFlowAndSemaRegressionTests.swift` の既存テスト `testDeeplyNestedGenericTypeRefEmitsDepthDiagnostic()`（`List<List<...<Int>...>>` を600段ネストして単体で `resolveTypeRef` を直接呼ぶ）を単独実行すると再現する: `DEVELOPER_DIR=/Applications/Xcode-beta.app/Contents/Developer SWIFT_TEST_PARALLEL=0 bash Scripts/swift_test.sh --filter CompilerCoreTests.DataFlowAndSemaRegressionTests/testDeeplyNestedGenericTypeRefEmitsDepthDiagnostic`。2回連続で同一の signal 10 crash を確認しており非決定的なタイムアウトではない（`swiftpm-testing-helper` 経由のテスト実行スレッドが、512フレームの再帰に対して十分な余裕を持たないスタックサイズで動いている可能性）。発見元: KSP-CAP-006 の検証中、`swift_test.sh --filter` によるスリープ復帰後の再実行で偶然発見（この既存テスト自体は本PRの変更が触れる `HeaderHelpers.swift`/`CallTypeChecker.swift` のいずれも経由せず、独立した `SymbolTable`/`TypeSystem`/`ASTArena` を手組みして `resolveTypeRef` を直接呼ぶだけの構成のため、本PRの変更由来ではないと判断。テスト自体および深度ガード機構は PR #4715（`Cap structural type resolution/substitution recursion depth`）由来で本PR着手前から存在）。本PRのスコープ外 — 原因調査（実行スレッドのスタックサイズ実測、ガード閾値512の妥当性再検証、必要なら閾値引き下げ or 明示的スタックサイズ指定）は別タスクで実施。**修正済み（2026-07-29）**: 実行スレッドのスタック残量を実測して打ち切る方式ではなく、深度キャップ自体をスタック枯渇より確実に手前で発火する値まで引き下げる方針で解消。PR #5094（`TypeRefParserCore.maxRecursionDepth`: 512→32、`BuildASTPhase+ExpressionParser` の `maxRecursionDepth`: 512→64、`LibraryMetadataParsing.maxDepth`: 512→64）に続き、PR #5093 で `DataFlowSemaPhase.maxStructuralRecursionDepth` をパーサ側の境界に連動させ `TypeRefParserCore.maxRecursionDepth - 1`（=31。パーサが先に `KSWIFTK-PARSE-TYPE-DEPTH` を出せるよう1段低く設定）に変更。600段ネストのハードコード AST fixture テストは、本番の閾値から導出した境界値テスト（31段ネストは正常解決・32段ネストは `KSWIFTK-SEMA-TYPE-DEPTH` を1件）に置き換えた。実行スレッドのスタック残量を pthread API で実測し閾値512のまま打ち切るアプローチ（PR #5085）も並行して存在したが、キャップ引き下げのみで512 KiBスレッド上のクラッシュが再現しなくなったためプラットフォーム依存コードを追加しないこちらの方針を採用し、PR #5085 は本修正により不要（superseded）と判断してクローズ
- [x] BUG-145: `String` 拡張関数の本体内で `substring(...)` を**暗黙レシーバ**（`this.` 無し）で呼ぶと実行時に巨大サイズ確保（`Fatal error: failed to allocate <huge> bytes`）でクラッシュする。`this.substring(...)` と明示すれば正常。同じ暗黙レシーバでも `get`/`uppercase`/`reversed` 等は正常に動作し、`substring` 固有。`substring` は `CallLowerer+LegacyMemberLikeCalls.swift` の name-based lowering（`kk_string_substring_flat`）を通り、暗黙レシーバ経路で `loweredReceiverID` が誤り、Int 引数がレシーバ／長さとして誤解釈されるものと推定。最小再現:
  ```kotlin
  fun String.g(n: Int): String = substring(n)   // crash
  fun main() { println("hello".g(2)) }           // this.substring(n) なら "llo"
  ```
  発見元: KSP-404（prefix/suffix の Kotlin 化）。KSP-404 の `StringPrefixSuffix.kt` では `this.substring(...)` 明示で回避済み。legacy `kk_string_substring_flat` lowering は KSP-406（substring/slice/range 系の Kotlin 化）で削除予定であり、暗黙レシーバ lowering の修正は KSP-404 の安全なスコープを超えるため本項で追跡する
- [x] BUG-146: `provideDelegate` operator を持つ delegate をローカル宣言（`fun f() { val x by Factory() }`）で使うと、`provideDelegate` 呼び出しが一切行われず `x` が `Factory()` インスタンス自身に束縛される（member/top-level の `by DelegateFactory()` は `KIRLoweringDriver+ProvideDelegate.swift` 経由で正しく動作する）。最小再現:
  ```kotlin
  class ValidatedDelegate(private var value: String) {
      operator fun getValue(thisRef: Any?, property: Any?): String = value
  }
  class DelegateFactory {
      operator fun provideDelegate(thisRef: Any?, prop: Any?): ValidatedDelegate = ValidatedDelegate("")
  }
  fun main() {
      val name by DelegateFactory()
      println(name)
  }
  ```
  発見元: KSP-CAP-007（ローカル delegate の getValue/setValue unboxing 修正）。同 PR で custom delegate の直接 getValue/setValue 解決は修正したが、`provideDelegate` は effective delegate の再解決（`sema.symbols.hasProvideDelegate`/`resolvedDelegateMemberSignature` の再帰）が member 側専用の経路にしか無く、ローカル宣言向けに同じ再解決をKIRで行う実装が別途必要なため本 PR のスコープ外とした（`ExprLowerer+ControlFlowAndBlocks.swift` の `.localDecl` は `isCustomDelegateDecl` 判定で `hasProvideDelegate` の場合を明示的に除外し、修正前と同じ挙動にフォールバックする）
- [x] BUG-147: stdlib 特殊ケース delegate（`lazy`/`Delegates.observable`/`vetoable`/`notNull`）をローカル宣言（`fun f() { val x by lazy { 42 } }`）で使うと、`kk_lazy_create`/`kk_lazy_get_value` 等の呼び出しが一切行われず `x` が delegate ファクトリの戻り値（`RuntimeLazyBox` 等のハンドル）自身に束縛される（member/top-level の `by lazy { }` は `StdlibDelegateLoweringPass` + `MemberLowerer+DelegatedAndAccessorLowering.swift` 経由で正しく動作する）。最小再現:
  ```kotlin
  fun main() {
      val x by lazy { 42 }
      println(x)
  }
  ```
  発見元: KSP-CAP-007（ローカル delegate の getValue/setValue unboxing 修正）。stdlib 特殊ケースは operator 規約ではなく `StdlibDelegateKind.detect` による構文形状判定 + `StdlibDelegateLoweringPass`（member/top-level の `$delegate_` field 経由）でハードコードされた別経路のため、ローカル宣言向けの同等経路（`$delegate_` field ではなくローカルレジスタでの同期を要する）を今回のPRスコープに含めなかった。KSP-491（Lazy/Delegatesの Kotlin 化で operator 規約解決へ統一）が完了すれば、本PRで修正した `.custom` kind 向けローカル delegate 呼び出し合成が自動的にカバーする可能性が高い（要再検証）
  - 修正済み: ローカル宣言はハンドルをローカルレジスタに保持し（`KIRLoweringContext.localStdlibDelegateKindBySymbol` で delegate kind を追跡）、読み出し・代入・複合代入のたびに `kk_<kind>_get_value`/`kk_<kind>_set_value` を合成するようにした（`ExprLowerer+ControlFlowAndBlocks.swift` の `.nameRef`/`.localAssign`/`.compoundAssign`、`ExprLowerer+ControlFlowHelpers.swift` の `lowerLocalStdlibDelegateRead`）。`lazy` は宣言時に getValue を呼ばないため初回読み出しまで初期化されない本家挙動を維持する。あわせて (1) `StdlibDelegateLoweringPass` が `$delegate_` field 初期化以外の `Delegates.observable/vetoable/notNull` 呼び出しも `kk_*_create` に書き換えるようにし（従来はローカルで未定義シンボル参照のままリンクエラー）、(2) synthetic stub の `Delegates.observable/vetoable/notNull` をジェネリック（`ReadWriteProperty<Any?, T>` を返し、コールバックはデフォルト値扱い）に変更して関数本体でのオーバーロード解決失敗（`KSWIFTK-SEMA-0002`）を解消し、(3) stdlib delegate のプロパティ型を `Lazy<T>`/`ReadWriteProperty<Any?, T>` の型引数から導出するようにした（`DeclTypeChecker+PropertyHelpers.swift`）。検証: `Scripts/diff_cases/local_delegate_stdlib_lazy.kt`・`local_delegate_stdlib_delegates.kt` を追加し実 kotlinc と出力一致、`LocalDelegatePropertyKIRTests` に KIR 回帰テスト4件追加
- [x] BUG-152: `CharSequence` インターフェース自体に `length`/`get(index)`/`subSequence(...)` がメンバとして登録されておらず、`registerSyntheticStringStubs`（`HeaderHelpers+SyntheticStringStubs.swift`）は `length` を `String` 型への extension function（`receiverType: stringType`）としてのみ登録している。実際の Kotlin では `CharSequence` がこれらを抽象メンバとして宣言し `String` 等の実装型がオーバーライドするため、静的型が `CharSequence`（`String` そのものではなくインターフェース型）の値に対するメンバアクセスは実装型に関わらず解決できるべきだが、本実装では静的型が具象型（`String` 等）の場合しか解決しない。最小再現: `fun printLength(cs: CharSequence) { println(cs.length) }; fun main() { printLength("hello") }` は `KSWIFTK-SEMA-0024: Unresolved member function 'length'` で失敗する（`"hello".length` の直接呼び出しは正常）。同様に `val cs: CharSequence = "hello"; cs.length` も失敗する。`CharSequence` 型のパラメータ・変数を経由した多態的なメンバアクセスが広く影響を受ける可能性がある（`length` 以外に `get`/`subSequence` も同様の懸念があるが未検証）。発見元: 本セッション（バグバックログ一括対応、BUG-044 の `StringBuilder is CharSequence` 修正検証中に、`is` チェックが通っても `cs.length` のようなメンバアクセスが別の理由で失敗することを発見）。番号は本セッション記載時点で未使用だった 152 を採番（BUG-141/142 は同日付で master に別内容が独立して着地したため衝突を避けた。BUG-150/151 は同セッション内の BUG-017 側追跡タスクで既に使用済み）。今回修正できない理由: `CharSequence` を真のインターフェースメンバとして再設計し、`String`/`StringBuilder` 等の実装型からの itable/vtable ディスパッチを通す変更は、Sema のシンボル登録方式とメンバ解決ロジック双方にまたがる規模の大きい変更で、単一の小さな修正では閉じられないため見送り 対応済み（本 PR）: `registerSyntheticStringStubs` に `receiverType: charSequenceType` の `length` / `subSequence(startIndex, endIndex)` スタブを追加し（`get` は既に CharSequence 版が存在）、静的型が `CharSequence` の値でもメンバ解決できるようにした。ランタイム側は `kk_char_sequence_length` が `String` ハンドルのみを受け付けて panic していたため、`RuntimeHelpers.swift` に `runtimeCharSequenceText(from:)` を追加して `StringBuilder` ハンドルも扱えるようにした。回帰は `Scripts/diff_cases/char_sequence_member_access.kt`（kotlinc diff PASS）と `KotlinCompilationStringCollectionTests` の BUG-152 節。残存課題は BUG-164 を参照
- [x] BUG-153: コンパイル時リテラルではなく実行時に構築された `String` 値（文字列連結・`toString()`・`buildString { ... }` 等の戻り値）は `is CharSequence` が誤って `false` を返す。リテラル文字列（`"hello" is CharSequence`）は `true` を返すため見かけ上は動いているように見えるが、実プログラムで実際に流通する動的な `String` の大半はこの誤判定の影響を受ける。原因は `kk_op_is`（`Sources/Runtime/RuntimeStringArray.swift:450-`）の型トークン分岐で、`CharSequence` は `String` 専用の `stringBase` 分岐ではなく interface 用の `nominalBase` 分岐（`runtimeObjectTypeID(rawValue:)` に依存）を通ること。`String` 自身は（本タスクで StringBuilder に対して追加した `kotlin.text.StringBuilder → kotlin.CharSequence` 相当の）`kotlin.String → kotlin.CharSequence` supertype edge をどの生成経路でも一度も登録していないため、`nominalBase` 分岐は常に該当インスタンスを見つけられず `false` を返す。リテラルが `true` になるのは、おそらく Sema/lowering 側でリテラル式に対する `is CharSequence` が定数畳み込みされ、この実行時チェック自体を経由しないため（未確認、コード追跡までは実施していない）。最小再現: `fun main() { val a = "he" + "llo"; println(a is CharSequence) }` は `false`（本家 kotlinc は `true`）。同様に `StringBuilder("x").toString() is CharSequence` や `buildString { } is CharSequence` も `false` になることを `.build/debug/kswiftc` で実機確認済み。発見元: 本セッション（バグバックログ一括対応、BUG-044 の `buildStringBuilder` フォローアップ検証中に誤って `buildString { ... }`（String を返す方）で同種のテストを書いてしまい、失敗して発覚）。今回修正できない理由: 全ての `String` 生成経路（flat string 生成・`toString()`・concatenation・`buildString` 等）を横断して type ID 登録を行き渡らせる設計と、`kk_op_is` の `CharSequence` 判定ロジック自体の見直し（`nominalBase` 単独ではなく `stringBase` 相当のケースへのフォールバックを追加する等）のどちらが正しい修正方針かの検討も含め、本 PR の StringBuilder 固有の修正スコープを大きく超えるため見送り（修正済み: `kk_op_is` の `nominalBase` 分岐に、object type ID を持たない `RuntimeStringBox` を `kotlin.String` として扱うフォールバックを追加し、`kotlin.String → kotlin.CharSequence` / `kotlin.Comparable` の supertype edge を Runtime 側で登録した。回帰テスト: `Tests/CompilerBackendTests/Codegen/CodegenBackendIntegrationTests+StringIsCharSequence.swift`、`Scripts/diff_cases/string_is_char_sequence_runtime_built.kt`）
- [x] BUG-154: `kotlin.text.CASE_INSENSITIVE_ORDER` が `HeaderHelpers+SyntheticStringStubs.swift`（`registerSyntheticStringTopLevelProperty`, STDLIB-TEXT-TYPE-004）により `kotlin.text` パッケージ直下のトップレベルプロパティとして登録されているが、実際の Kotlin では `String` の companion object メンバ（`String.CASE_INSENSITIVE_ORDER`）としてのみ存在し、トップレベル `kotlin.text.CASE_INSENSITIVE_ORDER` という名前は存在しない。最小再現: `import kotlin.text.CASE_INSENSITIVE_ORDER; fun main() { println(CASE_INSENSITIVE_ORDER) }` は kswiftc ではコンパイルが通るが、本家 kotlinc は `error: unresolved reference 'CASE_INSENSITIVE_ORDER'` で拒否する（`String.CASE_INSENSITIVE_ORDER` であれば通る）。発見元: BUG-036（`CASE_INSENSITIVE_ORDER` の参照同一性修正、PR #4994）の CI で `Scripts/diff_cases/case_insensitive_order_identity.kt` が kotlinc diff で失敗し判明。**修正済み**: 登録経路を top-level property（`registerSyntheticStringTopLevelProperty`、削除）から String companion object のメンバ property（`registerSyntheticCompanionExternalProperty`、`String.Companion.CASE_INSENSITIVE_ORDER`）へ変更。既存の object-parented synthetic external-property の global 初期化経路（`emitSyntheticTopLevelExternalPropertyInitializers` は `.object` 親も対象）＋ object-member read 経路（`tryLowerObjectMemberPropertyRead` → `loadGlobal`）にそのまま乗るため、`kk_string_case_insensitive_order()` は module init で 1 回だけ呼ばれ、単一 global により参照同一性が保たれる（BUG-036 のランタイムキャッシュも冗長ながら維持）。`registerSyntheticStringTopLevelProperty` は唯一の呼び出しだったため削除。回帰: `Scripts/diff_cases/case_insensitive_order_identity.kt` を `String.CASE_INSENSITIVE_ORDER` へ書き換え `SKIP-DIFF (DEBT-DIFF-005)` を解除、Sema link test（`testCaseInsensitiveOrderSurfaceResolves` 更新 + `testTopLevelCaseInsensitiveOrderIsUnresolved` 追加）、codegen edge test を `String.` 参照へ更新。
- [ ] BUG-155: source-backed な generic クラスの値フィールド（`T?`）に格納した非 null 値が、後続のメンバ呼び出しで正しく取り出せず既定値（`Int` なら `0`）になる。`AbstractIterator<T>` の State マシン（`setNext(value: T)` が `nextValue: T?` に格納 → `next()` が `nextValue as T` で取り出す）を素直に .kt 実装したところ、`hasNext()`/`next()` のロジック自体は正しく回るが、取り出された値が格納値ではなく `0` になる。最小再現: `import kotlin.collections.AbstractIterator` `class OneShot(private val v: Int) : AbstractIterator<Int>() { private var used = false; override fun computeNext() { if (used) done() else { used = true; setNext(v) } } }` `fun main() { val it = OneShot(42); while (it.hasNext()) println(it.next()) }` は `42` ではなく `0` を出力（`.build/debug/kswiftc` で実機確認）。発見元: KSP-664（AbstractIterator の .kt 化に伴い、ユーザー定義 AbstractIterator サブクラスが初めてコンパイル・実行到達可能になり発覚）。**回帰ではない**: 同コードは master（合成スタブ版 AbstractIterator）では `undefined reference to 'done'` / `undefined reference to 'setNext'` でリンク不能（合成殻に runtime body が無いため）だったので、リンク・実行到達自体が本 PR の前進。今回修正できない理由: generic 値のボックス化とフィールド（`T?`）への格納・取り出しを跨ぐ runtime/lowering 経路の見直しが必要で、KSP-664 のサーフェス移行スコープを大きく超える（`for` ループ等の実運用の反復は runtime ブリッジ経路を通り正常動作するため、この欠陥はユーザー定義 AbstractIterator サブクラスの実行時のみに限定される）
- [x] BUG-156: 「interface → override 実装を持つ抽象基底クラス → 具象クラス」の多段 generic 階層で、具象インスタンスを interface 静的型経由で呼ぶと、基底クラスが実装した interface メソッドの override が具象サブクラスの itable スロットに配線されず `KSWIFTK-RUNTIME-0001: Virtual dispatch failed: method not found in vtable/itable` でパニックする。最小再現: `interface Box<T> { fun get(): T }` `abstract class AbstractBox<T> : Box<T> { abstract fun compute(): T; override fun get(): T = compute() }` `class IntBox(val v: Int) : AbstractBox<Int>() { override fun compute(): Int = v }` `fun accept(b: Box<Int>): Int = b.get()` `fun main() { println(accept(IntBox(7))) }` は本ブランチではコンパイルは通るが実行時に上記パニック（`.build/debug/kswiftc` で実機確認）。具象クラスが interface を直接実装する単段ケース（`class IntBox : Box<Int>`）は正常に動作するため、欠陥は「基底クラスから継承した interface override」の itable 配線に限定される。発見元: KSP-664（`AbstractIterator<T> : Iterator<T>` の covariance 修正（`Inheritance.swift`）により本パターンが Sema を通過するようになり実行到達可能化）。**回帰ではない**: 同コードは master では `KSWIFTK-SEMA-OVERRIDE-RETURN: Override of 'get' has incompatible return type`（自クラス型パラメータを参照する supertype 型引数が解決されないことに起因する covariance 誤判定）で Sema 拒否され実行到達しない。 修正: 具象クラスの itable 配線を推移的 interface 集合 + 基底クラス鎖の override 探索に拡張（`KIRVtableRegistrationLowering.swift`、`CallLowerer.swift` の重複インライン実装を共通化）し、併せて暗黙レシーバ（`this` 修飾なし）のメンバ呼び出しを静的 call から virtualCall に修正（`AbstractBox.get` 内の `compute()` が abstract 実装へ静的束縛され 0 を返していた）。回帰: `CodegenBackendIntegrationTests` の 2 ケースと `Scripts/diff_cases/interface_inherited_from_abstract_base.kt`
- [x] BUG-049: `lazy()`/`lazy(mode)` の戻り値型が `kotlin.properties.Lazy`（`lazyOf` と本家は `kotlin.Lazy`）→ KSP-681 で修正。2026-07-23 に2セッションが並行して本項目に対応した。**セッションA（本 PR）の対応**: `lazy()`/`lazyOf()` 自体の関数シグネチャは既に `kotlin.Lazy`（`rootLazyInterfaceSymbol`）を返す実装だったが、`HeaderHelpers+SyntheticPropertyDelegateStubs.swift` の `registerSyntheticPropertyInterfaceStubs` が同時に登録していた未使用の `kotlin.properties.Lazy`（`legacyLazyInterfaceSymbol`/`legacyLazyInterfaceType`。宣言直後にしか使われず実質デッドコード）が `resolveTypeRef` の非修飾名フォールバック（`lookupByShortName` を `symbolID.rawValue` 昇順でソートし先頭を採用 — import 可視性を見ない）に混入し、明示的な `Lazy<T>` 型注釈を誤解決させていたと特定し削除。加えて `lazy { ... }` を `by` 経由でなく直接呼び出した戻り値の読み出しが高確率でクラッシュしていた別バグ（イニシャライザがクロージャ boxing 経路 `kk_function_create_0` で渡されるのに `RuntimeLazyBox.evaluateInitializer()` がコンテキスト無しの生関数ポインタとして直接呼び出していたため bad pointer dereference）も特定し、`kk_function_invoke_0` 経由に変更して修正。**セッションB（KSP-CAP-013, PR #4976, master 側で先にマージ済み）の対応**: 独立に (1) 呼び出し側の型注釈 `Lazy<Int>` 解決の問題と (2) `lazy(mode) { }` の2引数オーバーロードが `lazyModeSymbol` の旧fqNameによりデフォルトimportスコープから到達不能だった問題、の2点を発見・修正。**統合時の再検証（本 PR マージ作業中）**: セッションBの修正のみが適用された `origin/master`（本 PR の変更を含まない状態）で `val x: Lazy<Int> = lazyOf(99)`、`val direct: Lazy<Int> = lazy { 99 }`、`val x = lazy { 42 }; println(x.value)` の3ケースを実行したところ全て正しく動作することを確認 — つまりセッションBの修正だけで観測可能な症状は解消済みで、セッションAの `legacyLazyInterfaceSymbol` 削除自体は同一 HEAD 上では冗長（ただし依然として実プロダクトに存在しない型のデッドコードであり、将来的な混乱を避けるため削除自体は維持）。回帰テスト: `Tests/CompilerBackendTests/Codegen/CodegenBackendIntegrationTests+PropertyDelegateEdgeCases.swift` に両セッションのテスト（`testCodegenCompilesLazyBlockValueRead`/`testCodegenCompilesLazyBlockCapturingOuterVariable`/`testCodegenCompilesDirectLazyCallValueRead`/`testCodegenCompilesExplicitlyTypedLazyOfValueRead`）を統合。`Scripts/diff_cases/lazy_direct_call_and_explicit_type.kt` を追加。既存の `RuntimeDelegateTests`/`RuntimeDelegateEdgeCaseTests`/`DelegatePropertyKIRTests`（計 61 件）・Sema Golden 297件は green。**残課題（本 PR では未修正、BUG-150/151 として新規追跡。BUG-141/142 は既に別内容で使用中のため採番を避けた）**: (1) 関数ローカルスコープの `by lazy {}` は getValue を経由せず生ハンドルを返す別バグが残存（member/top-level スコープは正常、BUG-150）。(2) `Delegates.observable`/`vetoable`/`notNull` のメンバ委譲プロパティが広く壊れている（BUG-151）
- [x] BUG-050: `kotlin.text.CASE_INSENSITIVE_ORDER` 等の合成 top-level プロパティが、モジュール初期化時にキャッシュされるはずの global を読まず、参照のたびに `kk_string_case_insensitive_order()` を再実行して新規インスタンスを生成する（`--emit kir` で使用箇所ごとに独立した `call` が発行され、cached global への `loadGlobal` が起きないことを確認済み。値としては動作するが参照同一性が崩れる: `val a = CASE_INSENSITIVE_ORDER; val b = CASE_INSENSITIVE_ORDER` は同一インスタンスになるべき）— PR #4835 で発見。testKotlinTextCaseInsensitiveOrderEdgeCases の itable dispatch 障害報告の再現調査中に判明（**当該パニック自体は現行 HEAD `981b96169c` では再現せず**: `--emit kir` 上 dispatch=itable[0:0] がランタイム登録と一致、`swift_test.sh` で4回連続 pass 確認済み。原因は調査当時の Xcode ツールチェイン不一致の疑い — 本件はその副産物として見つかった別問題）。2026-07-23 バグバックログ一括対応セッションで修正: `Sources/Runtime/RuntimeComparator.swift` の `kk_string_case_insensitive_order()` にプロセス全体で共有する単一ハンドルのキャッシュ（`NSLock` 保護）を追加し、初回呼び出し時のみ `RuntimeCaseInsensitiveStringComparatorBox` を生成・登録して以後は同一ハンドルを返すよう変更。テスト分離用の `kk_runtime_force_reset()`（`kk_runtime_reset_gc()`）実行時にキャッシュが指す GC ハンドルが無効化されるため、`resetCaseInsensitiveOrderCache()` を追加し `kk_runtime_reset_gc()` から呼び出すことでキャッシュも同時にクリアされるようにした。回帰テスト: `CodegenBackendIntegrationTests+KotlinTextEdgeCases.swift` に `testKotlinTextCaseInsensitiveOrderIsReferentiallyStable` を追加、`Scripts/diff_cases/case_insensitive_order_identity.kt` を追加。`.build/debug/kswiftc` で `a === b` が `true` を返すことを確認
- [x] BUG-037: interface 型オペランド同士の `===`/`!==` が Sema 型チェックを通らず `KSWIFTK-TYPE-0001: Type constraint could not be satisfied` になる（具象クラスが実装した interface 型の変数同士でも再現。最小再現: `interface Foo { fun bar(): Int }` `class FooImpl : Foo { override fun bar() = 1 }` `fun main() { val a: Foo = FooImpl(); val b: Foo = a; val same: Boolean = (a === b) }`）— PR #4835 で発見。BUG-020 の再現性検証中に偶然発見。2026-07-23 バグバックログ一括対応セッションで再確認: 現行 HEAD の `ExprTypeChecker+BinaryAndFlowInference.swift` (`inferBinaryExpr`) は `op == .identityEqual || op == .notIdentityEqual` を演算子候補解決より前に処理し、無条件に `Boolean` を返す実装に修正済み（コメントにも「`===`/`!==` は演算子解決を完全にバイパスする」旨明記）。上記最小再現を `.build/debug/kswiftc` で実行し `true` を正しく出力することを確認。修正 PR/コミットの特定はできなかったが（このリポジトリの git 履歴は本セッション開始時点で単一の初期コミットに圧縮されており `git blame` によるアーカイオロジーは不可能）、コードは既に正しい
- [x] BUG-038: PR #4636 の CI で判明した runtime 表示/例外 message の不整合。`kk_println_any(kk_box_char(0xDF1F))` と `println("hello🌟".lastOrNull())` が `?` ではなく U+FFFD を出力し、`(42 as String)` の `ClassCastException.message` が空になる — 再現: `Tests/RuntimeTests/RuntimePrintlnTests.swift`、`Tests/CompilerBackendTests/Codegen/CodegenBackendIntegrationTests+StringHOFEdgeCases.swift`、`Tests/CompilerBackendTests/Codegen/CodegenBackendIntegrationTests+InlineFunctionExceptionPropagation.swift`。修正 PR: #4636（2026-07-15 マージ済み。2026-07-23 バグバックログ一括対応セッションで GitHub 上のマージ状態を確認し `[x]` 化）
- [x] BUG-039: master CI（`Full Swift Tests (RuntimeTests)` ジョブ）が2026-07-15〜16に直近4回中3回失敗。原因は BUG-021/023/024/028 系と同根で、`kk_runtime_force_reset()` 等が書き換える `Sources/Runtime/RuntimeGC.swift` のプロセス全体共有ハンドルテーブル（`runtimeStorage.objectPointers` 等）に対し、XCTest 時代の `RuntimeTestIsolationSupport.swift`（`gcSemaphore` 等のプロセス全体セマフォ、`IsolatedRuntimeXCTestCase.setUp/tearDown` 経由）が提供していたクロスファイル排他が、Swift Testing の `@Suite` では構造的に再現できない（`XCTestCase` サブクラス専用の仕組みで Swift Testing の struct suite からは使えない）。`@Suite(.serialized)` はスイート内のみを直列化し、他ファイルの並行スイートとは排他されないため、あるスイートの reset が別スイートの生存中ハンドルを消し飛ばし、`invalid range/array/string handle` パニックや `Set`/`Array` 内容破損（解放先アドレスの再利用によるものと推定）を引き起こす。対象確認: `Tests/RuntimeTests/RuntimeSynchronizedTests.swift`（PR #4823 で移行済み・`init()` と各 `@Test` の `defer` で `kk_runtime_force_reset()` を正しく呼び `.serialized` も付与済みだが、cross-suite 排他が無いため依然として攻撃源になり得ることを示す新規ケース。BUG-020〜035 では未記載）。既存の BUG-023/024/028 も現行 master 上で確認済みの同根の実攻撃源（詳細は各項目参照）。最小再現: `DEVELOPER_DIR=/Applications/Xcode-beta.app SWIFT_TEST_WORKERS=4 bash Scripts/swift_test.sh --filter '^RuntimeTests\.' -Xswiftc -swift-version -Xswiftc 6` をローカルで数回実行（`SWIFT_TEST_PARALLEL` 未設定＝デフォルト `--parallel --num-workers 4`）。発見元: 本 master CI 失敗調査タスク（2026-07-16）。暫定対応: `.github/workflows/ci.yml` の `full-swift-tests` ジョブで `RuntimeTests` マトリクスエントリのみ `SWIFT_TEST_PARALLEL: "0"` を設定し直列実行に固定（実測 196 tests / 0.235 秒、タイムアウト無関係）。根本対応基盤として、既存のプロセス全体セマフォを Swift Testing の各 test case に適用する `RuntimeIsolationTrait` を PR #4908 で追加し、対象ファイルの段階移行を開始。暫定緩和 PR: #4846 → **この緩和が BUG-040 を誘発した（`--no-parallel` 化で exec() 引数長制限に抵触）**。2026-07-23 バグバックログ一括対応セッションで現行 HEAD を再確認: BUG-040 の対応（`RuntimeTests` 独立ジョブ化 + 常時チャンク化）が適用済みで master CI の当該失敗（本項目の直接の追跡対象）は解消している。根本対応基盤の `RuntimeIsolationTrait`（PR #4908）も merge 済みで、共有の process-wide semaphore を `IsolatedRuntimeXCTestCase`（XCTest 経由、37ファイル）・`.runtimeIsolation()` trait（Swift Testing、31ファイル）・`RuntimeTestIsolationLease` 直接使用（Swift Testing、`RuntimeSynchronizedTests.swift` を含む）の3経路で共有し相互排他する設計になっている（本項目が個別に名指ししていた `RuntimeSynchronizedTests.swift` は現在 `RuntimeTestIsolationLease(lockSet: .all)` を各 `@Test` で使用しており、記載時点の「cross-suite 排他が無い」という状態から変わっている）。また `RuntimeSwiftTestingIsolationLintTests.testSwiftTestingSuitesDoNotResetGlobalRuntimeState` が全 Swift Testing ファイルを走査し `kk_runtime_force_reset`/`kk_system_gc` 等の直接呼び出しを禁止するハード gate として追加されており、新規の同種バグを機械的に防止している。**残課題**: `@Suite` を持つ 103 ファイルのうち 72 ファイルは `RuntimeIsolationTrait`/`IsolatedRuntimeXCTestCase` のいずれも使っておらず（lint はリセット系 API の直接呼び出しのみを検出するため、共有可変状態に触れるが reset は呼ばないファイルは lint をすり抜け得る）、簡易監査ではそのうち 18 ファイルが `registerRuntimeObject`/`kk_object_new` 等で GC 管理オブジェクトを生成している。CI 側の症状は解消済みだが、166 ファイル全件の意味的監査・migration は本セッションのスコープを超えるため未実施
- [x] BUG-137: 初期化子付き top-level `var` の custom getter 読み出しが getter を経由せず property global を直接読む（最小再現: `Scripts/diff_cases/top_level_backing_field.kt`、発見元: PR #4684 の CI）。修正: PR #4684（2026-07-19 マージ済み。2026-07-23 バグバックログ一括対応セッションで GitHub 上のマージ状態を確認し `[x]` 化）
- [x] BUG-138: クラスメンバの custom accessor が暗黙 receiver の setter dispatch と backing-field のインスタンス storage を迂回する（`implicit_receiver_setter_assign.kt` は setter が bypass され `10/1012` ではなく `5/4`、`custom_getter_field_access.kt` は getter 内 `field` と class-body initializer が global 経路になり `10/105/205/305` ではなく `10/5/5/5`）。最小再現: `Scripts/diff_cases/implicit_receiver_setter_assign.kt` / `Scripts/diff_cases/custom_getter_field_access.kt`。発見元: PR #4680 CI 修理。修正: PR #4680（記載時点で未マージだったが 2026-07-17 にマージ済み。2026-07-23 バグバックログ一括対応セッションで GitHub 上のマージ状態を確認し `[x]` 化）
- [x] BUG-040: BUG-039 の暫定緩和 PR #4846（`RuntimeTests` マトリクスエントリに `SWIFT_TEST_PARALLEL: "0"` を設定）が master CI に新規リグレッションを誘発。`swift test --no-parallel --filter '^RuntimeTests\.'` は `--parallel` 時と異なりマッチした全 XCTest ID（RuntimeTests は ABIMismatchTests 等を含め約3000件超）を **1つの exec() 引数** として `KSwiftKPackageTests.xctest` に直接渡す経路を通るため、Linux の per-argument exec() 制限（`MAX_ARG_STRLEN` ≈128KB）を超え `error: posix_spawn error: Argument list too long (7)` でテスト実行前に落ちる。Swift Testing 側のテスト（196件）はこのエラーの後に正常に走り切って `passed` と表示されるため、ログ末尾だけを見ると成功に見えるが、ジョブ全体の exit code は 1 のまま。macOS では `ARG_MAX` が大きく再現しないため、`--no-parallel` はローカル green でも Linux CI で落ちうる（PR #4846 バリデーションはこの経路を踏んでいなかった）。再現: master commit `a28cd1cf13`（PR #4846 マージ後）以降の全 `Full Swift Tests (RuntimeTests)` run（例: Actions run 29479751122 / 29416122834 / 29405211032）。発見元: 本 master CI 失敗調査タスク（2026-07-17）。対応: `RuntimeTests` を独立ジョブ `full-swift-tests-runtime` に切り出し、`Scripts/shard_swift_tests.sh` の `--mode static`（ソースから suite/class 名を抽出し `--target-prefix\.(Type1|Type2|...)(/|$)` を 50 件ずつチャンク化）経由で実行するよう変更、`SWIFT_TEST_PARALLEL=0` は維持。`shard_swift_tests.sh` 側の `shard_count <= 1` 早期リターン（チャンク化をバイパスして生の `--filter` を投げていた）も削除し、単一シャード実行でも常にチャンク化されるようにした。ローカルでチャンク生成をドライラン確認済み（187 suite/class → 50件ずつ4チャンク、各チャンクのフィルタ長は数KB程度で制限を十分下回る）。2026-07-23 バグバックログ一括対応セッションで現行 HEAD を再確認: `.github/workflows/ci.yml` は `RuntimeTests` を `Scripts/shard_swift_tests.sh --mode static`（`SWIFT_TEST_PARALLEL: "0"`）経由の独立ステップで実行しており記載の対応が適用済み。`Scripts/shard_swift_tests.sh` 本体も `shard_count` の値によらず常に `chunk_alternations` でチャンク化する実装（139-147行目のコメントで「単一シャードでも常にチャンク化」と明記）になっており、`shard_count <= 1` 早期リターンは残っていないことをソース確認した
- [x] BUG-041: `launch { }` の子コルーチン本体が、直後（介在する suspension point なし）の同期的な `cancelJob.cancel()` より先に実行を開始してしまうことがある、`kk_kxmini_launch`（`Sources/Runtime/RuntimeCoroutine.swift`）の起動-キャンセル競合。原因: `KxMiniRuntime.launch(workItem:)` が `DispatchQueue.global()`（真の並行スレッドプール）へ即座に dispatch するため、子スレッド側 `job.markStarted()` と親スレッド側 `job.cancel()` の `NSLock` 獲得順序に確定的な happens-before 保証がない。本家 kotlinx.coroutines は `runBlocking` 直下の `launch` を単一スレッドの協調的 event loop で動かすため、suspension point を跨がない `cancel()` は子の実行開始を確実に防げるが、本実装は実スレッド並行に依存しており、子スレッドが `markStarted()`→`cancellationSnapshot()` チェックを親の `cancel()` より先に通過すると本体が走り出し、`delay()` 到達後に協調的キャンセルで中断されて期待外の出力（例: catch節の "cancelled"）が漏れる。再現: `Scripts/diff_cases/coroutine_cancellation_edge_cases.kt` / `Tests/CompilerBackendTests/Codegen/CodegenBackendIntegrationTests+CoroutineCancellationEdgeCases.swift` の `testCodegenCompilesCoroutineCancellationEdgeCases`（PR #4830 で追加）。CI高負荷時に発現（本件は PR #4578 の `CompilerBackendTests shard 3/6`、`Actions run 29439186351/job 87460299566` で観測）。頻度は未確定 — ローカルでの繰り返し再現を試みたが、本開発機で他セッションの並行ビルドが多数走っていたことによるリソース競合とみられる無関係な `LinkPhase.swift:109` の `"outputUnavailable"` エラーに阻まれ、対象のレース自体をクリーンに再現できなかった（1回だけ `cancelJob.join()` 待ちとみられる無限待機/0%CPU sleepingも観測したが、上記のリソース競合下での観測のため本バグと確定できない）。低負荷環境での再現率は改めて要検証。同種バグは別セッションでも独立発見・BUG-039 として記録されていたが（`Log coroutine cancel-before-start timing race as BUG-039`, commit `1f925b36d4`）、その後 master 側で BUG-039/040 が全く別内容（RuntimeTests CI の cross-suite GC race / exec引数長制限）で確定したため番号が競合し、本項目として BUG-041 に採番し直した。発見元: PR #4578 の CI 診断中（#4830 自体の変更ではなく `kk_kxmini_launch` の既存 dispatch モデルに起因する既存バグ）。2026-07-23 バグバックログ一括対応セッションで現行 HEAD を再確認: `kk_kxmini_launch` は `DispatchQueue.global()` への即時 dispatch をやめ、`RuntimeCoroutineBurstDepth` が active な間は `RuntimePendingLaunchQueue` へキューイングし、同スレッドの次の `flush()`（suspension point 到達時等）まで実ディスパッチを遅延する実装に変更済み（`Sources/Runtime/RuntimeCoroutine.swift` の `RuntimePendingLaunchQueue` 定義コメントに「本バグの race を構造的に不可能にする」旨明記）。`flush()` は pending 中に `cancellationSnapshot()` が真になったジョブを dispatch せず捨てるため、`launch{}` 直後の同期的 `cancel()` は常に「まだ未ディスパッチ」の状態を観測する。回帰テストは `Tests/RuntimeTests/RuntimeChannelTests.swift` の「BUG-041 × channel blocking interaction」節に存在
- [x] BUG-051: ローカル環境（`DEVELOPER_DIR=/Applications/Xcode-beta.app` のツールチェイン）で `swift build --target Runtime --scratch-path .runtime-build/...`（`CodegenRuntimeSupport+RuntimeObjects.swift` の `buildRuntimeObjects`）が per-file `.swift.o` を格納する `Runtime.build/` ディレクトリではなく単一の WMO 統合 `Runtime.o` を生成する。`discoverScratchRuntimeObjectPaths` は `Runtime.build` ディレクトリ名固定探索のみで単一 `.o` へのフォールバックが無いため、ランタイムオブジェクトを発見できず `KSWIFTK-LINK-0001: Unable to locate packaged runtime object files under .../debug/Runtime.build` でリンク失敗する。最小再現: `bash Scripts/diff_kotlinc.sh Scripts/diff_cases/hello.kt`（任意のケースで再現）、および `swift test --filter testLLVMBackendCanLinkAndRunExecutable`（実行ファイルを実際にリンクする唯一の統合テスト、`LinkPhase.swift:109` で `outputUnavailable` 発生）。フロントエンド/KIR 止まりのテスト（SmokeTests 等）はリンク経路を通らないため影響を受けない — PR #4572 のコンフリクト解消作業（`claude/pr-4572-conflict-f4f7ec`）中、`diff_kotlinc.sh` によるマージ後検証で発見。`CodegenRuntimeSupport+RuntimeObjects.swift`/`LinkPhase.swift` は当該マージで一切変更されておらず、同一 HEAD で `swift build` 本体および StringBuilder/vararg boxing/Golden(Sema+Diagnostics) の500件超のテストは green のため、コード側の回帰ではなくローカルツールチェインの成果物レイアウト差異（WMO）に起因する可能性が高い。BUG-036 の「調査当時の Xcode ツールチェイン不一致の疑い」と同系統の環境要因の可能性あり。上記 BUG-041 の「本開発機で他セッションの並行ビルドが多数走っていたことによるリソース競合とみられる `LinkPhase.swift:109` の `outputUnavailable`」もこの同一事象の可能性が高い。2026-07-23 バグバックログ一括対応セッションでの追加調査: クリーンな Linux + Swift 6.3.3 ツールチェイン（Xcode-beta 不使用）で `swift build --target Runtime --scratch-path <tmp> -Xswiftc -whole-module-optimization` を実行し WMO を強制したところ、SwiftPM は WMO 下でも `Runtime.build/` ディレクトリ自体は変わらず作成することを確認した（`output-file-map.json`/`Runtime.dia`/`include/` が生成される）。このビルド自体は本ツールチェイン固有の無関係なコンパイラエラー（`index output filenames do not match input source files`）で完走しなかったため、WMO 時に `Runtime.build/` 直下へ単一 `.o` が実際に置かれるか、または全く別の場所に置かれるかまでは確認できなかった。通常（非WMO）ビルドではこの環境で `Runtime.build/` 配下に per-file `.o`（83ファイル）が生成され、`kswiftc` によるコンパイル・リンク・実行は本セッションの BUG-017/036 修正の検証を含む多数のケースで問題なく成功した。**結論**: 本セッションの環境では再現しない。`discoverScratchRuntimeObjectPaths` は `Runtime.build` に加え `Runtime-t.build`（新しい XCBuild エンジンのレイアウト）も探索し、`Objects-normal/<arch>/*.o` のようにネストしたパスも再帰的に収集する実装に強化されており、記載時点よりレイアウト差異への耐性は上がっている（ただし `Runtime.build`/`Runtime-t.build` いずれのディレクトリ名にも属さない、真にフラットな単一 `.o` ファイルへのフォールバックは依然として存在しない）。オリジナルの報告自体も「環境要因の可能性が高い」と留保付きだったこと、BUG-041 の "outputUnavailable" も同一のリソース競合起因と推定されていたことを踏まえ、コード側の追加修正は行わず本項目は環境確認のみに留める（`[ ]` のまま維持: 未修正のまま閉じるべきではない一方、再現不能で修正対象コードも特定できないため）。**2026-07-27 修正**: 再現不能でオリジナル報告は環境要因の疑いと留保付きだったが、記載の欠陥（`Runtime.build`/`Runtime-t.build` いずれのディレクトリにも属さないフラットな単一 WMO `.o` へのフォールバックが無い）自体は実在するため、防御的にフォールバックを追加した。`CodegenRuntimeSupport+RuntimeObjects.swift` の `discoverScratchRuntimeObjectPaths` に `discoverWholeModuleRuntimeObjectPaths(nearBuildDirectory:)` を追加し、per-file/products-dir 探索が空のとき `Runtime.build` の親（`debug`）ディレクトリ直下の単一 `Runtime.o`/`Runtime.swift.o` を拾う。SwiftPM が `Modules/Runtime.o` に出す AST-wrapper（`nm` で `__Swift_AST` のみ・実コードなし）や別ターゲットの `RuntimeABI.o` を誤検出しないよう、探索は親ディレクトリ直下（非再帰）かつ厳密なファイル名一致に限定。回帰テスト `Tests/CompilerBackendTests/Codegen/CodegenRuntimeObjectDiscoveryTests.swift`（5件、合成レイアウトで per-file 優先・WMO フォールバック・Modules AST-wrapper 除外を固定）を追加。`swift build` green、新規5件 + 既存 `LinkPhaseIntegrationTests` 15件 green。WMO の実レイアウトは本 Linux/Swift 6.2.4 ツールチェインでは再現不能（`-whole-module-optimization` 強制時に `index output filenames do not match input source files` で完走せず、報告時と同じ制約）のため、フォールバックのロジックは合成レイアウトによるユニットテストで検証している
- [x] BUG-052: 関数ローカルスコープの `val x by lazy { ... }` は getter が委譲先の `getValue`（`kk_lazy_get_value`）を呼ばず、delegate box の生ハンドルをそのまま返す（例: `println(x)` が `42` ではなく `<object 0x...>` を出力）。member/top-level スコープの `by lazy` は正しく `kk_lazy_get_value` を呼ぶ実装（`MemberLowerer+DelegatedAndAccessorLowering.swift` の `lowerDelegateAccessor`）が使われ正常動作するため、ローカル変数の delegated property だけが別の（未特定の）lowering 経路を通っていると見られる。最小再現: `fun main() { val x by lazy { 42 }; println(x) }`。発見元: 本セッション（バグバックログ一括対応、BUG-017 の runtime crash 調査中に偶然発見）。番号は本セッション記載時点で未使用だった 150 を採番（BUG-141/142 は同日付で master に別内容が独立して着地したため衝突を避けた）。今回修正できない理由: ローカル変数 `by` construct の lowering 経路の特定・修正は別途調査が必要な規模で、本セッションの残り対応項目（他の未修正バックログ）を圧迫するため見送り
- [x] BUG-151: `Delegates.observable(...)`/`Delegates.vetoable(...)`/`Delegates.notNull()` をクラスメンバの委譲に使うと広く壊れている。(a) observable/vetoable の3引数コールバックラムダ（`(property, old, new) -> Unit`/`Boolean`）は KIR lowering で本体の statement が丸ごと失われ、3パラメータを materialize して即座に `unit` を返すだけの空関数になる（`--emit kir` で `kk_delegate_lambda_NNNN` の body を確認: `const` でパラメータを束縛した後 `return r26=unit` のみで、元のラムダ内の `println`/比較式が存在しない）。結果、observable は値の変更もコールバック呼び出しも観測できず（`u.name = "hello"` 後も `println(u.name)` が初期値のまま）、vetoable はコールバックが常に falsy を返すため全ての変更を拒否する（`c.value = 5` 等が反映されず初期値のまま）。(b) `Delegates.notNull()` はコールバックを取らないため上記とは別原因と見られるが、setter 呼び出し後でも getter が `IllegalStateException: Property delegate must be assigned before being accessed` で fatal crash する（`kk_notNull_get_value`, `Sources/Runtime/RuntimeDelegates.swift:392`）。getter/setter 自体のアクセサ配線（`kk_observable_get_value`/`kk_vetoable_get_value`/`kk_notNull_get_value` 等の呼び出し）は KIR 上正しく見えるため、(a) はコールバックラムダ本体の lowering 段階、(b) は別の setter 配線または「代入済みフラグ」管理の問題と推定される。最小再現: `import kotlin.properties.Delegates` `class User { var name: String by Delegates.observable("initial") { _, old, new -> println("changed from $old to $new") } }` `fun main() { val u = User(); u.name = "hello"; println(u.name) }`（期待値: `changed from initial to hello` の後 `hello`、実際: `initial` のみ）。vetoable/notNull も同様の形（`kotlin.properties.Delegates` の各 factory を使うクラスメンバ委譲）で再現。発見元: 本セッション（バグバックログ一括対応、BUG-017 の runtime crash 調査の一環で lazy 以外の stdlib delegate も検証し発見）。今回修正できない理由: (a) はどの lowering pass がコールバック body を落としているかの特定から必要で、(b) は (a) とは別原因の可能性が高く、単一の小さな修正では閉じられない規模の調査が必要なため見送り
- [x] DEADCODE-RUNTIME-001: [E0] `RuntimeStringArray.swift:2120` の `kk_readln_from_syscall(_:)` を削除し、`RuntimeABISpec+IO.swift:28` の spec も消す。現行 IO は `kk_readline` / `kk_readln` / `kk_readlnOrNull`
- [x] DEADCODE-RUNTIME-007: [E0] `RuntimeStringArray.swift:2113` の `kk_sys_write(_:_:_:)` を削除し、`RuntimeABISpec+System.swift:97` も消す。console IO は Swift `print` / `readLine` 経路
- [x] BUG-157: `Float.toString()`（および `Float` 値の `println`）が特定の値で本家 kotlinc より有効数字を1桁少なく出力する。kotlinc は Float の最短往復（shortest round-trip）表現を出力するが、kswiftc の Float→String 変換はそれより短い（往復しない）表現を出す。最小再現: `Scripts/diff_cases/num_float_tostring.kt`（`1.23456792E8` が kswiftc では `1.2345679E8` になる）と `Scripts/diff_cases/num_conversions.kt`（`Int.MAX_VALUE.toFloat()` の `2.14748365E9` が kswiftc では `2.1474836E9` になる）。いずれも master（commit `c591d53dc` / `43419eb0b`）でも同一の diff で FAIL することを確認済み（本 PR の変更とは無関係な既存バグ）。原因は Runtime の Float→String 整形（`Sources/Runtime` の数値フォーマット経路。`Double` 側は正しく最短表現を出しているため Float 専用パスの精度不足の疑い）。発見元: KSP-657（Array ファクトリ Kotlin 化）の `Scripts/diff_kotlinc.sh Scripts/diff_cases` 完了ゲート実行中に検出。今回修正できない理由: Float の shortest round-trip 整形アルゴリズム（Ryū / Grisu 相当）を Runtime の数値フォーマット層に実装/修正する必要があり、Array ファクトリ移行という本 PR のスコープ（compiler フロントエンド + stdlib 宣言）とは別サブシステムで、単一の小さな修正に収まらないため見送り。2026-07-27 調査結果: **kswiftc 側のバグではなかった**。JDK 19 で `Double`/`Float.toString()` が最短往復表現を返すよう変更された（JDK-4511638）ため、報告された「本家の期待値」`1.23456792E8` / `2.14748365E9` は JDK 18 以前（旧 `FloatingDecimal`）で diff ゲートを回したときの出力。CI が pin している JDK 21 では kotlinc も `1.2345679E8` / `2.1474836E9` を出力し、kswiftc と一致する（ローカルで JDK 21 + Kotlin 2.3.10 により `num_float_tostring.kt` / `num_conversions.kt` の両ケースが PASS することを確認）。対応: 最短往復表現を固定する回帰テスト（`RuntimeFloatingPointToStringTests.testFloatShortestRoundTrip`）を追加し、`Scripts/diff_kotlinc.sh` に JDK 21 未満を弾くガードを追加して同種の誤検知を防止した

- [x] BUG-158: プリミティブ配列（`ByteArray`/`IntArray`/`LongArray` 等）の `joinToString` に `transform` オーバーロードが無く、末尾ラムダ付きで呼ぶと transform が黙って捨てられる。`HeaderHelpers+SyntheticArrayStubs.swift` はプリミティブ配列に対しては `joinToString(separator, prefix, postfix)`（3引数版、`kk_*Array_joinToString`）のみを登録し、`transform: ((T) -> CharSequence)?` を含むオーバーロードを登録していない（generic `Array<T>` には `kk_array_joinToString_transform` が line 2032 付近で登録済みだが、プリミティブ配列には無い）。そのため末尾ラムダ付き呼び出しは 3引数版へ解決され、ラムダが無視される。最小再現: `fun main() { val a = byteArrayOf(10, 20, 30); println(a.joinToString(",") { (it + 1).toString() }) }` は `10,20,30` を出力する（本家 kotlinc は `11,21,31`）。`IntArray`/`LongArray` 等でも同様。発見元: KSP-660（BUG-019 の `ByteArray.joinToString`/`contentEquals` 吸収作業中に、`transform` 付き `ByteArray.joinToString` の挙動を検証して発覚）。BUG-019 の定義スコープ（`Scripts/diff_cases/string_tobytearray.kt` の `joinToString(sep)`/`contentEquals` ギャップ、#4671 で解消済み）には `transform` は含まれない。今回修正できない理由: 全プリミティブ配列型（12 種）横断で `joinToString(..., transform)` オーバーロードの Sema 登録・KIR lowering・runtime helper（または Kotlin 実装への移行）を一貫して追加する必要があり、本 PR の符号なしビュー変換移行スコープを大きく超えるため見送り（解消: `HeaderHelpers+SyntheticArrayStubs.swift` にプリミティブ配列用 `joinToString(..., transform)` オーバーロード 4 種を登録し、`collectionFallbackElementType` がプリミティブ配列の要素型を `Any` に潰していた問題も修正。回帰ケース `Scripts/diff_cases/primitive_array_join_to_string_transform.kt`）
- [x] BUG-159: トップレベル宣言のシグネチャ（関数の値パラメータ型・戻り型・レシーバ型、プロパティ型）が `HeaderCollection.swift` の `collectHeader` 内で `resolveTypeRef` によりファイル単位で**インライン解決**され、`collectAllHeaders` は `ast.sortedFiles`（fileID 昇順）順にファイルを処理するため、あるファイルが後続 fileID のファイルで宣言された型を前方参照すると `KSWIFTK-SEMA-0025: Unresolved type` になる。ドライバは `LoadSourcesPhase` が bundled stdlib を先に注入する（＝低い fileID）ため bundled 型は解決できるが、ユーザーファイル間の前方参照はファイル指定順に依存して壊れる。最小再現: `printf 'fun useB(x: B?): Int = 0\n' > a.kt; printf 'class B\n' > b.kt` としたうえで `kswiftc a.kt b.kt`（→ `Unresolved type 'B'`）は失敗、`kswiftc b.kt a.kt` は成功する。発見元: KSP-668（`kotlin.experimental` 注釈の .kt 化）で source-backed 注釈クラスがテストハーネスのファイル追加順（ユーザーソースを先に addFile）で解決できなくなった調査中に判明。KSP-668 側は `collectAllHeaders` で bundled/residual stdlib ファイルをユーザーファイルより先に処理する順序修正で対応済み（source-backed stdlib 解決を fileID 割り当て順に非依存化）だが、ユーザー間前方参照の一般修正はヘッダ収集をシンボル定義パスとシグネチャ解決パスに分離する二段階化（全ファイルの top-level 宣言シンボルを登録してからシグネチャを解決）が必要で、Sema コアの構造変更を伴い KSP-668 の安全なスコープを超えるため見送り。**完了（2026-07-29）**: BUG-143 修正（`collectAllHeaders` の `predeclareNominalTypeHeaders` による top-level nominal 型の事前宣言と typealias 先行収集）でファイル間の前方参照も解決されるようになったため、本項はその一般化として解消。回帰テスト `Tests/CompilerCoreTests/Sema/CrossFileForwardReferenceSemaTests.swift`（両方のファイル順での関数シグネチャ・クラスメンバ・typealias の前方参照、および重複宣言診断の維持）を追加して最小再現を固定
- [ ] BUG-167: 静的型がインターフェース型 `Iterable<T>`（および同様に `Collection<T>` など、具象コレクション型ではなくインターフェース型）の値に対する `for (x in it) { ... }` が、実体（例: `List`）の本来の `iterator()`（itable ディスパッチ）ではなく **range イテレータ intrinsics**（`kk_range_iterator` / `kk_range_hasNext` / `kk_range_next`）へ誤ルーティングされ、コレクションオブジェクトを range として再解釈して不正な要素（ゴミ index）を返す。`ControlFlowLowerer.lowerForExpr` は `loopIterationBinding` が無くかつ array-like でない iterable を range intrinsics にフォールバックさせるが、`Iterable`/`Collection` インターフェース型は `bindLoopIterationOperators` で実 `iterator()` バインディングが生成されない（`List<Int>` 等の具象型は生成されるため正常）。DEBT-KIR-005（Array のフォールバック誤ルーティングを `lowerArrayForExpr` で回避した件）と同系統。最小再現: `fun f(xs: Iterable<Int>) { for (x in xs) println(x) }; fun main() { f(listOf(0, 2, 4)) }` は各要素が 0 相当のゴミになる（明示イテレータ `val it = xs.iterator(); while (it.hasNext()) { val x = it.next(); ... }` は正常に `0 2 4` を出力）。発見元: KSP-406（substring/slice/range 系の Kotlin 化）。`String.slice(indices: Iterable<Int>)` の source 実装で `for (index in indices)` が不正 index を返し、明示イテレータ経由に切り替えて回避済み（`StringSubstringSlice.kt` のコメント参照）。今回修正できない理由: インターフェース型 iterable に対する実 `iterator()` バインディング生成は Sema（`ControlFlowTypeChecker.bindLoopIterationOperators`）と KIR（`ControlFlowLowerer` の for-loop lowering）双方にまたがるループ意味論の変更であり、全 for-loop に影響しうる。substring/slice PR の安全なスコープを超えるため本項で追跡する
- [x] BUG-160: `Double`/`Float` の `compareTo` が符号付きゼロの total order を実装しておらず、`(-0.0).compareTo(0.0)` が `0` を返していた（本家 kotlinc は `-1`）。`runtimeCompareFloatingValues`（`Sources/Runtime/RuntimeCollectionHelpers.swift`）が IEEE 等価（`-0.0 == 0.0`）で `0` を返していたのが原因で、符号ビットを見て `-0.0 < 0.0` とする total order に修正。回帰: `Tests/RuntimeTests/RuntimePrimitiveCompareToSignedZeroTests.swift`、`Scripts/diff_cases/double_signed_zero_compare.kt`
- [ ] BUG-162: ボックス化された `Double` の `-0.0` が null と区別できない。`runtimeNullSentinelInt64 = Int64.min`（`Sources/Runtime/RuntimeHelpers.swift:241`）は `-0.0` の IEEE754 ビットパターン `0x8000_0000_0000_0000` と同一のため、raw value 経路を通る `-0.0` が null 扱いになる。最小再現: `fun main() { println(listOf(0.0, -0.0, 2.0).joinToString(",")) }` は kswiftc で `0.0,null,2.0`（本家 kotlinc は `0.0,-0.0,2.0`）。`DoubleArray` などフラット表現の経路は影響を受けない。発見元: BUG-160（符号付きゼロ compareTo 修正）の diff ケース作成中に `listOf(0.0, -0.0, ...).sorted()` で判明（当該 diff ケースからは boxed リストの行を除外済み）。今回修正できない理由: null センチネル表現の変更（別ビットパターンの採用、または boxed Double の常時ヒープ化）はランタイム全体の raw value ABI に波及し、BUG-160 の compareTo 修正スコープを大きく超えるため
  ```kotlin
  class V(val n: Int) : Comparable<V> { override fun compareTo(other: V): Int = n.compareTo(other.n) }
  fun main() { println(listOf(V(2), V(1)).sorted().map { it.n }) }
  ```
  発見元: KSP-669（Comparable/RandomAccess の interface 宣言 .kt 化）の回帰ケース追加中（当初 BUG-155 で採番したが、master 側に同番号が先着し続けたため 161 に再採番）。**master（本変更なし）でも同一バックトレースで再現する既存バグ**であり、KSP-669 の宣言移設（Sema のシンボル登録）とは無関係な runtime/ABI 層の欠陥。今回修正できない理由: `runtimeCompareValues` からユーザー `compareTo` を呼ぶ際の値ボクシング／関数ポインタ ABI の是正が必要で、`kk_list_sorted` 以外の比較経路（`sortedBy`/`max`/`min`/`Comparator` 系）への波及調査を含むため、宣言移設 PR の安全なスコープを超える。回帰ケース `Scripts/diff_cases/ksp669_comparable_randomaccess.kt` は本クラッシュ行を除外して追加済み
- [ ] BUG-162: 静的型が `KProperty0/1`・`KMutableProperty0/1`（`obj::prop` / `Class::prop` 由来）のプロパティ参照に対しアクセサ（`.name` / `.get(...)` / `.invoke(...)` / `.set(...)`）を呼ぶと実行時にクラッシュする。`.name` は `Runtime/RuntimeDelegates.swift` の `kk_kproperty_stub_name` が `invalid KProperty handle` で `Fatal error`、`.get()`/`.invoke()` は `KSWIFTK-RUNTIME-0001: Virtual dispatch failed: method not found in vtable/itable`。参照の生成・アップキャスト（`val r: KProperty<*> = obj::prop`、`KMutableProperty0<Int>` → `KProperty<Int>` 等）自体は正常で、アクセサ呼び出し時のみ発生する。最小再現: `class Person(val name: String, var age: Int); fun main() { val p = Person("A", 1); val r: kotlin.reflect.KProperty0<String> = p::name; println(r.name) }` が `invalid KProperty handle` でクラッシュ（`Scripts/diff_cases/kproperty_basic.kt` のようにアクセサを呼ばず `KProperty<*>` として捕捉するだけなら正常）。発見元: KSP-682（KProperty 殻の .kt 化）。`Sources/CompilerCore/Stdlib/kotlin/reflect/KProperties.kt` を退避して true master でも同一クラッシュが再現することを確認済みで、本タスクの source 移行に起因する regression ではない pre-existing のランタイム欠陥。今回修正できない理由: property-reference の実行時ハンドル表現と vtable/itable dispatch 機構（`__kk_kproperty_stub_*` ブリッジと reference lowering）の見直しが必要で、KProperty 殻の型宣言移行という本 PR のスコープを大きく超えるため見送り
- [ ] BUG-163: 期待引数型が `Any` の位置ではラムダ引数の型注釈による narrowing を検出できない（`val f: (Any) -> Int = { s: String -> s.length }; f(42)` が通り `0` を出力する。本家 kotlinc は initializer type mismatch）。BUG-046 の修正で `ExprTypeChecker+NameLambdaAndCallableRefInference.swift` の `lambdaAnnotationIsCompatible` は注釈が具体的な期待引数型を狭める場合に `KSWIFTK-SEMA-0025` を出すが、未解決の型変数が残った位置の引数型も `Any` にフォールバックする（例: `Grouping<K, E>.fold` の accumulator。`Scripts/diff_cases/collection_hof.kt`）ため、宣言された `Any` と推論が諦めた `Any` を区別できず、`Any` は一律で非権威として扱っている。修正には型推論側で「未解決型変数由来の `Any`」を `Any` と区別できる表現（型変数を保持したまま lambda 推論へ渡す、または defaulted フラグ）が必要で、BUG-046 の修正スコープを超える。`Any` 以外の具体型への narrowing（`(CharSequence) -> Int = { s: String -> ... }`）と nullability を落とす注釈（`(String?) -> Int = { s: String -> ... }`）は検出できている。発見元: BUG-046 修正 PR #5072 の完了ゲート検証
- [ ] BUG-164: `fun interface` 型のパラメータを取る関数に callable reference を実引数として渡すと `KSWIFTK-SEMA-0002: No viable overload found for call.` になる（同じ位置にラムダリテラルを渡せば SAM 変換されて通る）。`CallTypeChecker+SAMCalls.swift` の `isSamConvertibleArgument` は callable reference も SAM 変換候補として扱うが、Path 1（パラメータが fun interface 型の関数呼び出し）では引数を `expectedType = interfaceType` で推論するだけで、`ExprTypeChecker+NameLambdaAndCallableRefInference.swift` の `inferCallableRefExpr` は期待型が関数型でない場合に関数型のまま束縛し `markSamConversion` も行わないため、オーバーロード解決で interface 型パラメータに適合しない。最小再現: `fun interface IntOp { fun apply(a: Int, b: Int): Int }` / `fun useOp(o: IntOp): Int = o.apply(10, 4)` / `fun myCompare(a: Int, b: Int): Int = a - b` の後 `useOp(::myCompare)`。発見元: BUG-048（callable reference の SAM 変換 KIR lowering 修正）の検証中。BUG-048 のスコープ外 — SAM コンストラクタ経路以外での callable reference の SAM 変換マーキングを Sema のオーバーロード解決側に追加する必要があるため（番号は本マージ時点で BUG-163 が PR #5072 側と衝突していたため 164 に再採番）
- [ ] BUG-165: top-level に `if` 式で初期化されるプロパティを2つ以上宣言すると、コンパイル中（LLVM codegen 段）に `kswiftc` 自身が SIGSEGV でクラッシュする。最小再現: `val a = if (1 > 2) 1 else 2` と `val b = if (1 < 2) 3 else 4` を並べて宣言し `fun main() { println(a); println(b) }` をコンパイルする（1つだけなら成功、`if` 以外の初期化子と混在させても成功、class member プロパティは複数あっても成功）。ブロック `{ }` の有無とは無関係で、バックトレースは `libLLVM` 内。発見元: BUG-047（`parseTail` の複数ブロック取りこぼし）修正時の diff_cases ケース作成中に発見。検証環境は LLVM 14（CI は LLVM 18）のため環境依存の可能性があり、LLVM 18 環境での再現確認が最初のステップ。本 PR のスコープ外 — パーサ修正とは別レイヤ（KIR/codegen）の調査が必要なため
- [x] BUG-166: 既に完了した `async` の `await()` が子の例外を握り潰す。`kk_kxmini_async_await`（`Sources/Runtime/RuntimeCoroutine.swift`）は caller continuation 付きの呼び出しで `awaitResult(callerState:)` が `.completed(result, thrown)` を返した場合に `thrown` を捨てて `result`（例外時は 0）を返していたため、`await()` を呼ぶ前に子タスクが失敗し終えていると呼び出し側の状態機械が例外を観測できず、`coroutineScope { async { throw ... }.await() }` の catch 節が実行されなかった。子がまだ完了していない場合は completion resumer 経由（`callerState.resume(withException:)`）で正しく伝播するため、CI のロード状況次第で成否が変わる不安定な失敗として現れていた（`Scripts/diff_cases/coroutine_scope_wrapper_regression.kt` の `caught: child-fail` 行欠落。本 PR と無関係な `devin/1785117990-fix-bug-048` の CI run 30231394084 でも同一 diff で失敗）。修正: 完了済み分岐で `thrown != 0` のとき `callerState.thrownException` に公開して `signalResume()` し suspended トークンを返す（`kk_coroutine_call_direct_suspend` と同じプロトコル）。最小再現/回帰: `Scripts/diff_cases/async_await_completed_child_exception.kt`、`RuntimeCoroutineStateTests.testAwaitPropagatesExceptionFromAlreadyCompletedTask` / `testAwaitReturnsResultDirectlyForAlreadyCompletedTask`。発見元: KSP-672（PR #5051）のマージキュー CI 失敗調査
- [ ] BUG-168: REFACT-009（boxing/unboxing call を emit する重複パターンの `emitNonThrowingCall` ヘルパーへの集約リファクタ）のリファクタ PR 必須ゲート実行中（`bash Scripts/swift_test.sh`）に、リファクタとは無関係な `CompilerBackendTests.CodegenBackendIntegrationTests` 配下 13 件の既存失敗を発見。うち `testCodegenArrayAnyAllNoneCount` は REFACT-009 の変更を全て `git checkout` で退避させた HEAD 単体でも同一の実行時 SIGSEGV（exit 139）が再現することを確認済み — リファクタとは無関係な pre-existing バグ。残り12件は個別の変更前後比較までは実施していないが、症状がクラッシュ種別・アサーション不一致・リンク失敗と多岐にわたり単一原因とは考えにくいため、同様に無関係な既存バグ群である可能性が高い。テスト名と観測した症状:
  - `testCodegenCollectionNoneEdgeCases`: 実行時 exit code 11
  - `testCodegenArrayAnyAllNoneCount`: 実行時 exit code 11（SIGSEGV, 139）。最小再現: `fun main() { val nums = arrayOf(1, 2, 3, 4, 5); println(nums.any { it > 4 }) }`（`.build/debug/kswiftc` でのコンパイル自体に約60〜80秒かかり異常に遅く、生成された実行ファイルを実行するとクラッシュする）
  - `testCodegenBuildStringBuilderProducesMutableBuilder` / `testCodegenBuildStringCapacityProducesCorrectly`: クラッシュではなくロジック不一致。例外を捕捉するはずの分岐で出力が期待値 `"caught"` ではなく `"bad"` になる
  - `testCodegenCollectionConstructorsCopySourceElements` / `testCodegenCompilesAppendableAppendOverloads`: 実行時ランタイムパニック `KSWIFTK-RUNTIME-0001: Virtual dispatch failed: method not found in vtable/itable`
  - `testCodegenCompilesSequenceWindowedTransformOverload`: 実行時 exit code 10
  - `testCodegenIterableJoinToStringUsesRuntimeDefaultsAndNamedArguments`: リンクフェーズで `KSWIFTK-LINK-0001` / `outputUnavailable`
  - `testCodegenCollectionRunningReduceIndexedUsesListRuntimeForParameterReceiver`: 実行時 `KSWIFTK-LINK-0003: Unhandled top-level exception`（stdout に `[1, 4, 9]` までは出力される）
  - `testLLVMBackendEmitsFlatReplaceFirstCharRuntimeCallForStringOverload` / `testKotlinTextRemovePrefixSuffixCharSequenceEdgeCases` / `testCodegenCollectionRunningReduceUsesListRuntimeForParameterReceiver` / `testCodegenCompilesSequenceChunkedTransformOverload`: `swift_test.sh` の並列実行ログでは個別 XCTest の詳細エラー本文が省略され未確認（`bash Scripts/swift_test.sh --skip-build --filter 'CompilerBackendTests.CodegenBackendIntegrationTests/<テスト名>$'` で個別再実行して要確認）
  発見元: 本セッション（REFACT-009 のリファクタ PR 必須ゲート `bash Scripts/swift_test.sh` 実行中）。なお同一調査の過程で `CallLowerer+UnresolvedMemberCalls.swift` の `unresolvedCollectionMemberCallee` に `reduceRightOrNull` の `switch` case が丸ごと欠落しており Set/Iterable レシーバの `reduceRightOrNull` がリンクエラーになるバグ（`reduceRight`/`reduceIndexed`/`reduceRightIndexed`/`reduceRightIndexedOrNull` は個別 case を持つのに `reduceRightOrNull` だけ抜けていた）も発見したが、これは1行規模の修正で完結したため本 PR 内で直接修正済み（本バグバックログには追加していない。回帰は既存の `testCodegenCollectionReduceRightOrNullReadsIterableReceivers` がカバー）。今回修正できない理由: 13件それぞれが異なる根本原因を持つ独立した調査になる可能性が高く、REFACT-009（コード重複排除のみを目的とした機械的リファクタ）のスコープを大きく超えるため見送り。着手時は各テストを個別実行して再現性を確認し、変更前 HEAD との比較で既存バグと切り分けてから調査すること（番号は本マージ時点で BUG-165 が master 側の別内容と衝突し 166 に再採番→さらに BUG-166 も master 側の別内容（async await 例外握り潰し）と衝突したため 168 に再採番）
- [x] BUG-169: nullable `String?` に格納された空文字列 `""` が `== null` / `!= null` 比較で `null` と混同される（`== ""` 等の内容比較自体は正しい）。String はこの ABI 上 4 語の flat aggregate（`{data, length, byteCount, hash}`）値型でヒープポインタを持たないため、`CallLowerer+Operators.swift` の `lowerBinaryExpr`（`.equal`/`.notEqual` 分岐、L329-387）は null リテラル相手の比較であっても identity/センチネル判定を行わず、null 側を `.constValue(..., value: .null)` 型の「null string aggregate」として合成し `kk_string_equals_flat` へ両辺とも送って内容比較させる設計になっている。ところが `Runtime/RuntimeStringHOF.swift` の `kk_string_equals_flat`（L612-626）は両辺を `runtimeStringFromFlatFields`（`RuntimeStringArray.swift` L283-295、`data` が nil なら `""` を返す）で Swift `String` へデコードしてから比較しており、`data` が nil（null リテラルの aggregate 表現）の場合と、実際の空文字列（`RuntimeFlatStringStorage.init` は `byteCount == 0` でも常に 1 バイトの実バッファを確保する。`RuntimeStringArray.swift` L246-263）の場合を区別せず、両方とも Swift の `""` に潰してしまう。結果 `kk_string_equals_flat("", nullAggregate)` が `true` を返す。最小再現: `fun getEmpty(): String? { val x: String = ""; if (true) { return x }; return null }; fun main() { val r: String? = getEmpty(); println(r == null); println(r != null); println(r == "") }` は kotlinc `false`/`true`/`true` に対し kswiftc `true`/`false`/`true`（1・2行目が誤り）。`diff_kotlinc.sh` で real kotlinc 2.4.10 と照合し確認済み。boxed/non-flat 経路の `kk_string_equals`（`RuntimeStringHOF.swift` L604-610）はデコード前に `otherRaw == runtimeNullSentinelInt` を判定しており影響を受けない — flat ABI の演算子 lowering 経路のみの欠陥（`NativeEmitter+EmissionConstants.swift` の `emitStringAggregateEquality` も同型の並行 LLVM 直接 lowering 経路で同じ欠陥を共有している可能性が高い）。BUG-162（boxed `Double -0.0` と `Int64.min` センチネルのビットパターン衝突）とは無関係の別メカニズム — あちらは数値センチネル比較の誤り、こちらは 4 語 aggregate から `String` への非可逆デコードで null/非null 情報自体が失われることが原因。発見元: KSP-408（contains/indexOf 系の Kotlin 化）で、`Collection<String>` 引数に空文字列 needle を含む既存 diff_cases 4 件（`string_findanyof.kt`/`string_findlastanyof.kt`/`string_indexofany.kt`/`string_lastindexofany.kt`）が回帰し、内部ヘルパーが `String?` を返し `null` 比較で「見つからなかった」を表現していたことが原因と判明したため。KSP-408 では該当ヘルパー（`stringCollectionMatchAt`）を `Int` を返し `-1` を「見つからない」とする `stringCollectionMatchIndexAt` へ設計変更してこのバグの影響を局所的に回避しており、KSP-408 のスコープでは本体修正は行っていない。本体修正には `lowerBinaryExpr` の null リテラル比較（flat String 用の識別可能なセンチネル値の導入、または nil ポインタ情報を保持したままの専用比較パス新設）と `emitStringAggregateEquality` 側の同時見直しが必要で、String の flat ABI 表現そのものに関わる横断的な修正となるため KSP-408 の安全なスコープを超える

- [ ] BUG-170: 消去された型パラメータ `T : Comparable<T>` の受け手上でユーザー定義クラスの `<`/`<=`/`>`/`>=` を評価すると、`compareTo` を呼ばずヒープポインタの生アドレス比較にフォールバックし不定な結果を返す（プリミティブ・`String` は正常）。`CallLowerer+Operators.swift` の `lowerBinaryExpr` は `chosenCallee` が `Comparable.compareTo` かつレシーバ静的型が `.typeParam`（`shouldLowerComparableTypeParamViaRuntime`, L741-756）の場合、コンパイル時に解決できない compareTo 実装を消去型として扱い `kk_compare_any(lhsRaw, rhsRaw)`（`RuntimeStringComparison.swift` L4-55）へ委譲する設計になっている。しかし `kk_compare_any` は String / Float / Double / Int / UInt / Bool box の専用分岐のみを持ち、それ以外のオブジェクトポインタ（ユーザー定義 `Comparable` クラスなど）は他のどの分岐にもマッチせず最終フォールバック `return lhsRaw < rhsRaw ? -1 : 1`（L54、ヒープ確保アドレスの生ポインタ値比較）に落ちる。ユーザー定義 `compareTo` は一切呼ばれない。最小再現: `class Version(val major: Int, val minor: Int) : Comparable<Version> { override fun compareTo(other: Version): Int = ... }` の後 `fun <T : Comparable<T>> maxOfTwo(a: T, b: T): T = if (a >= b) a else b` を `maxOfTwo(Version(1, 2), Version(1, 5))` で呼ぶと、期待値 `Version(1, 5)`（`.minor == 5`）に対し実際にはヒープアドレスの大小関係次第で `Version(1, 2)`（`.minor == 2`）が返る（`maxOfTwo(3, 7)` や `maxOfTwo("apple", "banana")` のような非消去型（具体的な同一型が静的に分かる場合）は `chosenCallee` の直接呼び出し経路を通るため正常）。`diff_kotlinc.sh` で real kotlinc 2.4.10 と照合し確認済み（`Scripts/diff_cases/ksp669_comparable_randomaccess.kt` の `println(maxOfTwo(v1, v2).minor)` 行）。BUG-161（`List<UserComparable>.sorted()` が SIGSEGV する、`runtimeCompareValues`→ユーザー `compareTo` 呼び出し時の ABI 不整合が原因）とは別の runtime エントリポイント（`kk_compare_any` は `kk_list_sorted`/`runtimeCompareValues` からは呼ばれない、消去型パラメータ上の比較演算子 lowering 専用の経路）で、かつ症状も異なる（BUG-161 はクラッシュ、本件はクラッシュせず不定な誤った値を返す）ため独立した欠陥として起票。発見元: KSP-408（contains/indexOf 系の Kotlin 化）の完了ゲートで `bash Scripts/diff_kotlinc.sh Scripts/diff_cases` フルスイートを実行した際、既存の `ksp669_comparable_randomaccess.kt`（KSP-669 で追加、BUG-161 のクラッシュ行のみ除外済みだったが本行は当時見落とされていた）で新たに検出。KSP-408 は String の contains/indexOf 系のみが対象で、本件が関わる箇所（`CallLowerer+Operators.swift` の compareTo desugaring・`kk_compare_any`）は一切変更していない。該当行（`println(maxOfTwo(v1, v2).minor)`）は `diff_kotlinc.sh` フルスイートを green に保つため同ケースから除外済み。今回修正できない理由: `kk_compare_any` にユーザー定義オブジェクトの `compareTo` を仮想呼び出しする分岐を追加するには、消去型パラメータ経由で「レシーバの実行時型に応じた compareTo 関数ポインタ」を解決する仕組み（vtable/itable 経由の動的ディスパッチ）が必要で、BUG-161 の function-pointer ABI 是正と合わせて検討すべき runtime 比較基盤全体に関わる修正のため、KSP-408 の安全なスコープを大きく超える

- [ ] BUG-171: extension function 本体内で一部の String メンバを暗黙レシーバ（bare name、`this.` 無し）で呼び出すと型検査に失敗する（`this.` を明示すれば成功する）。`Sources/CompilerCore/Stdlib/kotlin/text/StringSearchReplace.kt` の `substringBefore`/`substringAfter` 等（`fun String.substringBefore(...)` 内で `this.indexOf(delimiter)` と書かれ、bare `indexOf(delimiter)` は使われていない）で発見・回避されている。発見元: KSP-407（substringBefore/After 系の Kotlin 化、#5104）。当該セッションはこの制約をコード内コメントで「BUG-170」という未登録の仮番号として記録していたのみで、TODO.md へのバックログ登録は行われていなかった。KSP-408 側でも同時期に別の内容へ「BUG-170」を採番していたため番号が衝突しており、KSP-408 の master マージ作業時に発見して本項として 171 に採番し直した上で正式にバックログへ追加した（コード側のコメントも本番号へ追従済み）。本セッションでは独立した最小再現やルートコーズの再調査は行っていない — 対象が String extension function 本体に限定される現象なのか、より広く任意の型の extension function に及ぶのかも含め未検証。今回修正できない理由: Sema の暗黙レシーバ解決（extension function 内の `this` バインディング）の調査が必要で、KSP-408（contains/indexOf 系の Kotlin 化）および本マージ作業のスコープを超えるため
- [ ] BUG-172: 静的型が `String`/`CharSequence` の値に対する 1-arg `lastIndexOf(Char)`（`startIndex`/`ignoreCase` 省略の完全省略形）を呼ぶと実行時に SIGBUS でクラッシュする。`String`-typed flat ABI dispatch へ誤ルーティングされることが原因と推定されている。回避策として `lastIndexOf(delimiter, this.length - 1, false)`（3-arg の完全展開形、kotlinc のデフォルト展開 `startIndex = lastIndex, ignoreCase = false` と等価）を明示的に呼べばクラッシュしない。`Sources/CompilerCore/Stdlib/kotlin/text/StringSearchReplace.kt` の `substringBeforeLast(Char)`/`substringAfterLast(Char)`/`replaceAfterLast(Char)`/`replaceBeforeLast(Char)` の4箇所がこの回避策を採用している。発見元: KSP-407（substringBefore/After 系の Kotlin 化、#5104）。当該セッションはコード内コメントで「BUG-171」という未登録の仮番号として記録していたのみで、TODO.md へのバックログ登録は行われていなかった。KSP-408 側の BUG-171（本ファイル、上記）と番号が衝突していたため、KSP-408 の master マージ作業時に発見して本項として 172 に採番し直した上で正式にバックログへ追加した（コード側のコメントも本番号へ追従済み）。本セッションでは独立した最小再現やルートコーズの再調査は行っていない。今回修正できない理由: `CharSequence.lastIndexOf(Char)` 1-arg オーバーロードの KIR/flat ABI dispatch ルーティングの調査が必要で、KSP-408 および本マージ作業のスコープを超えるため

**BUG-169/170 と BUG-168 の関係（KSP-408 の master マージ時点で追記）**: BUG-168 が報告する13件の既存失敗（クラッシュ・リンク失敗・ロジック不一致の混在）と BUG-170（消去 Comparable 比較の生ポインタ fallback）は別カテゴリの欠陥。BUG-168 の13件は KSP-408 側でも `swift_test.sh` フルスイート実行時に独立して同一の失敗として観測し、分岐元コミットからの独立ワークツリー再ビルドで同一内容が再現することを確認済み（BUG-168 の pre-existing 判定と整合）。

- [ ] BUG-173: `regex in charSequence`（`operator fun CharSequence.contains(regex: Regex): Boolean` を介した `in` 演算子解決）が常に `false` を返す。本家 kotlinc は `Regex("a.c") in "abc"` で `true` を返すが、kswiftc は `false` を返す（`diff_kotlinc.sh` で real kotlinc 2.4.10 と照合し確認済み）。発見元: KSP-408 の master マージ作業中、`ListSyntheticMemberLinkTests+MutableAndAdvancedMembers.swift` の `testContainsMembersAreMarkedOperatorFunctions()` が `sema.symbols.lookup(fqName: ["kotlin","text","contains"])`（単数形、複数候補があると解決順序に依存）経由で偶然 String の regex-contains 合成スタブを引き当てて落ちたことから、`HeaderHelpers+SyntheticStringStubs.swift` の `registerSyntheticStringExtensionFunction(named: "contains", externalLinkName: "kk_string_contains_regex_flat", ...)` が `flags:` を省略しデフォルトの `[.synthetic]`（`.operatorFunction` 無し）のまま登録されていたことが判明（本家 kotlinc は `public operator fun` として宣言）。**本 PR で対応済み**: 当該呼び出しに `flags: [.synthetic, .operatorFunction]` を明示し、フラグ自体は是正した（`Tests/CompilerCoreTests/Sema/ListSyntheticMemberLinkTests+MutableAndAdvancedMembers.swift` のテストは受信型（`CharSequence` vs `String`)で候補を判別する形に修正しグリーン化）。**ただし** 実行時の `in` 演算子解決自体は本 PR の修正後もなお `false` を返したまま（`diff_kotlinc.sh` で再確認済み、`Scripts/diff_cases/regex_in_operator.kt` で `in` 経由の2行はコメントアウトして除外・直接 `.contains(regex)` 呼び出しの2行のみ green を維持）。`ExprTypeChecker.inferContainsCallBinding`（`Sources/CompilerCore/Sema/TypeCheck/ExprTypeChecker.swift:521`）内の候補収集（`collectMemberFunctionCandidates` のノミナル型階層探索は `String` の `.stringStruct`（`.classType` ではない）表現とは相性が悪く見える一方、package-scope フォールバック `scopeCandidates`（`cachedScopeLookup` + `extensionSyntheticFallbackReceiverMatches`）が regex-contains 候補をなぜ拾えていないのかは未特定）に、operator フラグ修正だけでは解消しないより深い原因があると推定されるが、本セッションでは追加のデバッグ計装なしに特定できなかった。今回修正できない理由: `in` 演算子解決の候補探索パス（Sema）の踏み込んだ調査が必要で、KSP-408（String contains/indexOf 系の Kotlin 化）および本マージ作業の安全なスコープを超えるため。直接メソッド呼び出し形（`"abc".contains(regex)`）は影響を受けず正常動作する

**BUG-171/172 について（KSP-408 の2度目の master マージ時点で追記）**: いずれも KSP-407（#5104、`Sources/CompilerCore/Stdlib/kotlin/text/StringSearchReplace.kt`）が発見・回避策込みで実装していたが、コード内コメントの仮番号（170・171）が本 PR の BUG-169/170 と衝突していたため、本マージ作業でコメントを 171・172 に振り直しつつ本バックログへ正式登録した。両者とも KSP-408（String contains/indexOf 系）のスコープ外であり、本セッションでは独立検証を行っていない。

- [ ] BUG-174: bundled Kotlin source のトップレベル関数で、パラメータの型が「自分自身のパラメータに名前付きラベルを持つ関数型」（例: `(acc: Char, Char) -> Char` の `acc:`、`(value: Char) -> Boolean` の `value:`、`(index: Int, Char) -> R` の `index:`）であるとき、そのパラメータを関数本体内で名前呼び出しすると解決に失敗する。症状は状況により3通り: (1) Sema `KSWIFTK-SEMA-0023: Unresolved function '<param>'`、(2) `KSWIFTK-TYPE-0001: Type constraint could not be satisfied`、(3) ラベル名が同一コンパイル内の別トップレベル宣言のパラメータ名と偶然一致した場合、codegen がローカルのクロージャ値ではなく未定義のグローバルシンボル（例 `_transform`）への呼び出しを emit し、リンク時にのみ `Undefined symbols for architecture arm64` で失敗する。**arity は無関係**（重要な訂正: 当初「arity≥2 のラムダ引数呼び出しが壊れる」と誤診断したが、`(Char, Char) -> Char` のようなラベル無し arity 2/3 の関数型は問題なく動作し、`(value: Char) -> Boolean` のようなラベル付き arity 1 の関数型は同じ "Unresolved function" で失敗することを最小再現で確認済み。ラベルの有無だけが症状を分ける変数）。最小再現（bundled stdlib 側、例えば `Sources/CompilerCore/Stdlib/kotlin/text/` 配下に追加）: `public fun CharSequence.reduceRepro(reduceOperation: (acc: Char, Char) -> Char): Char { ...; accumulator = reduceOperation(accumulator, this[i]); ... }` をコンパイルすると `error KSWIFTK-SEMA-0023: Unresolved function 'reduceOperation'.` になるが、シグネチャを `(Char, Char) -> Char`（`acc:` ラベルを削除）に変えるだけで同じ本体がエラー無くコンパイル・実行できる。回避策: ラベルは Kotlin の関数型において呼び出し規約に影響しない純粋なドキュメント用シンタックスのため、upstream の `acc:`/`index:`/`value:` 等のラベルを落として同じパラメータ順序・型のまま宣言すれば動作する（KSP-410 の `StringHOF.kt` で実施）。発見元: KSP-410（String HOF Kotlin化）で `reduce`/`filterIndexed`/`mapIndexed` 等を upstream 準拠のラベル付きシグネチャで実装しようとして発見。当初 arity が原因と誤診断していたが、同セッション内の追加調査で真因を特定・訂正した。今回修正しない理由: Sema の関数型パラメータ解決（おそらく関数型のラベル付き引数と、それを保持するローカル/パラメータシンボルの紐付けロジック）に踏み込む調査が必要で、KSP-410 のスコープ（stdlib 移行）を超えるため、ラベルを落とす回避策で対処し本体の修正は見送り
- [ ] BUG-175: bundled Kotlin source のトップレベル関数で、単独の非有界ジェネリック型パラメータ `<R : Any>` を、nullable 戻り値（`R?`）のラムダ本体（`{ x -> if (cond) y else null }` の形）のみから、明示的な型引数や期待型無しに推論しようとすると `KSWIFTK-TYPE-0001: Type constraint could not be satisfied` になる。非 nullable なジェネリック（`map` の素の `<R>`）は影響を受けない。既存の `List<T>.mapNotNull`（型パラメータが2つあり `T` はレシーバから確定済み）も影響を受けないため、「レシーバ等の他の手がかりが無い状態で、nullable なラムダ戻り値のみから単独の型パラメータを推論する」ケースに特有の問題と見られる。最小再現: `public fun <R : Any> CharSequence.mapNotNullRepro(transform: (Char) -> R?): List<R>` のようなシグネチャを持つ bundled Kotlin 関数を、型引数無しで `"abc".mapNotNullRepro { c -> if (c == 'b') 1 else null }` のように呼び出すと発生（`mapNotNullRepro<Int> { ... }` のように明示的型引数を与えれば回避できる）。発見元: KSP-410（String HOF Kotlin化）で `mapNotNull`/`firstNotNullOf`/`firstNotNullOfOrNull` を Kotlin化しようとして発見。BUG-174（ラベル付き関数型パラメータの名前解決）とは症状も条件も異なる別バグと確認済み（ラベルの有無を変えても本バグは解消しない）。今回修正しない理由: ジェネリック型推論器がラムダ本体の nullable 戻り値から単独の型変数を解決する際の制約解消ロジックに踏み込む必要があり、KSP-410 のスコープを超えるため、当該3関数（`mapNotNull`/`firstNotNullOf`/`firstNotNullOfOrNull`）は `RuntimeStringHOF.swift` の既存 Swift 実装のまま維持する形で見送り
- [ ] BUG-176: bundled Kotlin source のトップレベル関数が `fun <R> X.f(transform: (Char) -> R): List<R>`（レシーバ型 X は任意、String に限らない）の形のとき、`R` が呼び出し側で `Char` または `Boolean` に具体化されると、変換結果を `List<R>` へ格納する箇所で **値が正しく box されず、生の unboxed スカラーが漏れる**（例: `"abc".map { it }` が `[a, b, c]` ではなく `[97, 98, 99]` を出力）。`R` が `Any`（型引数を明示的に `<Any>` にする、または `Any` としか推論されない状況）だと正しく box される一方、明示的に `<Char>` を指定しても具体的な `Char` である限り再現するため、「`R` が具体的な原始型ライクな型に narrow されると ABI lowering が不適切に unbox する」ことが真因と見られる。**arity は無関係**（BUG-174とは別バグ）で、**nullable 戻り値とも無関係**（BUG-175とも別バグ）。重要な限定条件: 変換結果を `List<R>` に格納・返却する関数でのみ発現し、`fold`/`reduce` のように `R` を関数自身の戻り値としてそのまま return するだけの形（`List` を経由しない）では発現しない（`"abc".fold('a') { acc, c -> ... }` は正しく動作することを確認済み）。最小再現: `public fun <R> CharArray.mapRepro(transform: (Char) -> R): List<R> { val result = mutableListOf<R>(); for (c in this) { result.add(transform(c)) }; return result }` を bundled Kotlin source に追加し `charArrayOf('a','b','c').mapRepro { c -> c.uppercaseChar() }` を呼ぶと `[65, 66, 67]`（本来 `[A, B, C]`）になる。KIR dump で確認すると、ラムダ本体の末尾に `call kk_unbox_char` が挿入されており、closure 自身の compiled body 内で不適切な unbox が発生していることを確認済み（`map`/`X.mapRepro` の呼び出し側コードに問題があるのではない）。`BooleanArray` でも同様に再現（`[false, true]` が `[0, 1]` になる）、`Int`/`IntArray` では再現しない（`List<T>.map<T,R>` のように要素型自体が型パラメータ `T` の場合も再現しない）。発見元: KSP-410（String HOF Kotlin化）で `map`/`mapIndexed` を Kotlin化し `diff_kotlinc.sh` フルスイート green を確認した後、`swift_test.sh` フルスイートの既存 XCTest `testCodegenStringMapVariants`（`Tests/CompilerBackendTests/Codegen/CodegenBackendIntegrationTests+StringHOFEdgeCases.swift`）で回帰を検出。`diff_cases` 側に `.map`/`.mapIndexed` を Char 保持変換で呼ぶケースが無かったため `diff_kotlinc.sh` では検出できなかった blind spot（`string_hof.kt` の `s.map { it.uppercase() }` は戻り値が String なので該当しない）。回帰防止として `Scripts/diff_cases/string_indexed_hof.kt` に `"abc".map { it }` 等のケースを追加済み。今回修正しない理由: ABI lowering（closure の box/unbox 挿入ロジック）に踏み込む調査が必要で KSP-410 のスコープを超えるため、`map`/`mapIndexed` は `RuntimeStringHOF.swift` の既存 Swift 実装（`kk_string_map(_flat)`/`kk_string_mapIndexed(_flat)`）へ revert して見送り

**BUG-174/175/176 について（KSP-408 の4度目の master マージ時点で追記）**: KSP-410（String HOF 26関数の Kotlin 化、#5205）が発見したバグ3件で、TODO.md 自体には正式登録されていたが（未登録の仮番号ではなく）、本 PR（KSP-408）が同時期に別内容へ BUG-169/170/171 を正式登録済みだったため番号が衝突しており、本マージ作業で 174/175/176 に採番し直した。3件とも KSP-410（String 高階関数の Kotlin 化）のスコープの話であり、KSP-408（contains/indexOf 系）とは無関係。本セッションでは独立検証を行っていない。

  **5度目の master マージ時点で追記**: origin/master が20コミット進んだ状態との再マージで、`Tests/RuntimeTests/RuntimeStringIndexOfLastTests.swift`（modify/delete）が唯一のコンフリクト。origin/master 側は XCTest → Swift Testing 移行（#5186）で内容修正のみだったが、全4テストが本 PR で削除済みの `kk_string_indexOfLast_flat` のみを検証する内容だったため、削除側で確定。マージ後再検証: Golden green（486秒、ホスト高負荷がほぼ解消）。`diff_kotlinc.sh` フルスイート `total=730 failed=0 passed=730 skipped=68` green。`swift_test.sh` フルスイートは17件失敗——前回ラウンドと完全に同一のテスト名セット（新規・消失とも無し）で、host-load 変動の影響を受けない決定論的な pre-existing 失敗と判断（5ラウンド連続で同一カテゴリのみ観測、本ラウンドで初めて件数・内訳とも完全一致）

  **6度目の master マージ時点で追記**: origin/master が20コミット進んだ状態との再マージで、コンフリクトは TODO.md のみ（BUG-177/178/179・BUG-180、詳細は該当項目）。マージ後再検証: Golden green（483秒）。`diff_kotlinc.sh` フルスイート `total=731 failed=0 passed=731 skipped=68` green。`swift_test.sh` フルスイートは17件失敗——前々回・前回と3ラウンド連続で完全に同一のテスト名セット。決定論的な pre-existing 失敗との判断を維持
- [x] BUG-177: `Direction.values()`/`Direction.entries`（および同一実装を独立に複製している `enumValues<T>()`/`enumEntries<T>()`）から読み出した**個々の要素**を `println` すると空行になる。最小再現: `enumValues<Direction>().forEach { d -> println(d) }` が `NORTH`/`SOUTH`/... ではなく空行を4回出力する（`Direction.entries` 経由の `for (d in Direction.entries)` や、コレクション全体をまとめて `println(Direction.entries)` する場合は元々正しく `[NORTH, SOUTH, ...]` を表示していた）。加えて、読み出した要素を実際の enum 定数と `==`/`when` で比較すると常に不一致になる（`enumValues<Direction>()[0] == Direction.NORTH` が `false` を返す）。原因: `appendEnumOrdinalArrayCreation`（`Sources/CompilerCore/Lowering/DataEnumSealedSynthesisPass+EnumSynthesis.swift`）と `lowerEnumEntryCollectionCallExpr`（`Sources/CompilerCore/KIR/CallLowerer+EnumStdlib.swift`、`values()`/`entries` 用と `enumValues<T>()`/`enumEntries<T>()` 用の独立した重複実装）が、Any 消去された裏配列の各要素に「boxed ordinal」ではなく `<Entry>$enumName()` の呼び出し結果（事前計算済みの名前文字列）をそのまま格納していた。他の全ての enum 値（`Direction.NORTH` 直接参照等）は raw ordinal Int として表現されるため、`println` が静的型からenumだと認識して発行する `$enumOrdinalToName(ordinal)` 呼び出し（`EnumNameAccessLoweringPass`）が、渡された文字列ハンドルを Int として unbox してゴミ値になり、switch がどの ordinal にもマッチせず既定の空文字列を返していた。`==`/`when`（`kirOp = .equal` の raw `icmp eq` 相当、`Sources/CompilerCore/KIR/CallLowerer+Operators.swift` / `Sources/CompilerCore/KIR/ControlFlowLowerer+WhenExpr.swift`）も同様に、Any 消去された配列から読み出した boxed ハンドルと `Direction.NORTH` の raw ordinal を素の word 比較しており、表現が食い違うため常に不一致だった。コレクション全体の `println` がこれまで正しく見えていたのは、要素の中身がたまたま名前文字列そのものだったからに過ぎない（`kk_println_any`/`runtimeRenderAnyForPrint` の汎用 Any 出力パスは boxed 値がどの enum の ordinal かを判定する仕組みを一切持たない）。修正: 両実装とも ordinal を新設の `kk_enum_box_ordinal(ordinal, name)`（`Sources/Runtime/RuntimeEnum.swift`）で boxing するよう変更。この関数は通常の `RuntimeIntBox` を作るため `kk_unbox_int`（`$enumOrdinalToName` 等が使う）はそのまま正しく ordinal を取り出せる一方、`RuntimeIntBox` に追加した `enumEntryName: String?` フィールドへ宣言時の名前もタグ付けし、`kk_println_any`/`runtimeRenderAnyForPrint`/`runtimeElementToString`（`Sources/Runtime/RuntimeStringArray.swift`, `RuntimeCollectionHelpers.swift`）の汎用 Any 出力パスがこのタグを見て名前を表示するようにした（静的型が判明している場合の `$enumOrdinalToName` 経路と、静的型が失われた汎用コレクション出力経路の両方が同じ boxed ordinal から正しい名前を復元できる）。`==`/`when` については、Any 消去配列から読み出した boxed ordinal と直接参照の raw ordinal の表現差を吸収するため、新設の共有ヘルパー `unboxIfEnumTyped`（`Sources/CompilerCore/KIR/KIRCallEmissionHelpers.swift`。静的型が non-null な具象 enum class の場合のみ `kk_unbox_int` を通す。raw ordinal に対しては no-op のため副作用なし）を `lowerBinaryExpr` の `.equal`/`.notEqual` と `lowerWhenConditionMatch` の両方に適用。回帰テスト: `Tests/CompilerBackendTests/Codegen/CodegenBackendIntegrationTests+EnumEdgeCoverage.swift` の `testCodegenEnumValuesEntriesElementAccessEqualityAndWhen`、`Scripts/diff_cases/enum_values_entries_element_access.kt`（forEach/for-in 個別要素・コレクション全体・`==`/`when` を一括カバー）。検証: `.build/debug/kswiftc` での手動実機確認（修正前は forEach 空行・`==` 常に false・`when` が意図しない分岐、修正後は全て正しい値）に加え、`Scripts/diff_cases/enum_values_entries_element_access.kt` を `bash Scripts/diff_kotlinc.sh`（実 kotlinc/JVM 比較）で PASS 確認済み。発見元: `EnumClass.values()/.entries` の Sema メンバ解決修正作業中に、個別要素アクセスのみ別の深いバグとして切り分けて報告された。**スコープ外として残した関連バグ**: BUG-178（`entries`/`enumEntries<T>()` の `[]` インデックスアクセス自体がクラッシュする、本修正とは別原因）、BUG-179（`val x: Any = Direction.NORTH` のように enum 定数を直接 Any 文脈へ渡す一般ケースは今回のボックス化の対象外で raw ordinal のまま漏れる）
- [ ] BUG-178: `Direction.entries[0]` や `enumEntries<Direction>()[0]`（`EnumEntries<T>` 型、内部表現は `RuntimeListBox`）のインデックスアクセスが `KSwiftK panic [KSWIFTK-LINK-0003]: Unhandled top-level exception` で実行時クラッシュする。最小再現: `enum class Direction { NORTH, SOUTH }` `fun main() { println(enumEntries<Direction>()[0]) }`。対照的に、同じ内容でも `values()`/`enumValues<T>()`（`Array<T>` 型、内部表現は `RuntimeArrayBox`）側の `[]` インデックスアクセス（`enumValues<Direction>()[0]`）は正しく動作する（`.build/debug/kswiftc` で実機確認済み）。`entries` プロパティ経由の `for (d in Direction.entries) { ... }`（forEach ではなく for-in ループ）はクラッシュせず正しく動作するため、`EnumEntries<T>`/`RuntimeListBox` そのものが壊れているわけではなく、`[]`/`get(index)` の呼び出し解決だけが誤ったパスに落ちていると見られる。原因は未特定だが、`EnumEntries<T>` が `docs/diff-skip-inventory.md` の DEBT-DIFF-007 節に記載の通りメンバー無しの空 interface として登録されている（`HeaderHelpers+SyntheticEnumStubs.swift` の `ensureEnumEntriesInterface`）ことと関連し、`[]` がフォールバック解決で `RuntimeListBox` ではなく配列前提のネイティブブリッジへ誤って配線されている可能性が高い（未検証の推測）。発見元: BUG-177 の検証中、`entries[0]`/`enumEntries<T>()[0]` を回帰テストの個別要素アクセス手段として使えるか確認する過程で発覚。今回修正できない理由: BUG-177 とは異なるレイヤ（`[]`/`get` のメンバー呼び出し解決）の別バグで、根本原因の特定からして別調査が必要なため、BUG-177 の修正スコープを超える。BUG-177 の回帰テストはクラッシュしない `enumValues<T>()`/`entries` の for-in ループ経由で個別要素アクセスをカバーしている
- [x] BUG-179: enum 定数を `Direction` 型のまま扱わず `Any` 型の変数・コレクションへ直接 widening すると、boxing が一切行われず raw ordinal のまま漏れる。最小再現: `enum class Direction { NORTH, SOUTH }` `fun main() { val x: Any = Direction.NORTH; println(x) }` は `NORTH` ではなく `0` を出力し、`println(listOf(Direction.NORTH, Direction.SOUTH))` は `[NORTH, SOUTH]` ではなく `[0, 1]` を出力する（`.build/debug/kswiftc` で実機確認済み）。原因: value class 向けの `resolveValueClassKind`（`Sources/CompilerCore/Lowering/ABILoweringPass+BoxingRules.swift`）や `BoxingCalleeTable`（`Sources/CompilerCore/Lowering/BoxingCalleeTable.swift`）、および `CollectionLiteralLoweringPass+FactoryPredicates.swift` の `primitiveBoxCalleeName` はいずれも `sym.flags.contains(.valueType)` でゲートされており、`enumClass` はこれを満たさないため常に「boxing 不要」経路へ落ちる。value class には用意されている「classType として見えるが実体は primitive」という ABI 境界での扱いが enum class には一切実装されていない。BUG-177 で追加した `kk_enum_box_ordinal`/`RuntimeIntBox.enumEntryName` によるタグ付けは `values()`/`entries` の裏配列生成という特定の呼び出し箇所にのみ適用したもので、この一般ケース（enum 定数が Any 文脈へ流れるあらゆる箇所）はカバーしていない。発見元: BUG-177 の調査中に静的トレースで発見し、本項目で実機検証して確定させた。**完了（2026-08-01）**: `resolveValueClassKind` を拡張し、value class と同様に非 null enum class の `.classType` kind を `.primitive(.int, .nonNull)` へ解決するようにした（enum 定数は Any 消去された文脈以外では常に raw ordinal Int として表現されるため）。ただし解決結果をそのまま `kk_box_int` として box すると BUG-177 の名前タグ付けが失われるため、box 呼び出し発行側の `emitBoxCallWithValueClassTag`（`Sources/CompilerCore/KIR/KIRCallEmissionHelpers.swift`）に、resolve 前の `rawSourceKind` を見て enum class なら代わりに `kk_enum_box_ordinal(ordinal, name)` を発行する分岐を追加した。`name` は enum class ごとに合成される `$enumOrdinalToName$<classSymbolID>(ordinal): String` ヘルパーを実行時に呼んで得る（コンパイル時に具体的な entry が分からない一般の値でも正しい名前を復元できる）。このヘルパー名にクラス自身の `SymbolID` を埋め込んでプログラム全体で一意にしているのは、`CollectionLiteralLoweringPass`（`listOf`/`setOf` 等の要素 boxing）が、このヘルパーを合成する `DataEnumSealedSynthesisPass` より**前**の lowering パスで走るため、ヘルパーの Sema シンボルがまだ存在しない時点で `symbol: nil`（codegen 側のコード生成時 bare-name 解決、`resolveUnnamedInternalFunction`）で呼び出さざるを得ないから（パス順序の変更は影響範囲が広いため見送り、名前ベース解決に倒した。`DataEnumSealedSynthesisPass+EnumSynthesis.swift`/`EnumNameAccessLoweringPass.swift` 側のヘルパー名構築も同じ `$<id>` 付与へ揃えた）。`Platform.OsFamily`/`RegexOption` 等のヘッダオンリー合成 enum（`ensureSyntheticPlatformEnumClass` 由来、`declSite: nil`・`flags: [.synthetic]`、`.nominalType` の KIR 宣言を持たないため `$enumOrdinalToName$<id>` が合成されない）は `sym.flags.contains(.synthetic)` で検出してタグなし box にフォールバックし、実機検証中に発覚したリンクエラー（`main` 冒頭の reflection 登録ボイラープレートが `kk_platform_osFamily` 等の合成 enum 戻り値を Any 文脈で参照する）を回避。当初の `resolveValueClassKind`/`emitBoxCallWithValueClassTag` の修正だけでは `sequenceOf(...)`/`MutableList<Any>.add`/配列要素代入 (`arr[i] = enumValue`)/`String.format(vararg)` 等の一部経路が直らないことが実機検証で判明した（`BoxingCalleeTable.boxCallee` を `resolveValueClassKind` を経由せず直接呼ぶ経路が `CallSupportLowerer.swift`（vararg 要素 boxing、`packVarargArguments`/その内部の `boxNonSpreadVarargArguments` 双方が使う `boxVarargElementIfNeeded` 2 実装）・`CallLowerer.swift`（generateSequence の KIR 特殊経路 2 箇所）・`CallLowerer+Operators.swift`（配列要素代入）・`CallLowerer+ClosureAdapters.swift`（generateSequence next 関数アダプタ）・`CallLowerer+LegacyMemberLikeCalls.swift`（`String.format` のハードコードされた primitive-only switch）に計 8 箇所存在した）。これらを共通ヘルパー `boxValueForAnySlot`（`resolveValueClassKind` → `BoxingCalleeTable.boxCallee` → `emitBoxCallWithValueClassTag` を1呼び出しに集約、`KIRCallEmissionHelpers.swift`）経由に統一し、`packVarargArguments` には `symbols: SymbolTable?` パラメータを追加してその4呼び出し元（`sema.symbols`）から配線した。回帰テスト: `Scripts/diff_cases/enum_any_widening.kt`（実 kotlinc/JVM 比較、`diff_kotlinc.sh` で PASS 確認済み）、`Tests/CompilerBackendTests/Codegen/CodegenBackendIntegrationTests+EnumAnyBoxing.swift`（`testCodegenEnumConstantWidenedToAnyIsBoxedNotRawOrdinal`＝本項目記載の最小再現そのもの、`testCodegenEnumConstantWidenedToAnyAcrossAllBoundaries`＝ローカル変数/関数引数/戻り値/data class フィールド/mixed enum classes 混在コレクション/`MutableList<Any>.add`/`Pair`/`sequenceOf`/nullable `Any?`/直接比較の広範カバレッジ）。検証: `bash Scripts/swift_test.sh` フル実行（既存失敗 26 件は本 PR の分岐元コミット単体でも完全に同一のエラーメッセージで再現する pre-existing なもので regression ではないことを隔離 worktree での再実行で確認済み）、`--filter Golden`（15 件全 green、golden fixture 差分なし）、`bash Scripts/diff_kotlinc.sh Scripts/diff_cases`（785 件中 712 件 PASS・0 件 FAIL・73 件既存 SKIP-DIFF、新規ケース含め全 green）。**スコープ外として残した関連バグ**: BUG-181（`generateSequence(seed, next)`/`sequence { yield(...) }` のバンドルソース実装内部でのジェネリック値格納が Any 消去 boxing されない。enum 固有ではなく `Char` 等プリミティブでも再現する既存の一般的 gap と確認済み）、BUG-182（Any 消去された enum 値への `is`/`as`/`KClass.isInstance` が boxing の有無に関わらずクラッシュする。`kk_enum_box_ordinal` が名前タグのみで nominal identity タグを持たないため）
- [ ] BUG-181: `generateSequence(seed: T, nextFunction: (T) -> T?)` および `sequence { yield(value) }` ビルダーで、シード/yield された値がジェネリック消去された内部ストレージ（`Sources/CompilerCore/Stdlib/kotlin/sequences/Sequences.kt` の匿名 `object : Sequence<T>` 実装内の `var item: T? = seed` 等）へ raw のまま格納され、Any 経由で取り出す際に boxing されない。最小再現: `generateSequence('A') { null }.toList()` は `[A]` ではなく `[65]`（Char の raw code point）を出力し、`sequence { yield('X'); yield('Y') }.toList()` は `[X, Y]` ではなく `[88, 89]` を出力する（`.build/debug/kswiftc` で実機確認済み。enum 固有ではなく `Char` で再現するため BUG-179 の enum 修正とは無関係の既存の一般的 gap と確認済み。`generateSequence(Direction.NORTH) { null }.toList()` も同様に `[0]` を出力する）。原因: `generateSequence`/`sequence`/`sequenceOf` はいずれも `Sequences.kt` のバンドル Kotlin ソースとして実装されており、Sema の呼び出し解決がこれらを `declSite` 付きの source-backed シンボルとして解決するため、`CallLowerer.swift`/`CallLowerer+ClosureAdapters.swift`/`CollectionLiteralLoweringPass+CallRewriteFactories.swift` に存在する複数の defense-in-depth 的な KIR-lowering-time boxing 特殊経路（`!isSourceBacked(...)` 等でガードされている）がいずれも未到達になる。一方 `ABILoweringPass.typeParamBoxingBoundaryCallees`（`kk_pair_new`/`kk_mutable_collection_add` 等、素の generic 引数を Any 消去ストレージへ書き込むことが分かっている特定のネイティブ `kk_*` callee だけに boxing 対象を限定するアローリスト）には `generateSequence`/`sequence` 自体が含まれておらず、汎用の引数 boxing 判定（`applyArgumentBoxing`）も一切トリガーされない。`sequenceOf(vararg elements: T)` は同じくバンドルソースだが vararg パラメータの boxing は `CallSupportLowerer.packVarargArguments`/`boxNonSpreadVarargArguments` という別経路（BUG-179 で修正済み）を通るため影響を受けない。発見元: BUG-179 の広範な回帰確認中に発見。今回修正できない理由: 修正するには、ネイティブ callee のアローリストに限定された現行の `typeParamBoxingBoundaryCallees` の設計を任意のバンドルソース関数呼び出しへ一般化する（あるいは callee 側の実装を静的解析して「T が Any 消去ストレージへ格納されるか」を判定する）必要があり、enum の Any 境界 boxing を主題とする BUG-179 のスコープを大きく超えるジェネリック消去 boxing 全体の再設計になるため
- [ ] BUG-182: `Any` 型の変数へ widening された enum 定数に対する `is`/`as`/`KClass.isInstance` が、boxing の有無に関わらずクラッシュ/未定義動作になる。最小再現: `enum class Direction { NORTH, SOUTH, EAST, WEST }` `fun main() { val boxed: Any = Direction.WEST; val unboxed = boxed as Direction; println(unboxed) }` が `KSwiftK panic [KSWIFTK-LINK-0003]: Unhandled top-level exception` でクラッシュする（BUG-179 修正前の raw ordinal のまま漏れる状態でも、修正後の `kk_enum_box_ordinal` で boxing された状態でも同一クラッシュを確認済み。BUG-177 の元となった修正単体でも再現するため BUG-179 由来の regression ではない pre-existing の別バグ）。原因（推測、未特定）: 通常の class/interface に対する `is`/`as` は `RuntimeTypeCheckToken`（`Sources/CompilerCore/KIR/RuntimeTypeCheckToken.swift`）が `stableNominalTypeID`（FQN のハッシュ）をトークンへ埋め込み、value class は追加で `kk_tag_value_class_box`（`emitBoxCallWithValueClassTag`）が boxed 値自体に同じ `stableNominalTypeID` をタグ付けすることで `kk_op_is` が両者を突き合わせられるようにしているが、`kk_enum_box_ordinal`（BUG-177、`Sources/Runtime/RuntimeEnum.swift`）が作る `RuntimeIntBox` は表示用の `enumEntryName: String?` のみを保持し、nominal identity 判定に使える tag を一切持たない。発見元: BUG-179 の広範な回帰確認中に発見（`as Direction` の round-trip を検証しようとして発覚）。今回修正できない理由: 修正するには `kk_enum_box_ordinal` に `stableNominalTypeID` 相当の tag を追加し（あるいは `kk_tag_value_class_box` と統合し）、`kk_op_is`/`kk_op_cast`/`kk_op_safe_cast`/`KClass.isInstance` 側の nominal 判定ロジックを enum の boxed ordinal ケースに対応させる必要があり、BUG-177 が確立した「boxed ordinal は表示専用タグのみ持つ」という設計そのものに手を入れる規模の変更になるため、BUG-179（Any 境界での boxing 判定）のスコープを超える
- [ ] BUG-183: `Byte` と `Short`（および `Byte`/`Short` と `Int`）で区別されるべきオーバーロードが `KSWIFTK-SEMA-0001: Duplicate JVM-erased callable declaration in the same package scope.` で拒否される。最小再現: `fun f(x: Byte): String = "byte"` / `fun f(x: Short): String = "short"` の 2 宣言だけで 2 つ目の宣言が SEMA-0001 になり、続く呼び出しも `KSWIFTK-SEMA-0003: Ambiguous overload resolution.` になる（本家 kotlinc 2.3.10 では JVM ディスクリプタが `B`/`S` で異なるため両方受理され `byte`/`short` を出力することを実機で確認済み）。原因: KSwiftK の `PrimitiveType`（`Sources/CompilerCore/Sema/TypeSystem/TypeModels.swift`）に signed の byte/short が無く、`Byte`/`Short` の型注釈が `resolveBuiltinTypeName`（`Sema/TypeCheck/Helpers.swift`）・`Sema/DataFlow/Analysis.swift`・`CompilerKnownNames.swift` のいずれでも `types.intType` へ解決されるため、`hasSameJVMErasedCallableSignature`（`Sema/DataFlow/HeaderHelpers+JVMErasedCallableConflict.swift`）から見て 3 者が同一シグネチャになる。unsigned 側は `ubyte`/`ushort` が独立プリミティブとして存在するのに対し signed 側だけが欠けている非対称な状態。発見元: KSP-645（`kotlin.experimental` の Byte/Short 版 and/or/xor/inv の Kotlin ソース化）— 本家準拠の `Byte.and(Byte): Byte` と `Short.and(Short): Short` を同一ファイルに宣言した時点でこのエラーに当たり、KSP-645 が現行型モデルでは実装不能であることの直接原因でもある。今回修正できない理由: `PrimitiveType` に `.byte`/`.short` を追加してリテラル型付け・算術昇格（Byte + Byte → Int）・`IntegerNarrowingPass`・ABI/codegen の格納幅・boxing・`ByteArray`/`ShortArray` の要素型・`toByte()`/`toShort()` の戻り型・Int/Long/UInt/ULong/UByte/UShort を列挙している各特例テーブルまで一斉に波及させる必要があり、diff ケース全件への回帰リスクを伴う型システム基盤の変更になるため、KSP-645（stdlib 1 ファイルの Kotlin ソース化）のスコープを大きく超える

**BUG-177/178/179 について（KSP-408 の6度目の master マージ時点で追記）**: いずれも「Fix BUG-171: box values()/entries elements as ordinals, not name strings (#5209)」が発見・修正した enum の `values()`/`entries` 関連バグ3件で、当該 PR は自身の直前に master へ着地した KSP-410 の BUG-169/170/171 に続く番号として BUG-172/173/174 を採番していた（master 自身の履歴内では無衝突）。しかし本 PR（KSP-408）はそれより前に別内容へ BUG-169〜173 を、直前のマージで KSP-410 の 169/170/171 を 174/175/176 に採番し直し済みだったため、今回のマージで再度衝突。3件を 177/178/179 に採番し直し、内部の相互参照（BUG-172→177 等）も追従した。3件とも enum の `values()`/`entries` 個別要素アクセスに関するバグで KSP-408（contains/indexOf 系）とは無関係。本セッションでは独立検証を行っていない。

**別セクションに紛れていた BUG-169 の衝突（同マージ時点で追記）**: ファイル冒頭「Sema 型チェック」節の単発エントリとして「enum companion object の静的関数呼び出しが解決できない」バグが `BUG-169` で登録されており（DEADCODE-014 監査、2026-07-31発見）、本 PR の主バックログの `BUG-169`（空文字列 vs null）とは無関係ながら番号が衝突していた。この衝突は git の自動マージでは検出されない位置（ファイル内の離れたセクション）にあったため、`grep -oE '^\- \[.\] BUG-[0-9]+' TODO.md | sort | uniq -c` で重複を機械的に確認して発見。BUG-180 に採番し直した。なお同じチェックで BUG-049/162/164 が各2回ずつ使われている既存の重複（本 PR や今回のマージとは無関係、origin/master 自身の履歴に元から存在）も発見したが、これは対象・スコープが不明な独立した棚卸し作業になるため本 PR 内では修正せず別タスクとして切り出した。

**BUG-179/181/182 について（BUG-173 のローカルブランチを master へリベースした時点で追記）**: enum の Any 境界 boxing 修正はローカルでは「BUG-173」として作業していたが、master 側で先行する複数回の番号衝突解消により同一バグが既に `BUG-179`（未着手）として登録済みだったため、リベース時に master の BUG-179 エントリへ完了マークと修正内容を統合する形で解消した。ローカルで「BUG-174」「BUG-175」として追加した2件のフォローアップも master 側の別内容（BUG-174: ラベル付き関数型パラメータ、BUG-175: nullable ラムダ戻り値からのジェネリック推論）と番号が衝突していたため、末尾の空き番号 181・182 に採番し直した。
