val a = 10
val b = 3
val sum = a + b
val diff = a - b
val product = a * b
val quotient = a / b
val remainder = a % b
println(sum)
println(diff)
println(product)
println(quotient)
println(remainder)
val nested = (a + b) * (a - b)
println(nested)
val conditional = if (a > b) a else b
println(conditional)
